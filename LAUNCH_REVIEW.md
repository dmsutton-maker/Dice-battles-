# Launch review — handoff

**Status: FINAL — every review agent completed; sections A, A2 and B are fixed and shipped (v1.65.0, v1.66.0).** Generated from the verifier verdicts in the launch-review workflow (run `wf_82e1508f-191`), not written by hand, so the fix instructions below are the verifiers' exact words.

## WHERE THIS STANDS — read before doing anything below

The review is FINISHED and sections A, A2 and B are DONE, shipped as
v1.65.0 and v1.66.0 and deployed to the site. Do not work through them
again — every item in those sections is either fixed in the code or, in
the handful of cases where the verifier's suggestion was wrong or
harmful, deliberately not taken with a comment in the code saying why.

What is left is **sections C and D**, which are David's alone: decisions
about the product and things only he can do in App Store Connect, the
AdMob console and Apple's own forms. They were published for him as an
Artifact page on 7 Sep 2026. Nothing in C or D should be built by an
agent without him answering first.

Two things a future session should know:

- **The versions.** `app.json` says 1.60.0, `GAME_VERSION` says the
  current OTA version, and the App Store Connect record says 1.0. Three
  numbers, and which one becomes the public 1.0 is C4/C5/C17 — David's
  call, and permanent. `tests/submission.test.ts` pins the only relation
  that has to hold whichever way he goes: `app.json` never ahead of the
  JavaScript.
- **Ads are OFF** and no built binary contains the SDK, so
  `runtimeVersion` is correctly on the `sdkVersion` policy. Turning them
  on is a single co-ordinated change — see the two-state rule in
  AGENTS.md, which `tests/ads.test.ts` now enforces for every native
  package rather than only AdMob.

The review's own artefacts, if any of this needs re-deriving: the
verdicts live in the workflow journals for runs `wf_82e1508f-191` and
`wf_e779625a-9c7`, and this document is regenerated from them with
`python3 tools/launch-review/extract-verdicts.py <journal> /tmp/v.json &&
python3 tools/launch-review/make-handoff.py /tmp/v.json LAUNCH_REVIEW.md
"<status>"`. The renders the graphics section judged are under
`scratchpad/launch-review/{arenas,dice}/`; `render-all.sh` rebuilds them
and REMOVES the web packages afterwards, which is not optional — Expo
infers a web target from `react-dom` being present and `eas update`
then fails on the platforms array.

**Ship procedure for any further batch** (from AGENTS.md): `npm run
check` — which is typecheck, tests and the Metro bundle in one, and now
really does include the bundle — then `grep -c RNGoogleMobileAds` on the
exported `.hbc` must print 0, then a CHANGELOG entry, bump
`src/game/version.ts`, commit, `EXPO_TOKEN=… npx eas update --branch
main --message "vX.Y.Z — …"`, a row in the Supabase `changes` table in
plain words, and push. Site changes also need `cd hq && VERCEL_TOKEN=…
npx vercel deploy --prod --yes --scope suttonsteam`. Tokens live in the
conversation only, never on disk — this repository is public.

The bug-board routine (`trig_01TtJdRSGx9JjmoeFt991K3F`, 7am and 7pm ET)
fires into the session that created it; answer it by querying
`public.ideas where kind='bug'` on Supabase project
`eqdbvpnckriscvzalwix`.

## How the review was done

- The nine-area map is at `scratchpad/launch-review/maps/*.json`; two independent verifiers judged every blocker and major, with a tiebreaker where they disagreed; an art director looked at fresh renders of all 16 themed arenas and 53 dice skins, under `scratchpad/launch-review/{arenas,dice}/`.
- Read `AGENTS.md` (the rules) and `STATE.md` (the facts) before touching anything.
- Sections A, A2 and B below are a RECORD of what was fixed, not a list of work outstanding. Sections C and D are the outstanding work, and both are David's.


## A. Fix these yourself — safe, no decision needed (43)

Verified by two independent lenses (or a tiebreaker). Severity is the verifiers' launch calibration.

### 1. [major · small] arenas-dice: The Inventory/Store/Leaderboard thumbnails for Snowy Hollow, Sky Kingdom and Glow Glade were last generated on 26 Aug and do not reflect the 31 Aug palette repaints, so the picture a player buys or unlocks from is not the arena they get — and the Sky Kingdom thumbnail is still the white-on-white picture the review scored 4 and 3.

- **Evidence:** `git log -1 -- assets/arenas/{sky,glade,snow}.png` -> 67f2ddc 2026-08-26 (city.png alone was regenerated 27 Aug in 4f6d915). 67f2ddc's themeData had snow `meadow: '#e9f1f6', hill: '#dae7f0'`, sky `'#a8d4f0'/'#8fc4ea'`, glade `'#3f7a58'/'#33684a'`; current src/arena/themeData.ts:211 snow `'#e7f0f6'/'#c8d9e8'`, :453 sky `'#9fd0f2'/'#5b9fd8'`, :619 glade `'#2b5747'/'#1e453c'`, all commented '31 Aug 2026'. PIL exact-colour check on opaque pixels: snow.png has #e9f1f6 and #dae7f0 (old) and not #e7f0f6/#c8d9e8 (current); glade.png has neither current colour; sky.png has none of #a8d4f0/#8fc4ea/#9fd0f2/#5b9fd8. WORSE than the map says: sky.png DOES contain snow's 26 Aug #e9f1f6 and #dae7f0, because make-themed-art.py:44 `theme_block` does `SRC.index('\n  sky: {')` and the first match is the `sky: {` field of the ArenaTheme TYPE at themeData.ts:143, whose block runs to the `\n  },` that ends the snow theme — so Sky Kingdom has been painted with Snowy Hollow's palette since the file was written; re-running the script today reproduces the bug (parser returns meadow #e7f0f6 for 'sky'). tests/tutorial.test.ts:530-541 only asserts the generator source `includes('src/arena/themeData.ts')`, so neither drift nor the wrong-block bug is caught.

- **Do this:** 1) In /home/user/Dice-battles-/assets/arenas/make-themed-art.py, change `theme_block` (line ~44) to search only inside the themes object: compute `START = SRC.index('export const ARENA_THEMES')` (themeData.ts:201) and use `SRC.index(f'\n  {tid}: {{', START)`; do the same start bound in `sky_color` for safety. 2) Run `python3 assets/arenas/make-themed-art.py` from /home/user/Dice-battles-/ to regenerate all sixteen PNGs (PIL is installed; the script writes assets/arenas/<id>.png). 3) Add a test in /home/user/Dice-battles-/tests/tutorial.test.ts beside 'the themed pictures are painted from the themes themselves' that, for each id in IDS, parses `meadow` and `hill` from the theme's block in src/arena/themeData.ts (search after 'export const ARENA_THEMES') and asserts both exact RGB triples appear among opaque pixels of assets/arenas/<id>.png, using the same `execSync('python3 -c ...PIL...')` pattern as `stats()` at line 379. 4) Run `npm test` (the brightness floor and pairwise 0.28 distance tests must still pass with the new palettes, especially glade which got darker) and `npx tsc --noEmit`. 5) Add a `changes` row (area 'game'): 'The Sky Kingdom shelf picture was painted in Snowy Hollow's colours by mistake, and three arena pictures were out of date — all sixteen now match the arenas.'

### 2. [major · small] config-release: runtimeVersion is the `sdkVersion` policy, resolving to `exposdk:54.0.0`, while AGENTS.md states as a rule that it is an explicit string "1.1.0" and never the policy. The rulebook describes a state that existed for one commit (5b6ef13, v1.41.0) and was reverted (a390b7e); the code and tests now enforce the opposite.

- **Evidence:** /home/user/Dice-battles-/app.json:43-45 `"runtimeVersion": { "policy": "sdkVersion" }`; /home/user/Dice-battles-/AGENTS.md:66-68 "**`runtimeVersion` is the only real gate.** It is an explicit string in `app.json` (`"1.1.0"`), never the `sdkVersion` policy". `git log -S'"policy": "sdkVersion"' -- app.json` → b688a73 (added), 5b6ef13 (removed, 25 Aug), a390b7e (restored, 25 Aug). a390b7e's message: "Pinning runtimeVersion stopped the crash but froze every over-the-air update: no binary reports runtime 1.1.0 yet ... runtimeVersion goes back to the sdkVersion policy, which is correct while there is no native code to gate" — and its --stat touches CHANGELOG, app.json, adSdk.ts, ads.ts, ads.test.ts but NOT AGENTS.md. tests/ads.test.ts:169-192 enforces the two-state pairing (explicit string only when ads are ON); in the OFF branch it asserts only `runtime !== undefined`, so a session that 'corrects' app.json to match AGENTS.md would pass tests while stranding every installed build from all future OTAs.

- **Do this:** Edit /home/user/Dice-battles-/AGENTS.md lines 66-69 (the paragraph beginning "**`runtimeVersion` is the only real gate.**") to describe the actual two-state rule now enforced by tests/ads.test.ts:169-192 and documented in src/game/adSdk.ts:17-41: runtimeVersion stays on the `sdkVersion` policy while the JS bundle contains no native module that older binaries lack (current state, ads OFF); it becomes an explicit string, raised, in the SAME change that puts such a module into the bundle (e.g. restoring the require in adSdk.ts:55), followed by a build. Explicitly warn that setting an explicit runtimeVersion with no matching binary freezes OTA delivery to every install (commit a390b7e). Keep the following bullets (lines 71-77) as they are — they are still correct.

### 3. [major · small] config-release: GMA app measurement is not delayed: GADDelayAppMeasurementInit is false, so once the SDK is compiled in it starts collecting on launch before JS ever sets tagForChildDirectedTreatment / maxAdContentRating — and with ads OFF in JS that configuration is never applied at all. This undercuts the App Privacy answers that AGENTS.md says are only true because of those flags.

- **Evidence:** node_modules/react-native-google-mobile-ads/plugin/build/index.js:62-67 `withIosAppMeasurementInitDelayed = (config, delayAppMeasurementInit = false) => ... config.modResults.GADDelayAppMeasurementInit = delayAppMeasurementInit`; lines 34-41 `withAndroidAppMeasurementInitDelayed` writes DELAY_APP_MEASUREMENT_INIT only when the option is defined. /home/user/Dice-battles-/app.json:60-66 passes only iosAppId/androidAppId. Introspection: GADDelayAppMeasurementInit = false; the only GMA meta-data in the Android manifest is APPLICATION_ID. Google's documented default is that app measurement starts at app launch unless this flag is true; the child-directed request configuration is only applied in src/game/ads.ts:239-243 immediately before `initialize()` at ads.ts:244, and with adSdk.ts returning null that code never runs. So in the current state a new binary collects measurement at launch with no child-directed tagging ever set.

- **Do this:** In /home/user/Dice-battles-/app.json, change the plugin entry at lines 60-66 to `["react-native-google-mobile-ads", {"iosAppId": "ca-app-pub-5310885665976703~4644888759", "androidAppId": "ca-app-pub-5310885665976703~4644888759", "delayAppMeasurementInit": true}]`. The plugin then writes GADDelayAppMeasurementInit=true (iOS) and com.google.android.gms.ads.DELAY_APP_MEASUREMENT_INIT=true (Android), so measurement waits for `initialize()`, which src/game/ads.ts:244 calls only after consent and after setRequestConfiguration with tagForChildDirectedTreatment/G rating (ads.ts:239-243). Optionally extend tests/ads.test.ts:169-192 to assert the plugin options include `delayAppMeasurementInit: true` whenever the plugin is present. This is a native change; it lands in the next build (which is needed anyway) and must go in the same change as any ads-ON switch.

### 4. [major · small] config-release: iPad is allowed to rotate to landscape even though the app declares portrait: with supportsTablet true and no ios.requireFullScreen, Expo writes all four UISupportedInterfaceOrientations~ipad and UIRequiresFullScreen=false (required for Split View). The portrait-designed board, split-screen two-player and popups will be exercised in landscape by App Review on iPad, since iPad screenshots are on the listing.

- **Evidence:** /home/user/Dice-battles-/app.json:7 `"orientation": "portrait"`, :11 `"supportsTablet": true`, no `requireFullScreen`. node_modules/@expo/config-plugins/build/ios/RequiresFullScreen.js:56-66: `if (isTabletEnabled && !requiresFullScreen) infoPlist[iPadInterfaceKey] = [...existing, ...requiredIPadInterface]` and `UIRequiresFullScreen: requiresFullScreen` (false). Introspection: UISupportedInterfaceOrientations = [Portrait, PortraitUpsideDown] but UISupportedInterfaceOrientations~ipad = [Portrait, PortraitUpsideDown, LandscapeLeft, LandscapeRight], UIRequiresFullScreen = false. The code assumes the opposite: /home/user/Dice-battles-/src/game/safeArea.ts:21-31 reads `Dimensions.get('window')` ONCE at module scope with the comment "Read once: the game is portrait-locked", and `grep useWindowDimensions|landscape src` finds no rotation handling anywhere. STATE.md:104-107 confirms six 2048x2732 iPad screenshots are live on the listing, so iPad will be reviewed. Whether the layouts visibly break in landscape cannot be verified without a device; that the app is NOT portrait-locked on iPad, contrary to its own comment, is verified.

- **Do this:** Add `"requireFullScreen": true` inside the `ios` object of /home/user/Dice-battles-/app.json (alongside `supportsTablet` at line 11). RequiresFullScreen.js then skips the landscape merge and writes UIRequiresFullScreen=true, so `orientation: portrait` holds on iPad too, matching the portrait-locked assumption in src/game/safeArea.ts:21-31. Trade-off to state in the commit: this opts the app out of iPad Split View/Slide Over, which Apple permits and is the norm for full-screen games. Native change — next build. Verify with introspection that UISupportedInterfaceOrientations~ipad is portrait-only. (Alternative — making every screen work in landscape — is large and is only worth it if David wants iPad landscape play.)

### 5. [major · small] config-release: CC-BY 4.0 music and cheer are shipped with no attribution anywhere a player or visitor can see it — only in a source comment. CREDITS.md marks this as a required release action.

- **Evidence:** /home/user/Dice-battles-/assets/sounds/CREDITS.md:11 cheer.wav — Gregor Quendel, CC-BY 4.0 "attribution required"; :14 music-monkeys/sneaky/polka.m4a — Kevin MacLeod, CC-BY 4.0 "attribution required"; :18-20 "**Action items for release:** the app's credits/about screen must include: 'Music by Kevin MacLeod (incompetech.com), licensed under CC-BY 4.0' / 'Crowd cheering by Gregor Quendel'". All four files are bundled: src/audio/sounds.ts:43-46 requires the three MacLeod tracks (plus music.m4a), cheer is in MIX at sounds.ts:59. `grep -rniE 'MacLeod|Quendel|incompetech' src hq/src store/*.txt README.md` → only the comment at src/audio/sounds.ts:40; `grep -rniE 'credits|attribution|licen[cs]e' hq/src -l` → nothing. The only About-like surface in the app is the version line at src/demo/DiceDemoScreen.tsx:1890 `<Text style={styles.versionLine}>{GAME_VERSION}</Text>`. Shipping CC-BY works without attribution is a licence breach, independent of App Review.

- **Do this:** In /home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx, directly below the version line at line 1890 (Settings tab), render two small credit lines using the existing `styles.versionLine` (or a sibling style with the same THEME tokens): "Music by Kevin MacLeod (incompetech.com), licensed under CC BY 4.0" and "Crowd cheering by Gregor Quendel, licensed under CC BY 4.0". Add a test next to 'Settings shows the version under Report a Bug' in tests/screen.test.ts asserting DiceDemoScreen.tsx contains both 'Kevin MacLeod' and 'Gregor Quendel'. This is pure JS, so it ships over the air now (CHANGELOG entry + `changes` row per AGENTS.md). Optionally mirror the same two lines on the Dice Battles app page in hq/ (website), which is a separate deploy. Where a fuller credits panel goes is David's call, but the licence text itself is not optional.

### 6. [major · small] gameplay: A cup run is a global: ANY battle started while a run is open — the Play tab's Start button, a tap on the board, or Play again — silently counts as the next bracket round, so a casual Easy loss knocks the player out of a 150-coin Grand Championship.

- **Evidence:** Both reviewers are right on the facts; I side with B on fixClass because the code documents the intent that the fix implements. src/demo/DiceDemoScreen.tsx:259-261 `// The cup being played, if any. A round started from a cup reports back // to it when it finishes.` — but :560 `if (runRef.current && outcome !== 'tie') {` → :563 `const next = advanceRun(runRef.current, cup, outcome === 'won');` has no notion of where the round was started. playCupRound :681-685 is `/** Start the next bracket round: same flow as a normal battle. */ ... setTab('play'); startCountdown();` — identical to the Start button (:1516 `onPress={startCountdown}`), board tap (:970) and Play again (:1252 `<Pressable style={styles.playAgainButton} onPress={startCountdown}>`). Worse than the map says: enterTournament :662-663 sets `difficultyRef.current = tournament.difficulty` once, but the Play tab's difficultyRow (:1516) stays live during a run, so even the Cups button plays a Hard cup at whatever difficulty is currently selected. Home overlay :1473-1545 shows no 'cup in progress' line. The only thing to decide is what 'Play again' after a cup round does, and the least-surprising answer (stay in the cup) needs no product call.

- **Do this:** In /home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx: (1) add `const cupRoundRef = useRef(false);` beside runRef (:351). (2) Give startCountdown a parameter `(origin: 'cup' | 'casual' | 'again' = 'casual')`: at its top set `if (origin === 'cup') cupRoundRef.current = true; else if (origin === 'casual') cupRoundRef.current = false;` (so 'again' keeps whatever the previous round was — Play again after a cup round continues the cup, Play again after a casual round stays casual). Wire :1516 Start and :970 board tap to `() => startCountdown('casual')`, :1252 Play again to `() => startCountdown('again')`, and playCupRound (:682-685) to `startCountdown('cup')`. (3) In playCupRound, before startCountdown, re-pin the cup's difficulty: `const cup = tournamentById(runRef.current?.tournamentId ?? ''); if (cup) { setDifficulty(cup.difficulty); difficultyRef.current = cup.difficulty; }`. (4) Change :560 to `if (runRef.current && cupRoundRef.current && outcome !== 'tie') {`. (5) In quitToMenu (:535) set `cupRoundRef.current = false;`. (6) On the home overlay, after the unlock line (~:1512), render `{run && !run.finished && (<Text style={styles.trophyNext} numberOfLines={1}>In the {tournamentById(run.tournamentId)?.name} — play the next round from Cups</Text>)}`. Add a CHANGELOG entry and a `changes` row ('A normal battle no longer counts as a cup round — only rounds started from the Cups tab do').

### 7. [major · small] gameplay: Cup difficulty is advertised but not enforced: after entering, the player can switch to Easy on the Play tab or on any result screen and play every Grand Championship round at Easy for the 500–800 coin prize (a normal Easy win pays 10–20).

- **Evidence:** src/demo/DiceDemoScreen.tsx:656-664 enterTournament sets `difficultyRef.current = tournament.difficulty;` exactly once. difficultyRow's onPress (:1214-1216) does `difficultyRef.current = d.id; playClick(); setDifficulty(d.id);` unconditionally, and difficultyRow is rendered on the home screen (:1516), the won screen (:1575), the tie screen (:1586) and the lost screen (:1906). playCupRound (:682-685) does not re-assert the cup's difficulty; startCountdown (:675) builds the layout from `difficultyRef.current` and finishRound (:554, :586-590) pays coins/trophies from it. Since AI cadence is identical at every difficulty (src/game/ai.ts:27 AI_ROLL_INTERVAL_MS = 2000), difficulty is only the obstacles, so Easy removes the mound and moat. TournamentScreen.tsx:124 shows `{AI_DIFFICULTIES[t.difficulty].label}`; tournament.ts:62 prize {500,800}. Note coins buy cosmetics only (currency.ts) and are not posted to Game Center, so this is an economy exploit, not a leaderboard one.

- **Do this:** In src/demo/DiceDemoScreen.tsx playCupRound (:682-685), before calling startCountdown, re-assert the cup's difficulty: `const cup = runRef.current ? tournamentById(runRef.current.tournamentId) : undefined; if (cup) { setDifficulty(cup.difficulty); difficultyRef.current = cup.difficulty; }`. Together with the cupRoundRef fix for the previous item this means only rounds launched from the Cups tab count and they always run at the advertised difficulty. If the cupRoundRef fix is NOT applied, put the same re-assertion at the top of startCountdown guarded by `runRef.current && !runRef.current.finished` instead, and hide difficultyRow on result screens while a run is open (`{!(run && !run.finished) && difficultyRow}` at :1575, :1586, :1906).

### 8. [major · small] gameplay: A finished run never clears itself: after being knocked out the Cups tab still says 'YOU ARE IN THE …' and offers 'Play the Final'; after winning it offers 'Play the Champion · 1 left in'. Those rounds play but advance nothing, and the only way to enter another cup is to tap 'Give up this run' — after winning it.

- **Evidence:** src/demo/TournamentScreen.tsx:53 `{run && active ? (` — never reads run.finished; :55 'YOU ARE IN THE'; :87-89 `Play the {roundName(active.size, run.wins)}`. src/game/tournament.ts:92-94 roundName returns 'Champion' when remaining <= 0, :103 playersLeft returns `Math.max(1, …)` = 1, and :112 `if (run.finished) return run;` so a further round changes nothing. DiceDemoScreen.tsx:563-564 `setRun(next)` stores the finished run; `grep -n 'setRun('` finds no other clear except onAbandon at :1656. There is also no knockout message anywhere: `grep -n knocked src/demo/DiceDemoScreen.tsx` returns nothing — only the champion case queues a RewardPopup (:566-577). tests/tournament.test.ts:77 covers advanceRun only.

- **Do this:** In src/demo/DiceDemoScreen.tsx finishRound (:563-564): replace `setRun(next);` with `setRun(next.finished ? null : next);` — the champion RewardPopup is already queued from `next`/`cup` locals, so clearing the run does not lose it. For the knockout, add a plain message so the player learns the run ended: inside the same block, `if (next.finished === 'knocked-out') { setRewards((queue) => [...queue, { emoji: cup.emoji, name: `Out of the ${cup.name}`, kicker: 'CUP OVER', note: 'Lose once and the run is over. Enter again from Cups whenever you like.' }]); }`. Belt-and-braces in src/demo/TournamentScreen.tsx:53 change the condition to `run && active && !run.finished ?` so a stale finished run can never show the Play button.

### 9. [major · small] gameplay: Two-player Color War never shows either player which colour is theirs, and the colours are hard-wired to Red (bottom) / Blue (top) every match; the ready card also tells both players 'First to rescue all six prisoners wins' in every mode, which is wrong for Color War (three) and Skirmish (most of a shared six).

- **Evidence:** src/demo/TwoPlayerScreen.tsx:229-232 `useRef<[PrisonerColorId, PrisonerColorId]>([PRISONER_COLORS[0].id, PRISONER_COLORS[1].id]).current` — colors.ts:30-31 are red and blue, and the ref is created once per mount so a rematch keeps them. :184-189 zoneHud renders only `{modeName} · {difficultyName} · {score} / {target}` plus 'catch up!' — no swatch. :440-442 `First to rescue all six prisoners wins.{'\n'}Tap fast. No mercy.` unconditionally. splitRules.ts:82-84 returns effect 'none' for a non-own colour and TwoPlayerScreen.tsx:328 `if (outcome.effect === 'none') return;` so matching the other player's colour gives no sound, haptic or text at all. The only cue is makeUnits (modes.ts:77-95) placing your three on the LEFT of your jail. Single-player does show swatches (DiceDemoScreen.tsx:1377-1379, 1385-1387) and draws colours randomly (:396-400). Severity is right for the Color War case; the wrong ready-card copy is cosmetic on its own.

- **Do this:** In src/demo/TwoPlayerScreen.tsx: (1) ready card copy at :440-442 — replace the literal with `{MODES[mode].rules}` (import MODES from '../game/modes'; it already imports makeUnits from there), keeping 'Tap fast. No mercy.' on the second line. (2) Pass the zone's own colour into Zone: add a prop `myColor?: ColorDef` (PRISONER_COLORS.find(c => c.id === zoneColors[zone]) when mode === 'colorwar', else undefined) and in the zoneHud (:184-189) render a swatch View before the text when myColor is set: `<View style={{ width: 16, height: 16, borderRadius: 8, backgroundColor: myColor.hex, borderWidth: 2, borderColor: THEME.ink }} />` followed by `Rescue {myColor.label}` in the score line. (3) Give a wrong-colour cue: in makeSettleHandler after `applySplitMatch`, if `mode === 'colorwar' && outcome.effect === 'none'` play `playCue('wrong')` and a Warning haptic before returning. Randomising the two colours each match instead of Red/Blue is a product choice — leave zoneColors fixed unless David asks; if he does, move zoneColors into startMatch as `[...PRISONER_COLORS].sort(() => Math.random() - 0.5).slice(0,2)` stored in a ref and read from there.

### 10. [major · small] hq-api: Every non-block action writes the OTHER party's row unconditionally from the MOVES table, so a player's quiet block is silently deleted when the blocked person cancels, removes, declines or accepts.

- **Evidence:** hq/src/app/api/friends/route.ts:193-201: `if (action !== 'block') { writes.push(supabase.from('friendships').upsert({ player_id: otherId, other_id: auth.playerId, state: move.theirs, since: now })) }` — the only read of THEIR row is inside `if (action === 'request')` (163-168) and is used solely to fake success. Walkthrough: B blocks A from friends (line 33: mine 'blocked', A's row untouched at 'friends'); A calls `remove` (allowed, `from: ['friends']` line 29) -> B's row upserted to `move.theirs` = 'none', block gone; A then `request`s, theirRow.state is 'none' so the fake-success guard at 169 does not fire and B gets a pending request. Worse path: B requested A then blocked (B='blocked', A='pending'); A `accept`s -> B's row becomes 'friends'. src/game/friends.ts:60-61 promises blocked is 'terminal' and the blocked player 'cannot send another request'. tests/friends.test.ts:307-320 compares only `from` lists and line 330 only asserts `/if \(action !== 'block'\)/`, so nothing catches this.

- **Do this:** In /home/user/Dice-battles-/hq/src/app/api/friends/route.ts POST: move the `theirRow` query (currently lines 163-168, inside `if (action === 'request')`) to just above line 162 so it runs for every action; keep the existing blocked-request fake success at 169-171; change line 193 from `if (action !== 'block') {` to `if (action !== 'block' && theirRow?.state !== 'blocked') {` with a comment that a row holding 'blocked' is never overwritten by the other side, so the block survives every move the blocked player can make. Then in /home/user/Dice-battles-/tests/friends.test.ts, in the describe block that reads the server file (~line 282), add a test 'the blocked side can never overwrite a block' asserting `/action !== 'block' && theirRow\?\.state !== 'blocked'/.test(server)`. Add a CHANGELOG.md entry and a `changes` row ('If someone blocked you, nothing you do can undo it any more — before, leaving or cancelling quietly wiped their block.'). Run `npm test` and `cd hq && npx tsc --noEmit`.

### 11. [major · small] hq-site: The two gameplay screenshots are from 18 Aug 2026, before the Paper & Ink redesign shipped (v1.32.0, 24 Aug), and show the old 'DICE BATTLES' title chrome; the six designed store screenshots in store/screenshots/ (uploaded to App Store Connect 30 Aug) are not used. The site will show an old look next to a new listing.

- **Evidence:** `git log --format='%h %ad %s' --date=short -- hq/public/images` → single commit 56d0d14 2026-08-18; hq/public/images/game-screenshot-1.jpeg viewed: raw phone capture with the iOS status bar (2:14, battery 100), letter-spaced 'DICE BATTLES' title, green-grass castle board and yellow 'BATTLE!' text — the pre-Paper & Ink look (STATE.md:84 'Paper & Ink (since v1.32.0, 24 Aug 2026)'). store/screenshots/iphone/01-06.png exist at 1290×2796 (STATE.md:105 'Screenshots: uploaded and live'). The app page references only game-screenshot-1/2.jpeg (hq/src/app/apps/dice-battles-color-rush/page.tsx:154,161).

- **Do this:** Replace the two images with web-sized copies of the designed store shots: from /home/user/Dice-battles-/store/screenshots/iphone/ pick 01.png and 02.png (or two that best show gameplay and split-screen), resize to 520×1127 (keeps the 260×565 slot at 2x; use sharp from hq/node_modules or `npx sharp-cli`) and save as hq/public/images/game-screenshot-1.jpeg and game-screenshot-2.jpeg (JPEG q≈80, well under 200 KB each). Keep the same filenames so hq/src/app/apps/dice-battles-color-rush/page.tsx:153-166 needs only new alt text (see the copy item). Verify with `node .claude/skills/site-check/check.mjs http://localhost:3000/apps/dice-battles-color-rush` or `--live` after deploying with `npx vercel deploy --prod` in hq/.

### 12. [major · medium] hq-site: The website describes a game that does not exist. Apps card: 'Roll color dice, march your army and take the other castle' (apps/page.tsx:310); app page description and meta: 'march your army across the board, and take the other castle' (dice-battles-color-rush/page.tsx:414,469); highlights 'Pass and play on a single device — the board flips so each side always faces its own castle' (435) and 'Roll, march, and clash' (439); screenshot alt 'two-player pass-and-play' (564). The actual game is: roll two dice, match both colours to free one of six prisoners, four modes, split screen with 'Nothing to pass back and forth' (store/description.txt; src/game/modes.ts rules 'Match a color to rescue that prisoner. First to all six wins!'). The DB rows carry the same wrong text, so it is live.

- **Evidence:** hq/src/app/apps/page.tsx:27 'Roll color dice, march your army and take the other castle — pick up and play, no reading required.'; hq/src/app/apps/dice-battles-color-rush/page.tsx:12 and :67 'march your army across the board, and take the other castle', :33 'Pass and play on a single device — the board flips so each side always faces its own castle', :37 'Roll, march, and clash', :162 alt 'two-player pass-and-play screenshot'. Live fetch today of /apps and /apps/dice-battles-color-rush shows the same 'march your army … take the other castle' and 'Pass and play … board flips' text. store/description.txt: 'Roll both dice. Land the SAME color on both and that prisoner runs free … TWO PLAYERS, ONE PHONE — Split screen, both halves live at once. Nothing to pass back and forth.'; src/game/modes.ts:33 'Match a color to rescue that prisoner. First to all six wins!'. `grep -rn 'march|pass and play|board flips|take the (other )?castle' src/` finds only a comment about a prisoner animation — no such mechanic.

- **Do this:** Rewrite the game copy from store/description.txt (the verified-accurate text), keeping the family framing. In hq/src/app/apps/page.tsx:27 change apps.card_description fallback to e.g. 'Roll two color dice — match both and a prisoner runs free. First to free all six wins. No reading required.' In hq/src/app/apps/dice-battles-color-rush/page.tsx: metadata.description (:12) and the dice_battles.description fallback (:67) → 'Two dice, six colors, six prisoners to set free. Roll both dice, match the colors, and race to empty your jail first. Simple enough to learn in a minute, at any age.'; DEFAULT_HIGHLIGHTS (:31-38) → 'Two players, one phone' body 'Split screen, both halves live at once — nothing to pass back and forth. No wifi, no second phone, nobody left out.' and 'A battle in a few minutes' body 'Roll, match, rescue. Four ways to play — Color Rush, Ultimate, Skirmish and Color War — and a game wraps up fast.'; alt text :155 and :162 → 'Dice Battles: Color Rush — rolling two dice to free a prisoner' / 'Dice Battles: Color Rush — split-screen two-player game'. Then update the LIVE rows, since site_content overrides the code: apps.card_description, dice_battles.description, dice_battles.highlights (use /admin/content/apps and /admin/content/dice-battles, or a Supabase update on public.site_content for those keys). Add a `changes` row ('website') per AGENTS.md and redeploy with `npx vercel deploy --prod` in hq/.

### 13. [major · small] integrations: When ads are turned on, the EU/UK consent form is shown to the child and the child's tap is taken as GDPR/TCF consent: requestInfoUpdate() is called with no tagForUnderAgeOfConsent option, and the under-age flag is only set on the request configuration AFTER the form has been shown. It also fires on first mount, stacking with the tutorial popup and the Game Center sign-in sheet.

- **Evidence:** src/game/ads.ts:229 `await mod.AdsConsent.requestInfoUpdate();` (no argument) → :230 `loadAndShowConsentFormIfRequired()`; the under-age flag appears only at :242 inside setRequestConfiguration, after the form. The local interface at :75 declares `requestInfoUpdate(): Promise<…>` with no options, so tsc cannot prompt for it, while node_modules/react-native-google-mobile-ads/src/AdsConsent.ts:30,46-50 accepts `options.tagForUnderAgeOfConsent`. Stacking: src/demo/DiceDemoScreen.tsx:219 `initAds()` runs in the same mount effect that sets `setPopup('howto')` on first launch (:205), and :238-250 `loadIdentity()` on mount triggers Game Center sign-in. Caveat: entirely inert today — adSdk.ts returns null so `moduleOrNull()` is null and initAds returns at :221; this bites only when ads are switched on.

- **Do this:** In /home/user/Dice-battles-/src/game/ads.ts: change the NativeAds interface at line 75 to `requestInfoUpdate(options?: { tagForUnderAgeOfConsent?: boolean }): Promise<{ canRequestAds: boolean }>;` and change line 229 to `await mod.AdsConsent.requestInfoUpdate({ tagForUnderAgeOfConsent: true });`. Add a test in /home/user/Dice-battles-/tests/ads.test.ts next to the COPPA-flag tests (around lines 94-131) that reads src/game/ads.ts and asserts `/requestInfoUpdate\(\{[^}]*tagForUnderAgeOfConsent:\s*true/` matches. Leave the mount-time call where it is; whether to defer initAds until the tutorial is dismissed is a separate product choice for David (the code is dead while ads are OFF).

### 14. [major · medium] integrations: An interstitial due at 'Play again' is presented over a countdown that keeps running: showAdIfDue() is fire-and-forget and the same function immediately enters 'matching', whose overlay advances on a JS interval into the 1100/1800 ms arm/go/battle timers and then the AI roll interval. JS timers do not pause under a native ad, so the player closes the ad to find the battle already started and the rival ahead.

- **Evidence:** src/demo/DiceDemoScreen.tsx:666-678 `startCountdown`: line 669 `showAdIfDue();` (not awaited), then :678 `setPhaseBoth('matching')` synchronously. src/demo/MatchmakingOverlay.tsx:35-45 `setInterval(() => { … if (isComplete(next) …) { … done.current(); } }, SCAN_TICK_MS / 2)` → beginCountdown (:687-703) arms 1100/1800 ms setTimeouts into 'battle'. src/game/ads.ts:332 `await interstitial.show()` resolves on presentation; the CLOSED listener (:274-278) only preloads and nothing exposes 'closed' to the screen. The app stays foreground/active while the SDK presents its view controller, so JS timers run. `quitToMenu` (:535-545) is unaffected because the menu is static. Caveat: inert while adSdk.ts returns null — `showAdIfDue` exits at :321 because `mod` is null.

- **Do this:** In /home/user/Dice-battles-/src/game/ads.ts make `showAdIfDue()` resolve when the ad CLOSES, not when it presents: inside the try at lines 331-333, before `await interstitial.show()`, build `const closed = new Promise<void>((resolve) => { const off = interstitial!.addAdEventListener(mod.AdEventType.CLOSED, () => { off(); resolve(); }); const offErr = interstitial!.addAdEventListener(mod.AdEventType.ERROR, () => { offErr(); resolve(); }); setTimeout(resolve, 60_000); });` then `await interstitial.show(); await closed; return true;`. In /home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx restructure `startCountdown` (lines 666-678) so everything after line 669 runs in `void showAdIfDue().then(() => { …existing body… })` (keep the same deps). Leave `quitToMenu` (:544) fire-and-forget. Add a test in tests/ads.test.ts using the existing fake SDK that `showAdIfDue()` does not resolve until the CLOSED listener fires.

### 15. [major · small] menus: Tapping a cup card charges the entry fee and starts the run instantly, and 'Give up this run' discards it — no confirmation and no refund either way. One mis-tap by a five-year-old costs 150 coins.

- **Evidence:** src/demo/TournamentScreen.tsx:111-118 `<Pressable key={t.id} disabled={!affordable} … onPress={() => { playClick(); onEnter(t); }}>` — the whole card is the button and `disabled` only guards affordability; :91-99 'Give up this run' calls `onAbandon()` directly; src/demo/DiceDemoScreen.tsx:1656 `onAbandon={() => setRun(null)}`; enterTournament :657 `if (!spendCoins(tournament.entry)) return;` spends immediately. The run card does say 'Lose and the run is over' but nothing warns before the tap that spends.

- **Do this:** In src/demo/TournamentScreen.tsx add local state `const [confirming, setConfirming] = useState<string | null>(null)` (cup id). For cups with `t.entry > 0`, the card onPress sets `confirming = t.id` instead of calling onEnter; when `confirming === t.id` render inside the card a second row: text `Enter for {t.entry} coins?` plus `PrimaryButton` 'Enter' (calls onEnter(t), clears confirming) and `SecondaryButton` 'Not now' (clears confirming). Free cup (entry 0) may stay one tap. For 'Give up this run' do the same two-step: first tap changes the quiet button into `Give up? Yes / Keep playing`; only 'Yes' calls onAbandon. Use the Card/PrimaryButton/SecondaryButton kit from src/ui/Card.tsx (already imported) rather than Alert so it stays Paper & Ink. Refund on give-up is a separate product decision — do not add it here.

### 16. [major · small] menus: The Friends page ignores the tab bar: it is rendered on `showFriends && me` alone, so tapping Store/Items/Battle moves the highlight but leaves Friends covering the screen; its back button reads '‹ Ranks' but drops you on whichever tab is now lit. Tapping 'Friends' before the identity has loaded does nothing, then the page pops up later, possibly over another tab.

- **Evidence:** src/demo/DiceDemoScreen.tsx:1624 `{showFriends && me && (` versus :1597 `{menuTab === 'store' && preview === null && (` and :1605 `{menuTab === 'leaderboard' && preview === null && (`. `grep -n setShowFriends` gives only :1614 (set true from Ranks) and :1639 (onClose) — nothing clears it on tab change; :1708 `<BottomNav active={tab} onSelect={setTab} />` passes setTab straight through. FriendsScreen page style (FriendsScreen.tsx:384) uses MENU_PAGE_AREA so the bar stays visible and tappable beneath. `me` is set asynchronously at :237-246, and the onFriends handler at :1612-1615 sets showFriends regardless of `me`. Back label :161 `‹ Ranks`.

- **Do this:** In src/demo/DiceDemoScreen.tsx: (1) change the render guard at :1624 to `{showFriends && me && menuTab === 'leaderboard' && preview === null && (`. (2) Change :1708 to `<BottomNav active={tab} onSelect={(next) => { setShowFriends(false); setTab(next); }} />`. (3) In the LeaderboardScreen `onFriends` handler (:1612-1615) do nothing (or keep the click sound) when `me === null`; better, pass `friendsReady={me !== null}` to LeaderboardScreen and render its Friends button disabled/sunk with 'Loading…' until ready. (4) Add a regex test in tests/screen.test.ts asserting the FriendsScreen render line contains `menuTab === 'leaderboard'`.

### 17. [major · small] menus: The Store ships a 'COIN PACKS / Not open yet' placeholder whose copy is aimed at the developer, not the player.

- **Evidence:** src/demo/StoreScreen.tsx:168-176 `<Text style={styles.sectionTitle}>COIN PACKS</Text> <View style={styles.comingSoon}> <Text style={styles.comingTitle}>Not open yet</Text> <Text style={styles.comingBody}>Buying coins with real money needs the App Store payment setup finished first. Until then every coin is earned by playing — …`. No test references it (`grep -rn 'Not open yet|comingSoon|COIN PACKS' tests/` → nothing). App Review Guideline 2.1 treats visible 'coming soon' placeholders as an incomplete-app signal, and the sentence talks about the developer's App Store setup, not the player.

- **Do this:** In src/demo/StoreScreen.tsx delete lines 168-176 (the COIN PACKS section title and the comingSoon View) and the now-unused `comingSoon`, `comingTitle`, `comingBody` entries in the StyleSheet. Run `npx tsc --noEmit` and `npm test` (no test references it). Re-add a real coin-pack shelf only when StoreKit ships. If David prefers to keep a hint, replace the body with one player-facing sentence: 'Coins are earned by playing. Coin packs are coming in a later update.'

### 18. [major · medium] menus: Any battle started from the home screen while a cup run is active is scored as a cup round — a casual Easy loss knocks you out of a paid Grand Championship, and the difficulty chips stay live mid-run so a Hard cup can be played on Easy.

- **Evidence:** src/demo/DiceDemoScreen.tsx:573 `if (runRef.current && outcome !== 'tie') {` … :576 `advanceRun(runRef.current, cup, outcome === 'won')` — no cup-round flag. The same `startCountdown` is called from the gesture layer (:970 `if (phaseRef.current === 'pick') startCountdown();`), the Start battle button (:1517 `onPress={startCountdown}`), Play again (:1256) and playCupRound (:684). difficultyRow (:1208-1233) has no disabled/run check and is rendered on the home screen (:1516) and all three result screens (:1575, :1586, :1906); enterTournament (:656-664) sets difficulty once and nothing re-asserts it. Note: 'paid' means 50/150 in-game coins, not money.

- **Do this:** In src/demo/DiceDemoScreen.tsx: (1) add `const cupRoundRef = useRef(false);` next to runRef. (2) At the top of `startCountdown` (line ~666) set `cupRoundRef.current = false;`. (3) In `playCupRound` (line ~682) after `setTab('play')`: read `const cup = run ? tournamentById(run.tournamentId) : undefined;`, set `difficultyRef.current = cup?.difficulty ?? difficultyRef.current; setDifficulty(difficultyRef.current);`, call `startCountdown()`, then set `cupRoundRef.current = true` (startCountdown is synchronous so this ordering works; do NOT pass a boolean parameter to startCountdown because `onPress={startCountdown}` would receive the press event as a truthy argument). (4) In `finishRound` change the guard to `if (runRef.current && cupRoundRef.current && outcome !== 'tie')`. (5) Also in finishRound, when `outcome === 'tie'` and cupRoundRef is set, leave cupRoundRef true so the replayed round still counts; otherwise `cupRoundRef.current = false` after advancing. (6) Hide or disable the difficulty chips while `run !== null && run.finished === null`: in difficultyRow wrap the Pressable `onPress` with `if (run && !run.finished) return;` and add a disabled style, or render a one-line 'Set by the cup' note instead of the chips on the home screen. Add a headless test in tests/tournament.test.ts is not possible for this UI path; add a regex assertion in tests/screen.test.ts that `finishRound` references `cupRoundRef`.

### 19. [major · medium] menus: 'Remove friend' and 'Block' are single taps with no confirmation, there is no unblock anywhere in the UI even though the state machine says only unblock gets you out, and a failed action from the profile page shows no error.

- **Evidence:** src/demo/FriendsScreen.tsx:143-144 `onRemove={() => act(showing.playerId, 'remove')}` / `onBlock={() => act(showing.playerId, 'block')}` wired straight to :358-363 `<SecondaryButton style={styles.wideButton} onPress={onRemove}>` / `onPress={onBlock}`; :128-138 `act` does `if (!result.ok) { setProblem(result.error); return; }` while `ProfileView({ profile, onBack, onRemove, onBlock })` at :305-315 has no `problem` prop, and the only `{problem}` render is :266-272 inside YOUR FRIENDS on the list page. `grep -rn unblock src/demo` → nothing; `grep -rn blocked src/demo/FriendsScreen.tsx` → nothing. Yet unblock is already real everywhere else: src/game/friends.ts:104 `blocked: { unblock: 'none' }`, hq/src/app/api/friends/route.ts:34 `unblock: { from: ['blocked'], mine: 'none', theirs: 'none' }`, src/game/friendsApi.ts:32 `blocked: ProfilePeek[]` filled at :114 `blocked: result.data.blocked ?? []`, tests/friends.test.ts:117-133 'a blocked player can do nothing but be unblocked'. The 'terminal' note at friends.ts:60-63 describes what the BLOCKED player sees, not the blocker.

- **Do this:** Reviewer b is right: the server, the API type, the state machine and its tests all already carry unblock — only the screen is missing it, and the design notes only say the blocked player must not be able to tell; they say nothing about the blocker being unable to undo. In src/demo/FriendsScreen.tsx: (1) pass `problem` into ProfileView as a prop and render `<Text style={styles.note}>{problem}</Text>` under `styles.profileActions` (clear it in `setShowing(null)`/onBack so it does not leak back to the list); (2) add a 'BLOCKED' section on the list page after YOUR FRIENDS, rendered when `list.blocked.length > 0`, one `<Card style={styles.rowCard}>` per entry with `rowName` and a `SecondaryButton style={styles.smallButton}` 'Unblock' calling `act(b.playerId, 'unblock')` — no server or friendsApi change needed; (3) make Remove/Block two-step: a `confirm: 'remove' | 'block' | null` state in ProfileView so the first tap swaps the two buttons for a one-line question ('Remove {name} as a friend?' / 'Block {name}? They will not be told and cannot ask again.') with 'Yes' / 'Cancel' SecondaryButtons; there is no existing confirm pattern in src/demo (grep 'confirm|Are you sure' finds none), so this is the first, keep it in-page not an Alert. Add a source regex test in tests/friends.test.ts or tests/screen.test.ts asserting FriendsScreen.tsx contains 'Unblock' and 'blocked'. Add a CHANGELOG entry and a `changes` row ('You can now unblock someone, and the game asks before removing or blocking a friend.'). Ship OTA — all JS.

### 20. [major · small] store-listing: The device secret - the only credential protecting a child's profile and friend list - is sent as a URL query parameter on every friends fetch, so it lands in Vercel request logs, proxies and any URL-logging middleware in plain text, defeating the hash-at-rest design.

- **Evidence:** Verified at both ends and found no guard. /home/user/Dice-battles-/src/game/friendsApi.ts:106 builds the request as `` `/friends?playerId=${encodeURIComponent(me.playerId)}&secret=${encodeURIComponent(me.secret)}` `` and /home/user/Dice-battles-/hq/src/app/api/friends/route.ts:59 reads it with `authenticate(params.get('playerId'), params.get('secret'))`. The write paths do it correctly — friendsApi.ts:81 and :135 put the secret in a JSON body — so this is the one leaking call, not a consistent design. It contradicts the code's own written invariant: src/game/playerIdentity.ts:49 says of the secret 'NEVER shown on screen, never in a bug report, never logged.' The test the map points at is genuinely narrow: tests/friends.test.ts:355 'the device secret is never stored in the clear' only regex-checks `secret_hash: hash(` in hq/src/app/api/players/route.ts, i.e. the database column — nothing asserts anything about transport. The secret is 128 bits (playerIdentity.ts:56-60) and never rotates, and it is the sole credential: holding it lets you read a child's friend list and requests and act on their behalf (friendsApi.ts:126-140). Bounded, but real — HTTPS protects the wire, so the exposure is Vercel's request logs and any intermediary that records URLs.

- **Do this:** Move the secret into a request header. In hq/src/app/api/friends/route.ts:57-59, read it from `request.headers.get('x-player-secret')` with a temporary fallback to `params.get('secret')`, deploy the site first; then change src/game/friendsApi.ts:104-106 to fetch `/friends?playerId=...` with `headers: { 'x-player-secret': me.secret }` (the `call` helper at :46 already spreads `init.headers`) and ship the OTA update; then delete the query-string fallback in a follow-up deploy. Add a test next to tests/friends.test.ts:355 asserting that no template literal in src/game/friendsApi.ts interpolates `me.secret` into a URL, so this cannot come back. Two-step sequencing matters because old JS bundles keep using the query string until they update.

### 21. [major · small] tests-tooling: The core game-loop result path has no behavioural test. finishRound (award coins, advance tournament, apply trophies, queue unlock popups, sync Game Center) is checked only by text windows. applyMatchResult — the function that decides trophies, wins and unlocks after every battle — is called in exactly one test, and that test only asserts a cheat code leaves wins alone. Nothing asserts a win lands inside TROPHY_STAKES[d].win, a loss floors at 0, newUnlocks equals the tiers crossed, or modeWins[mode] increments only on a win.

- **Evidence:** The applyMatchResult half is exactly right and I reproduced it: `grep -rn applyMatchResult tests/` returns two hits only — the import at tests/persistence.test.ts:12 and one call at :143, inside 'wins are left alone — a cheat must not rewrite the record', whose sole assertion is `assertEqual(getProgress().wins.easy, winsBefore)` after setTrophies(900). Nothing asserts a band, the floor, newUnlocks or the increments. The function is at src/game/progress.ts:388 with an injectable `rng` as its fourth parameter (:392), the floor is `const after = Math.max(0, before + delta);` (:401), and the returned `delta` is `after - before` — so on a floored loss the reported delta deliberately differs from the roll, which is precisely the kind of thing only a test pins.

Where the headline overreaches, and why this is 'partly' rather than 'confirmed': the same sentence lists five things finishRound does, and two of them ARE behaviourally tested with injected RNG. awardCoins band ends are pinned at tests/currency.test.ts:66-76 (`() => 0` → band.min, `() => 0.999999999` → band.max, plus a 400-iteration in-band check); advanceRun win/loss/knocked-out/champion is covered at tests/tournament.test.ts:58-80. So 'the core game-loop result path has no behavioural test' is too broad — what is uncovered is applyMatchResult itself and the finishRound orchestration around it (src/demo/DiceDemoScreen.tsx:562, applyMatchResult called at :620, syncGameCenter at :643), which is verified only by tests/ads.test.ts taking a text window between `const finishRound` and `const startCountdown`. Both reviewers agree on severity, fixClass and effort; only the verdict was in dispute, and the precise version is the skeptic's.

- **Do this:** Add an applyMatchResult suite to tests/persistence.test.ts — the harness it needs is already imported there (tests/storageMock.ts, resetProgressForTests, setTrophies), and the injectable rng at src/game/progress.ts:392 makes it fully deterministic with no source change, exactly as tests/currency.test.ts:66-76 does for awardCoins. Assert, per difficulty: a win's delta lands within TROPHY_STAKES[d].win (feed `() => 0` and `() => 0.999999999` for the band ends); a loss lands within -loss.max..-loss.min; a loss from below the stake floors at 0 AND the returned `delta` equals after-before rather than the raw roll; `newUnlocks` is exactly the TIERS with `at > before && at <= after` and is empty on a loss; `wins[difficulty]` and `modeWins[mode]` increment on a win only, with the mode not played untouched. Finish with a round-trip through the storage mock so loadProgress() reads back the same numbers. When restating the finding, use 'applyMatchResult and the finishRound orchestration' — do not repeat 'the core game-loop result path has no behavioural test', because awardCoins and advanceRun are already covered and saying otherwise will get the whole item dismissed. Do not attempt to test finishRound in place: it lives inside a React component and there is no renderer in this toolchain (see item 1); if the cup branch is worth covering, extract the advanceRun/champion-prize block into a pure helper first.

### 22. [major · small] tests-tooling: No CI and no hooks: nothing runs the suite, the typecheck or the bundle check except a person remembering to. `npm run check` is typecheck + test and omits the Metro bundle check that AGENTS.md lists as mandatory (and README says catches device-only import breaks). The pre-submission 'full test on Fable' gate is prose only.

- **Evidence:** Verified literally: package.json has `test`, `typecheck`, `bundle` (expo export --platform ios) and `check: npm run typecheck && npm test` — `bundle` exists and `check` never calls it. No .github directory in the repo root; `ls .git/hooks | grep -v sample` prints nothing. I then ran the whole mandated gate myself rather than reasoning about it: `npm test` → 549 passed, 0 failed, 30.5s (33.9s wall); `npm run bundle` → clean, exported a 5.44 MB hbc, 13.7s wall (the impact reviewer measured 27.9s cold, so budget ~30s). `git status --porcelain` was empty before and after both, so wiring bundle into check costs nothing but time: ~34s → ~48-62s. Two useful by-products of the export: `grep -c RNGoogleMobileAds` on the hbc returns 0 and `strings | grep -c react-native-google-mobile-ads` returns 0, so ads-OFF is real in the shipped bundle and not merely claimed; ExpoGameCenter appears 4 times, which is what item 4 turns on.

On the severity dispute I am between the two and closer to the skeptic. 'Blocker' overstates it: nothing here blocks submitting 1.0, because AGENTS.md's pre-submission gate mandates the export in prose and a session following that gate runs it whatever `check` contains. The real exposure is the routine path — AGENTS.md ships every change over the air 'as soon as it is verified — typecheck, tests, bundle check', and the command literally named `check` omits the only step that exercises Metro, which is the step whose absence crashed David's phone on 25 Aug 2026.

- **Do this:** Siding with the impact reviewer on fixClass: the valuable half is one line and needs nobody's permission. Set `"check": "npm run typecheck && npm test && npm run bundle"` in package.json. Then fix README.md:48-51, which claims `npm test # ~3s` (measured 30.5s, a 10x understatement that makes the suite sound skippable) and lists `npm run bundle` as an optional extra line — rewrite the block so `npm run check` is described as the full gate at roughly a minute. Do NOT add .github/workflows as part of this: the skeptic is right that a CI workflow on a public repo is David's call about the repo rather than a code fix, and it is also the half that does not need doing — the one-line change removes the way the mandatory step gets skipped, and the pre-submission gate is a human process that CI would not enforce anyway. Ask David about Actions separately if it comes up; do not let that question hold up the script fix.

### 23. [major · medium] tests-tooling: The friends feature shipped yesterday (v1.64.0) with its network client untested and its screen untouched by any test. friendsApi.call() has timeout/abort, non-JSON and non-OK branches that nothing exercises; FriendsScreen.tsx (460 lines) is neither imported nor text-checked. The server routes that friends.test.ts itself calls 'the copy that actually decides' have zero tests, and hq/ has no test runner at all — the game suite polices the API by regex.

- **Evidence:** Every part checks out. `grep -rn friendsApi tests/` → zero hits; FriendsScreen appears in neither the import set nor the text-read set in my own coverage script (it lands in the 18 files touched by nothing at all). src/game/friendsApi.ts is 141 lines with four exported network calls (pushProfile :73, findByCode :91, fetchFriends :102, actOnFriend :126) all funnelling through `call()` at :36-63, whose catch collapses offline/timeout/non-JSON into `{ok:false, error:'No connection'}` — three distinct branches, none exercised. hq/package.json scripts are exactly dev/build/start/typecheck; `find hq -name '*.test.*'` outside node_modules returns nothing. tests/friends.test.ts:276 does literally say 'the copy that actually decides' about the server route, then greps it — the readFileSync of hq/src/app/api/friends/route.ts is at :282-285 and players/route.ts at :356-359, both 209 and 168 lines with zero tests of their own.

One factual nit that does not change anything: 'shipped yesterday' is wrong — CHANGELOG.md:3 dates v1.64.0 as 2026-09-04 and today is 2026-09-06.

- **Do this:** Add tests/friendsApi.test.ts copying the fetch-swap pattern that already works in tests/news.test.ts:24-32 (`const real = g.fetch; g.fetch = fake; try {...} finally { g.fetch = real }`). Cover: non-OK with `{error:'...'}` surfaces that string; non-OK without one gives 'that did not work'; a fetch that throws, one that rejects on abort, and one whose .json() throws all return `{ok:false, error:'No connection'}` and never reject; findByCode on `{found:false}` returns null; fetchFriends on `{}` returns EMPTY_LIST-shaped arrays rather than undefined. Whether hq/ gets its own runner is a separate question for David (it is an open question in the map) — do not add one unasked.

### 24. [major · large] tests-tooling: Ranked list of the 10 most valuable missing tests: (1) applyMatchResult behaviour — win delta within TROPHY_STAKES[d].win, loss within .loss and floored at 0, newUnlocks = tiers crossed, wins/modeWins increment on win only, persisted JSON round-trips. (2) loadProgress migration — pre-modeWins save loads with zeros, junk JSON keeps defaults, trophies preserved. (3) friendsApi.call — non-OK body.error surfaces, non-JSON/timeout/offline all return 'No connection' without rejecting, findByCode found:false → null, fetchFriends fills missing arrays (fetch-mock pattern already exists in news.test.ts). (4) hq API handler tests with a fake Supabase client for friends/players routes — blocked request answers ok:true, secret stored hashed, stranger peek has only name+trophies — replacing the regexes in friends.test.ts:283-376. (5) crashGuard — installCrashGuard wraps ErrorUtils, reportFatal notifies listeners and survives a throwing one, describe() truncates. (6) Submission-gate facts — app.json version is a CHANGELOG heading and ≤ GAME_VERSION, ios.buildNumber present, store/subtitle ≤30 / keywords ≤100 / promo ≤170 / description ≤4000 chars, every store/screenshots PNG exactly 1290×2796 or 2048×2732 via the existing readPng. (7) Extract finishRound's tournament branch and test it — tie neither advances nor ends the run, champion prize granted exactly once, loss ends the run. (8) usingTestAds() as behaviour — tester mode true → test unit, false → real unit, progress not loaded → real unit — instead of ads.test.ts:316-345's regexes. (9) Seeded RNG injection for generateObstacleLayout so the 240-roll suite is reproducible and the 4000-sample proof can be a scheduled job rather than a manual env var. (10) A general native-module gate — every package with native code in package.json that live src references must coincide with an explicit runtimeVersion string — generalising ads.test.ts:157-190 to expo-game-center and the coming StoreKit module.

- **Evidence:** Nine of ten check out; the list contains one instruction that would break a healthy repo, which is what makes this partly rather than confirmed. Item (6)'s `ios.buildNumber present` is wrong — eas.json is `"appVersionSource": "remote"` with autoIncrement true on both build profiles, and app.json has no buildNumber by design; a test demanding one would fail today and push someone to fight remote versioning. Both reviewers caught this independently, so it is settled. Spot-checks of the rest: (8) tests/ads.test.ts:316-345 is four regexes plus an indexOf-slice regex for the catch fallback, exactly as described, against a pure function that could just be called; (9) src/game/obstacles.ts:55 is `const rand = (min,max) => min + Math.random()*(max-min)` with no injectable rng; (3) tests/news.test.ts:24 already has a working `withFetch` helper to lift rather than a mere "pattern"; (10) is real and is the sharpest item on the list — `grep -rn expo-game-center tests/` is empty, expo-game-center ^1.0.1 is a live dependency at package.json:14, and app.json:44 has runtimeVersion as an OBJECT (the sdkVersion policy), which is the precise configuration AGENTS.md says crashed build 6. On severity I side with the skeptic. Impact's "regression net around code I found correct" is fair for (1),(2),(5),(7) and I reached the same conclusion reading progress.ts and crashGuard.ts myself — but (10) is not a net, it is the missing guard on the failure mode this project has already been bitten by once, and calling the aggregate minor undersells that.

- **Do this:** Adopt the list as the plan with two corrections: strike 'ios.buildNumber present' from item (6) entirely (eas.json's remote appVersionSource owns it), and note in (3) that tests/news.test.ts:24 already contains the reusable withFetch helper. Sequence: (10) first and independently of the rest — generalise the ads.test.ts:157-190 assertion so that ANY package with native code that live src imports forces an explicit `"runtimeVersion": "x.y.z"` string, covering expo-game-center now and StoreKit/ads later; it must land in the same commit as any native addition, per AGENTS.md. Then the corrected (6), then (1),(2),(5) as one small persistence-and-safety batch, then (3) using withFetch. (4) needs David's answer on whether hq/ gets its own test runner at all before anyone writes handler tests. Leave (7),(8),(9) until after launch — (9) in particular changes the signature of generateObstacleLayout in shipping physics to buy back a documented ~1-in-4000 flake, which is the wrong trade in submission week. Every new suite file must be added to tests/run.ts:10-39; that import list is hand-maintained and an unregistered file silently never runs.

### 25. [minor · small] config-release: The game asks for microphone access on both platforms although it never records: expo-audio's plugin defaults inject NSMicrophoneUsageDescription ("Allow $(PRODUCT_NAME) to access your microphone") and android.permission.RECORD_AUDIO. For a 4+/family title this is a reviewer question on iOS and a Families-policy/Data-safety flag on Play.

- **Evidence:** node_modules/expo-audio/plugin/build/withAudio.js:5-9 `MICROPHONE_USAGE = 'Allow $(PRODUCT_NAME) to access your microphone'` applied via createPermissionsPlugin with `microphonePermission` undefined (only `=== false` deletes the key — @expo/config-plugins/build/ios/Permissions.js:28-30); withAudio.js:6,23 `recordAudioAndroid = true` → RECORD_AUDIO. /home/user/Dice-battles-/app.json:58 `"expo-audio"` with no options. Introspection: NSMicrophoneUsageDescription present; android.permissions = [RECORD_AUDIO, MODIFY_AUDIO_SETTINGS, READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE, INTERNET]. `grep -rn useAudioRecorder|allowsRecording|AudioRecorder src` → no matches; the game only plays. The fact is right, the severity is not: an Info.plist purpose string with no code path that requests the permission never prompts, is not shown to App Review, and does not touch the App Privacy label; it is a genuine flag only for Play Families/Data safety, and Play is out of scope (no account).

- **Do this:** In /home/user/Dice-battles-/app.json replace the bare string `"expo-audio"` at line 58 with `["expo-audio", {"microphonePermission": false, "recordAudioAndroid": false}]`. Per withAudio.js this deletes NSMicrophoneUsageDescription and drops RECORD_AUDIO while keeping MODIFY_AUDIO_SETTINGS. Native change — takes effect in the next build; verify with `npx expo config --type introspect` that NSMicrophoneUsageDescription is gone and android.permissions no longer lists RECORD_AUDIO.

### 26. [minor · small] config-release: Old-binary safety for Game Center rests on a third-party package's internal try/catch, not on runtimeVersion or any test here. expo-game-center is in every OTA bundle via a literal require, and only survives on binaries without the native module because ExpoGameCenterModule.js swallows requireNativeModule's throw itself. A package upgrade that removes that guard reproduces the 25 Aug crash, and tests/ads.test.ts only watches the ads SDK string.

- **Evidence:** Facts hold: /home/user/Dice-battles-/src/game/gameCenter.ts:71 `const mod = require('expo-game-center');` (literal, so bundled); node_modules/expo-game-center/build/ExpoGameCenterModule.js:5-24 wraps `requireNativeModule('ExpoGameCenter')` in try/catch and exports null; `grep -rln expo-game-center tests/` → nothing; app.json:43-45 is the sdkVersion policy. The framing is wrong on two points. (1) This is NOT the pattern AGENTS.md condemns: Metro's guardedLoadModule escalates only when a module FACTORY throws; here the throw is caught inside the throwing module's own factory, so no factory throws and the outer require returns normally — structurally different from the ads SDK, whose factory called TurboModuleRegistry.getEnforcing uncaught. (2) It is empirically proven, not theoretical: Game Center arrived in v1.23.0 (CHANGELOG.md:1954-1957, 23 Aug) AFTER build 5 (1.0.0, CHANGELOG.md:2699), and every OTA since through v1.64.0 has run on build 5 without the native module. Also noted: gameCenter.ts:73 `typeof candidate.submitScore === 'function'` is always true because index.js:17-37 wraps every method, so on build 5 `native` is the wrapper and each call throws TypeError on null — all of which are inside try/catch (signIn gameCenter.ts:96-104, post :137-142), so no-op as intended. Residual risk is only a future package change; package-lock pins 1.0.1 (`^1.0.1` in package.json:14).

- **Do this:** Two small guards: (1) in /home/user/Dice-battles-/package.json:14 pin `"expo-game-center": "1.0.1"` (exact, no caret) so an `npm update` cannot change the guarded module silently; (2) add a test (new tests/gameCenter.test.ts registered in tests/run.ts, or a suite in tests/ads.test.ts) that reads node_modules/expo-game-center/build/ExpoGameCenterModule.js and asserts the text contains `try {` before `requireNativeModule('ExpoGameCenter')` and a `catch` after it, with a message pointing at AGENTS.md's 25 Aug note — so a package bump that removes the internal guard fails `npm test` instead of crashing old binaries. Not a launch item: the next binary compiles the native module in.

### 27. [minor · small] gameplay: The bottom HUD announces '<COLOR> RESCUED!' for every matched pair, including matches that rescued nothing: in Ultimate it says 'RED RESCUED!' while the callout says 'Oh no! Red was captured again!'; in Skirmish when the opponent already took it; in Color War when it is the opponent's colour or a neutral one.

- **Evidence:** src/demo/DiceDemoScreen.tsx:1446-1448 `{isMatch ? `${rolledFaces[0].label.toUpperCase()} RESCUED!` : …}` with isMatch at :1159-1162 = `rolledFaces[0].id === rolledFaces[1].id`, styled green via styles.matchText (:2168-2171 color THEME.good). handleSettled: Ultimate send-back at :846-857 shows 'Oh no! … was captured again!'; Skirmish already-taken at :887-890 returns after a haptic with no callout; Color War wrong colour at :913-921 shows 'hands off' / 'isn't your color'. So the fact is right. Severity is overstated: the board itself is correct (the figure visibly moves back / stays), the announcer banner is correct, and the HUD line is text — the pre-reader the map worries about cannot read 'RESCUED' either; what they see is two matching swatches, which IS accurate. It is a wrong label for a few seconds, not misleading game state.

- **Do this:** In src/demo/DiceDemoScreen.tsx: add `const [matchEffect, setMatchEffect] = useState<'rescued' | 'returned' | 'taken' | 'wrong' | null>(null);` near rolledFaces state. In handleThrow (:813-816) add `setMatchEffect(null);`. In handleSettled set it at each branch: after the Ultimate send-back moveUnit (:850) `setMatchEffect('returned')`; in the Skirmish `if (!unit)` branch (:887) and the Classic `if (!jailUnit)` non-ultimate branch (:859) `setMatchEffect('taken')`; in the Color War wrong-colour branch (:913) `setMatchEffect('wrong')`; before each `celebrate()` call `setMatchEffect('rescued')`. Change the HUD text at :1446-1448 to: rescued → `${label} RESCUED!` (keep matchText style); returned → `${label} CAPTURED AGAIN!`; taken → `${label} — already gone`; wrong → `${label} — not yours`; and apply styles.matchText only when matchEffect === 'rescued'.

### 28. [minor · small] hq-api: The player_profiles and friendships tables that the players and friends routes depend on have no DDL anywhere in the repository, so their columns, primary key, unique constraints and RLS status cannot be reviewed or reproduced from a fresh setup.

- **Evidence:** Repo half is true: `grep -rIl 'player_profiles\|friendships'` matches only hq/src/app/api/players/route.ts, hq/src/app/api/friends/route.ts and LAUNCH_REVIEW.md; hq/supabase/schema.sql has no such tables. Security half is refuted by the live project: Supabase migration `20260903234809 friends_and_profiles` exists (list_migrations), both tables have `rls_enabled: true` (list_tables) with zero policies (security advisor: 'Table public.player_profiles has RLS enabled, but no policies exist'), so the anon key can read nothing; pg_constraint shows `player_profiles_friend_code_key UNIQUE (friend_code)` (so the `error.message.includes('friend_code')` check at players/route.ts:152 works) and `friendships_pkey PRIMARY KEY (player_id, other_id)` (so the upserts at friends/route.ts:184,195 work), plus FKs from both friendships columns to player_profiles. Only 1 profile row and 0 friendship rows exist today.

- **Do this:** Append the live migration text verbatim to the end of /home/user/Dice-battles-/hq/supabase/schema.sql under a heading `-- Friends and profiles (migration 20260903234809)`. The exact SQL, copied from supabase_migrations.schema_migrations, is saved at /tmp/claude-0/-home-user-Dice-battles-/e5070910-a4c9-5052-b188-05edc16ca70e/scratchpad/launch-review/friends_and_profiles.sql (two `create table if not exists`, two indexes, two `enable row level security`; all statements are idempotent so re-running schema.sql on the live DB is safe). Update the map's assumption in hq/README.md:51 only if it stops being true — it does not.

### 29. [minor · small] hq-api: The device secret is sent in the URL query string on every friends-list fetch, where it lands in Vercel request logs, proxies and any URL-logging middleware.

- **Evidence:** CODE FACT — confirmed exactly, and it is the whole substance of the finding. src/game/friendsApi.ts:106 builds `/friends?playerId=${encodeURIComponent(me.playerId)}&secret=${encodeURIComponent(me.secret)}`; hq/src/app/api/friends/route.ts:58-59 reads both from `request.nextUrl.searchParams`. This is the ONLY place a credential goes into a URL in the whole repo — `grep -rn 'secret='` over all .ts/.tsx returns just that line plus the route's own doc comment. Every other path uses the JSON body (friends/route.ts:127, players/route.ts:126), and findByCode sends only `?code=`. One caller (src/demo/FriendsScreen.tsx:86), no test asserts the URL shape.

SETTLING THE SPLIT ON THE CONSEQUENCE. The skeptic is right that this project's own logs cannot be checked — I ran `get_runtime_logs` against prj_1i6PYVBjBnspKmWTLBsISG56LwRd / team_qr5cGQ9U7oDjgnEKBs2PbmRJ myself and got the identical 403, which matches STATE.md:35-37 on the expired Vercel token. But that path is closed to every reviewer, and an unreadable log is not evidence of absence; Vercel's documented runtime-log schema carries `requestPath` on every request row. Two sub-clauses of the stated consequence ARE wrong and the skeptic deserves credit for pushing: TLS terminates at Vercel's edge, so intermediate 'proxies' never see the URL, and there is no URL-logging middleware here — hq/src/middleware.ts:47-49 matches only /admin, /password, /login and /auth, never /api.

Why confirmed and not partly: the defect is provable in-repo without touching Vercel at all. src/game/playerIdentity.ts:49 promises this value is 'NEVER shown on screen, never in a bug report, never logged'. Line 106 makes that sentence false. A written guarantee contradicted by the code two files away is a confirmed defect regardless of what any log retention policy turns out to be — and in a project whose entire privacy posture rests on such written claims being literally true, that is the part that matters.

BOTH REVIEWERS AGREED ON THE SIZE, and they are right: minor. The credential guards trophy counts, dice-set counts and friend actions (players/route.ts:20-21, 'Nothing here is worth stealing'). Native app, so no browser history, no Referer, no shared-link vector, no analytics SDK. No player hits this and no App Store reviewer sees it.

- **Do this:** Move the secret to a header, and keep the old path working — both reviewers independently spotted the compatibility trap, which is the only non-obvious part. (1) hq/src/app/api/friends/route.ts:57-59: pass `request.headers.get('x-player-secret') ?? params.get('secret')` to authenticate, with a comment saying the query-string fallback exists for builds shipped before this change and naming the version it can be deleted after. Leave playerId in the query string — it is not a credential. (2) src/game/friendsApi.ts:106: drop `&secret=…`, and pass `{ 'x-player-secret': me.secret }` as the `headers` on the init object — `call()` already spreads `init?.headers` at line 48, so a GET needs no other change. (3) Update the route's own doc comment at hq/src/app/api/friends/route.ts:56, which currently documents the leaky form. ORDER MATTERS: deploy hq first (`npx vercel deploy --prod` from hq/) so the server accepts both, then ship the game side over the air — TestFlight builds 5 and 6 run the old JS until they pull the update, and reversing the order 401s every un-updated phone. Do NOT remove the fallback in the same change; delete it a release later. Verify with `npx tsc --noEmit`, `npm test` (tests/friends.test.ts reads the route source directly, so check its regexes still pass) and the Metro bundle check. Ship it alongside another friends change rather than on its own. playerIdentity.ts:49 needs no edit — it becomes true again.

### 30. [minor · small] hq-api: No rate limiting on any players/friends route: unauthenticated friend-code lookups hit the database per call, profile creation is unbounded, and outgoing friend requests are unbounded because the friend cap counts only state='friends'.

- **Evidence:** True parts: players/route.ts:90-106 answers any well-formed code with a DB query and no throttle; players/route.ts:134-158 creates a profile for any never-seen playerId, admitted at 15-21; friends/route.ts:172-176 counts `.eq('state', 'friends')` so pending requests are uncounted; the only brake anywhere is bug-report/route.ts:46-51. Refuted part: 'rows can be created for player ids that do not exist' — the live table has `friendships_other_id_fkey FOREIGN KEY (other_id) REFERENCES player_profiles(player_id)` (pg_constraint on project eqdbvpnckriscvzalwix), so the upsert at friends/route.ts:195 fails with a 23503 and the request gets a 500 at 206; no orphan rows. Severity is overstated for launch: the live DB holds 1 profile and 0 friendships, an attacker needs a modified client to do anything beyond code lookups, and the blast radius is free-tier quota, not player data.

- **Do this:** Two cheap, unambiguous changes in /home/user/Dice-battles-/hq/src/app/api/friends/route.ts: (1) line 176 change `.eq('state', 'friends')` to `.in('state', ['friends', 'requested'])` so open requests count toward MAX_FRIENDS; (2) after line 130 validate `otherId` with `/^[\w:.-]{1,80}$/` (Game Center ids look like `A:_...` / `G:...`, local ids `local-` + 20 base36) and return 400 'not a player id' otherwise, and in the same file map a write error with `code === '23503'` to `{ error: 'no such player' }` 404 instead of the raw message. Leave real per-IP rate limiting as a separate decision for David (Supabase-backed counter vs Vercel firewall rules) — do not build it for launch.

### 31. [minor · small] hq-api: The server accepts any 24-character display name and arbitrary JSON objects for wins/modeWins from any caller, so a modified client can put unmoderated text in front of children and store unbounded blobs that are then echoed to every friend.

- **Evidence:** hq/src/app/api/players/route.ts:62-66 cleanName only collapses whitespace and slices to 24; lines 47-48 `wins: typeof body.wins === 'object' && body.wins ? body.wins : {}` / `mode_wins: ... body.modeWins ... : {}` pass any object through; the live columns are `wins jsonb`, `mode_wins jsonb` with no size check (information_schema on project eqdbvpnckriscvzalwix), and friends/route.ts:90-91 send both to every friend. Genuine client cannot do this: src/game/playerIdentity.ts:134 `const name = apple?.name || storedName || ANONYMOUS_NAME` and there is no name-entry UI (grep for NAME_KEY setters finds only the cache write at 146), so text needs a modified client, and its audience is limited to players who chose to add that attacker by code. Real, but the map's 'major' assumes an attacker the game does not yet have.

- **Do this:** In /home/user/Dice-battles-/hq/src/app/api/players/route.ts cleanProfile (lines 40-55): replace lines 47-48 with a `counts(v, keys)` helper that returns `Object.fromEntries(keys.map(k => [k, int((v as Record<string, unknown>)?.[k], 1_000_000)]))` and call it with `['easy','medium','hard']` for wins (AiDifficultyId, src/game/ai.ts:17) and `['classic','ultimate','skirmish','colorwar']` for mode_wins (ModeId, src/game/modes.ts:8); in cleanName (62-66) additionally strip control, zero-width and line characters with `.replace(/[\p{Cc}\p{Cf}  ]/gu, '')` before trimming. Add a test in tests/friends.test.ts asserting the players route source contains those two key lists. `cd hq && npx tsc --noEmit`, `npm test`, deploy hq. Free-text name filtering beyond that is a separate decision for David.

### 32. [minor · small] hq-site: Page titles and meta descriptions are static and NOT editable in the admin, although the content index promises 'Everything written on the public site'. So the privacy meta still says the studio 'doesn't … show ads' and the app page meta still says 'march your army … simple enough that a five-year-old can jump right in' (kid-first wording) even after David edits the visible copy — and these are what Google and link previews show.

- **Evidence:** hq/src/app/apps/dice-battles-color-rush/page.tsx:9-13 `export const metadata: Metadata = {… description: 'Roll color dice, march your army across the board, and take the other castle. Fast, colorful, and simple enough that a five-year-old can jump right in.'}` — a static export, not generateMetadata; hq/src/app/privacy/page.tsx:6-10 static description "…doesn't collect personal information, show ads…"; hq/src/app/apps/page.tsx:9-12 and page.tsx:9-13 likewise. hq/src/app/admin/content/page.tsx:16-18 'Everything written on the public site.' Live `<meta name="description">` on /apps/dice-battles-color-rush today is the 'five-year-old' string while the visible live description already ends '…simple enough to learn in a minute — then surprisingly hard to put down.' — proof the two have already diverged.

- **Do this:** In hq/src/app/apps/dice-battles-color-rush/page.tsx replace the static `metadata` export with `export async function generateMetadata(): Promise<Metadata>` that calls getSiteContent() and uses text(content, 'dice_battles.description', <corrected fallback>) as the description (title unchanged). In hq/src/app/privacy/page.tsx and hq/src/app/terms/page.tsx do the same using the first section body from sectionList(content, 'privacy.sections'|'terms.sections', DEFAULT_SECTIONS) truncated to ~155 chars, or make the fallback a neutral static string ('How Paper Ship Studio apps handle your information.') so it cannot be false. In hq/src/app/page.tsx and hq/src/app/apps/page.tsx derive description from home.hero_subhead / apps.intro. Change the admin index wording at hq/src/app/admin/content/page.tsx:17 only if some text stays static. Validate with `npx tsc --noEmit` in hq/.

### 33. [minor · small] hq-site: A validation error on the contact form wipes everything the visitor typed. The form uses uncontrolled inputs with `<form action={action}>` (React 19 resets the form after the action completes), and the server returns `{ok:false}` for a short message or an odd-looking email — so the person sees 'That email address does not look right' with an empty name, email and message box.

- **Evidence:** hq/src/app/support/ContactForm.tsx:43 `<form action={action}>` with uncontrolled inputs (:50-57, :61-68, :85-93 — no value/defaultValue tied to the result); hq/src/app/support/actions.ts:42-47 returns {ok:false} for body <5 chars or a non-matching email (regex requires a dot after the @, which the browser's type=email check does not, so 'me@nowhere' passes the browser and fails the server). React source confirms the reset is unconditional: hq/node_modules/react-dom/cjs/react-dom-client.production.js startHostTransition → `function () { requestFormReset$1(formFiber); return action(formData); }` — the reset is requested before the action runs and applies when the transition commits, whatever the action returns (react-dom 19.1.0 per package.json). The mapper's Chromium run showed {name:'',email:'',body:''} after the error; the mechanism here is the same one.

- **Do this:** Echo the submitted values back and use them as defaults so the reset restores them: in hq/src/app/support/actions.ts extend ContactResult with `values?: { name: string; email: string; subject: string; body: string }` and include `values: { name, email, subject: field('subject'), body }` in every `{ ok: false … }` return (:43, :46, :61-65, :86-90). In hq/src/app/support/ContactForm.tsx set `defaultValue={result?.values?.name ?? ''}` on the name input, same for email and the textarea body, and `defaultValue={result?.values?.subject ?? TOPICS[0]}` on the select. Optionally also `key={result ? 'retry' : 'first'}` on the form is NOT needed. Alternatively switch the fields to controlled useState and call the action via `onSubmit` — but the defaultValue echo is the smaller change. Run `npx tsc --noEmit` in hq/ and re-test with a local `next dev` (dummy SUPABASE env) plus Playwright from /opt/node22/lib/node_modules/playwright.

### 34. [minor · small] hq-site: Measured colour-contrast failures on body-size text across the public site: orange section labels #e0503f on white 3.90:1 ('Who we are', 'Our apps', 'Highlights', 'FAQ', 'FAQ by game', 'Policies'); cyan #0088b0 on white 4.08:1 ('See our apps →', 'View app →', 'Full FAQ on the app page →'); the three claim badges 3.55–4.38:1; and the muted #a6a29d notes at 2.33:1 (contact-form small print) and 2.54:1 ('Last updated August 2026'). WCAG AA needs 4.5:1, and the stated audience includes grandparents.

- **Evidence:** Recomputed with the WCAG formula from hq/src/components/site/tokens.ts values: orange #e0503f on #ffffff = 3.90; cyan #0088b0 on #ffffff = 4.08; badge 'No ads' cyan on cyanTint #eef8fb = 3.78; 'No accounts' yellowDeepText2 #a67c1f on cream #fef6ee = 3.55; 'No tracking' orangeDeep #c8412f on orangeTint #fdeeec = 4.38; muted #a6a29d on offWhite #f7f5f2 = 2.33 and on white = 2.54. Usage: section labels at 12px 800 (page.tsx:144-153,178-187; dice-battles page :174,:191) — below the 18.66px/bold threshold for the 3:1 large-text exemption, so 4.5:1 applies; ContactForm.tsx:125 small print 12.5px in colors.muted; privacy/page.tsx:58 and terms/page.tsx:57 'Last updated' 13px in colors.muted; the 'See our apps →' pill uses colors.cyan on #fff (page.tsx:120-121).

- **Do this:** Adjust tokens in hq/src/components/site/tokens.ts so every body-size use passes 4.5:1, verified values: orange labels → use #b83a29 (5.71 on white) — add `orangeText: '#b83a29'` and switch the six section-label spans (hq/src/app/page.tsx:149,183; hq/src/app/apps/dice-battles-color-rush/page.tsx:174,191; hq/src/app/support/page.tsx label spans) from colors.orange to colors.orangeText; cyan text on white → add `cyanText: '#006f91'` (5.70 on white, 5.28 on cyanTint) and use it for the pill labels at page.tsx:121, apps/page.tsx:104 and PILL_TINTS[0].ink; badges: PILL_TINTS[1].ink → colors.yellowDeepText '#8a6d1f' (4.58 on cream), PILL_TINTS[2].ink → '#a6402f' (5.48 on orangeTint) — add as orangeDeepText; muted notes → change ContactForm.tsx:125, privacy/page.tsx:58, terms/page.tsx:57 and support/page.tsx:59 from colors.muted to colors.secondary '#6b6763' (5.15 on offWhite, 5.61 on white). Keep colors.orange/cyan for decorative fills (hero gradient, circles). Confirm with `node .claude/skills/site-check/check.mjs --live https://papershipstudio.com/` after deploy.

### 35. [minor · small] hq-site: Admin: deleting a news post is done through a ContentForm whose only button is labelled 'Save changes'; the small print above it says 'Saving below deletes this post for good'. A destructive action behind a Save button, on a page the children use.

- **Evidence:** hq/src/app/admin/news/page.tsx:141-148 `<ContentForm action={deleteNewsPost}>` containing only a hidden id and the text 'Saving below <strong>deletes</strong> this post for good.'; the comment at :141-142 shows it was done knowingly ('Its own form so deleting cannot be confused with saving — ContentForm's Save button is the only button in a form'), but hq/src/app/admin/content/ContentForm.tsx:42-44 renders `{pending ? 'Saving…' : 'Save changes'}` with no way to change the label, so the delete button really does read 'Save changes' and there is no confirm step. Admin-only, family audience, and news posts are recreatable — hence minor rather than major.

- **Do this:** In hq/src/app/admin/content/ContentForm.tsx add optional props `submitLabel?: string` (default 'Save changes'), `pendingLabel?: string` (default 'Saving…'), `idleNote?: string` (default 'Nothing is live until you press Save.') and `confirmText?: string`; render `{pending ? pendingLabel : submitLabel}`, the idleNote, and when confirmText is set attach `onSubmit={(e) => { if (!window.confirm(confirmText)) e.preventDefault(); }}` to the form. In hq/src/app/admin/news/page.tsx:143 use `<ContentForm action={deleteNewsPost} submitLabel="Delete this post" pendingLabel="Deleting…" idleNote="This cannot be undone." confirmText="Delete this post for good?">` and change the small print at :146 to 'Deleting removes this post from the game for good.' Run `npx tsc --noEmit` in hq/.

### 36. [minor · small] integrations: The long-lived device secret is sent in the URL query string on every Friends-screen open, so it lands in Vercel request logs and any intermediate logging; the POST variants correctly put it in the body.

- **Evidence:** Fact confirmed: src/game/friendsApi.ts:106 `/friends?playerId=${…}&secret=${encodeURIComponent(me.secret)}` and hq/src/app/api/friends/route.ts:59 `authenticate(params.get('playerId'), params.get('secret'))`; playerIdentity.ts:49 says 'never logged'. Severity overstated for launch: transport is HTTPS so no intermediary sees the URL; the only party whose logs hold it is David's own Vercel project (Hobby runtime-log retention is short), and players/route.ts:15-21 is explicit that the secret guards trophy counts and friend actions, not anything monetisable. It is still wrong for a credential and trivially fixed.

- **Do this:** Move the secret out of the URL. In /home/user/Dice-battles-/src/game/friendsApi.ts `fetchFriends` (lines 102-117) call `call<FriendList>('/friends?playerId=' + encodeURIComponent(me.playerId), { headers: { 'x-player-secret': me.secret } })`. In /home/user/Dice-battles-/hq/src/app/api/friends/route.ts GET (line 59) read `request.headers.get('x-player-secret') ?? params.get('secret')` (keep the query fallback for one release so phones on the old JS still work, then remove it). Add an assertion to tests/friends.test.ts near the 'no-secret-select' rule (around 272-375) that friendsApi.ts contains no `secret=` in a template string.

### 37. [minor · small] menus: A large share of menu text is 10–12pt, and the tab labels are 10pt with system text scaling switched off — the one place a grandparent's larger-text setting is ignored is the navigation.

- **Evidence:** `grep -rnE 'fontSize: (10|11|12)(\.5)?\b' src/demo src/ui` → 34 hits, but `fontSize: 10\b` → only six: src/demo/InventoryScreen.tsx:336,343,349,354 (equippedTag/tapTag/priceTagText/priceTag), src/demo/StoreScreen.tsx:271 (ownedTag) and src/ui/theme.ts:152 `nav: { fontSize: 10, fontWeight: '800' }`. `grep -rn allowFontScaling src/` → only src/demo/BottomNav.tsx:81 `allowFontScaling={false}`, so every other menu string does follow the system text size. That one exception is deliberate and pinned by tests/layout.test.ts:89-99 (`assert(/allowFontScaling=\{false\}/.test(label!), 'tab labels scale with system text size, which breaks the row')`) and mirrors UITabBar's own 10pt non-scaling labels. The rationale is stale, though: BottomNav.tsx:73-79 says 'Seven cells across the narrowest iPhone leaves about 53pt each' and the test is titled 'seven labels each hold one line', while TABS at BottomNav.tsx:39-44 has five entries and the next test (:101-110) asserts exactly five.

- **Do this:** Reviewer a has the better read: the nav is platform-consistent and its non-scaling is a tested decision, so leave `allowFontScaling={false}` and `TYPE.nav` alone for 1.0 — changing them means rewriting tests/layout.test.ts:89-99 for a behaviour Apple's own tab bar does not offer. What is genuinely small is the five 10pt badges: in src/demo/InventoryScreen.tsx lines 336, 343, 349, 354 and src/demo/StoreScreen.tsx line 271 replace `fontSize: 10` with `...TYPE.label` (11.5pt/800 — import TYPE from '../ui/theme' if not already), keeping each style's colour/letterSpacing/marginTop, and eyeball that OWNED/EQUIPPED still sit on one line in the grid card. Separately fix the stale copy: in src/demo/BottomNav.tsx:73-79 change 'Seven cells … about 53pt' to 'Five cells … about 75pt' and rename the test title at tests/layout.test.ts:89 from 'seven labels' to 'five labels' with the matching comment at :90-91. Run `npm test` and `npx tsc --noEmit`. If David later wants scaling nav labels, the safe form is `maxFontSizeMultiplier={1.3}` in place of `allowFontScaling={false}` plus the test change — a decision, not a launch item.

### 38. [minor · small] menus: Many controls fall under the theme's own 44pt floor; MIN_TAP is declared 'Nothing tappable is smaller than this' and never imported anywhere.

- **Evidence:** `grep -rn MIN_TAP src tests` → only src/ui/theme.ts:156 `export const MIN_TAP = 44;`. Measured from the styles: DiceDemoScreen.tsx:2465-2472 difficultyButton `paddingVertical: 9` + 14pt text ≈ 35pt; :2024-2036 quitButton `width: 40, height: 40` with no hitSlop (grep hitSlop in DiceDemoScreen → none); :2542-2548 codeGo `paddingVertical: 10` + 14pt ≈ 37pt; LeaderboardScreen.tsx:361-371 gcButton `paddingVertical: 11` + 13.5pt ≈ 38pt; TournamentScreen.tsx:284 quietButton `paddingVertical: 12` + 13pt ≈ 39pt; FriendsScreen.tsx:431 smallButton `paddingVertical: 10` + 14pt ≈ 37pt, :445 back `paddingVertical: 6` + 17pt ≈ 32pt; TwoPlayerScreen.tsx:536-549 exitButton `width: 36, height: 36`. None of those files use hitSlop. Calibration: all are tappable in practice; this is HIG guidance, not an App Review rejection.

- **Do this:** Import MIN_TAP from src/ui/theme.ts and add `minHeight: MIN_TAP, justifyContent: 'center'` to: difficultyButton, codeGo (src/demo/DiceDemoScreen.tsx), gcButton (src/demo/LeaderboardScreen.tsx), quietButton (src/demo/TournamentScreen.tsx), smallButton, wideButton and back (src/demo/FriendsScreen.tsx). For the fixed-size round buttons add `hitSlop={4}` on quitButton's Pressable (DiceDemoScreen) and `hitSlop={6}` on exitButton's Pressable (TwoPlayerScreen.tsx) rather than growing the circles. Add a regex test in tests/layout.test.ts asserting MIN_TAP is imported by those files. Run npm test; check tests/popupLayout.test.ts still passes since the Settings panel height changes slightly.

### 39. [minor · medium] menus: A cup run lives only in React state, so killing the app mid-run loses the run and the 50/150-coin entry fee with it.

- **Evidence:** src/demo/DiceDemoScreen.tsx:261 `const [run, setRun] = useState<RunState | null>(null);`; src/game/tournament.ts:14 'Pure: no storage, no React.'; `grep -rn AsyncStorage src/game/tournament.ts` → nothing; `grep -rn 'saveRun|loadRun|persistRun' src/game` → nothing; the hydrate chain at :185-192 loads audio/loadout/progress/wallet/colorblind/tutorial and no run. Entry fee is spent at :657-659 before startRun. Calibration: it is in-game coins, a run is 2-3 rounds long, and iOS keeps a backgrounded app's state most of the time, so this bites on a force-quit or an eviction, not every day.

- **Do this:** Add `saveRun(run: RunState | null): Promise<void>` and `loadRun(): Promise<RunState | null>` to a new src/game/tournamentStore.ts (AsyncStorage key e.g. 'dicebattles.cupRun', JSON, validate `tournamentById(id)` exists and `finished === null` on load, otherwise return null). In src/demo/DiceDemoScreen.tsx add `loadRun()` to the Promise.all at :185-192 and `setRun(savedRun)` in the then; add `useEffect(() => { void saveRun(run); }, [run]);` after the run state. Keep src/game/tournament.ts pure. Add tests/tournamentStore.test.ts covering round-trip, unknown id → null, finished run → null.

### 40. [minor · medium] menus: Text slots on the home screen are fixed-height boxes while their text still scales with the system text size, so at larger Dynamic Type the 'Next unlock' line, the mode rules and the difficulty hint are clipped; tests lock the fixed heights in.

- **Evidence:** src/demo/DiceDemoScreen.tsx:2081-2090 trophyNext `fontSize: 13.5, … lineHeight: 18, height: 18`; :2436-2446 modeRules `lineHeight: 18, height: 36`; :2455-2464 difficultyHint `lineHeight: 18, height: 36`. These Texts (:1502-1512, :1241) carry no `allowFontScaling={false}` or `maxFontSizeMultiplier` (grep: only BottomNav.tsx:81 disables scaling), so at a 1.35x+ text size the glyphs outgrow an 18pt line and the fixed box. tests/screen.test.ts:1065 `test(`${name} reserves its height`` asserts `height` and `lineHeight` are present and a whole-line multiple, which locks the fixed box in but does not prevent scaling it.

- **Do this:** In src/demo/DiceDemoScreen.tsx: import `PixelRatio` from react-native and add `const FONT_SCALE = Math.min(PixelRatio.getFontScale(), 1.5);` near the styles. For trophyNext, modeRules and difficultyHint set `lineHeight: Math.round(18 * FONT_SCALE)` and `height: Math.round(18 * FONT_SCALE) * lines` (1, 2, 2), and add `maxFontSizeMultiplier={1.5}` on the three Text elements (:1502-1512, the modeRules Text, :1241) so the cap and the box agree. Update tests/screen.test.ts:1065-1080 to read the `18 * FONT_SCALE` form (regex `lineHeight:\s*Math.round\((\d+)`) or compute lines from the literal 18 and the multiplier. Run npm test.

### 41. [minor · small] tests-tooling: Save-file migration is untested. loadProgress fills modeWins/wins for saves written before those fields existed and swallows junk JSON, but no test calls it; the persistence suite covers loadLoadout and audio settings only. A regression here silently zeroes a player's trophies on the next launch.

- **Evidence:** The fact is exactly right: `grep -rn loadProgress tests/` returns nothing, and tests/persistence.test.ts imports loadLoadout only. I side with the skeptic on severity, not on the verdict label — the "what" states a coverage gap plus a hypothetical ("a regression HERE would..."), and the gap is real, so it is confirmed rather than partly. Reading src/game/progress.ts:335-352, the function is twenty lines: one AsyncStorage read, one JSON.parse, one spread merge with `wins: {...EMPTY_WINS, ...(parsed.wins ?? {})}` and the same for modeWins, all inside a single try/catch whose comment says defaults are fine. It cannot zero trophies as written — junk JSON throws into the catch, a non-object parse spreads harmlessly, and the loading order means a failed read never overwrites a good save. So: an untested but currently-correct function, guarding the one thing a family would actually mourn (months of trophies), on code nobody is editing before launch. Minor, and cheap enough that there is no reason not to.

- **Do this:** Add cases to tests/persistence.test.ts, which already imports tests/storageMock.ts. Seed the mock with (1) a pre-modeWins save `{"trophies":420,"wins":{"easy":3,"medium":1,"hard":0}}` and assert loadProgress() returns trophies 420, those wins, and every modeWins key 0 rather than undefined; (2) `'{'` and `'null'` and assert defaults survive and nothing throws; (3) a full save round-tripped through applyMatchResult then loadProgress with identical numbers. Call resetProgressForTests (src/game/progress.ts:365) between cases so module state does not leak. Under 30 lines, no new files, no run.ts change.

### 42. [minor · small] tests-tooling: crashGuard.ts — the thing that keeps a fatal on screen instead of letting expo-updates abort the process — is never imported by any test. installCrashGuard's ErrorUtils wrapping, reportFatal's listener isolation and describe()'s stack truncation are unverified; CrashScreen is checked only for palette and emoji.

- **Evidence:** Both reviewers established the same facts and I confirmed them: the only grep hit for crashGuard/reportFatal/installCrashGuard across tests/ is tests/ads.test.ts:148, which is Metro's `global.ErrorUtils.reportFatalError(e)` quoted inside a comment — a red herring, not coverage. I read all 70 lines of src/debug/crashGuard.ts. It is correct and defensive: reportFatal coerces non-Errors via `new Error(String(error ?? 'Unknown error'))`, wraps each listener in its own try/catch ("A failing listener must not become a second crash"), describe() slices the stack to 12 lines, and installCrashGuard returns early when globalThis.ErrorUtils is absent — which is precisely why the node suite never exercises it — and deliberately withholds fatals from the previous handler because that handler is what aborts. Verdict is confirmed on both sides' own evidence; the split was purely severity, and the skeptic has it right. This is short, straight-line, already shipping since the early TestFlight builds, and not on anyone's edit path this week. Its value is real (without it a crash makes the app vanish and "it just closed" is an unactionable bug report) but nothing about it is subtle enough to rot silently.

- **Do this:** Add tests/crashGuard.test.ts and register it in tests/run.ts:10-39 — that import list is hand-maintained, so an unregistered file silently never runs. Set a fake `globalThis.ErrorUtils = {getGlobalHandler, setGlobalHandler}`, call installCrashGuard, assert the handler was replaced; invoke the installed handler with isFatal true and assert getLastFatal() returns the error and the previous handler was NOT called, then with isFatal false and assert it WAS. Assert reportFatal('a string') stores an Error with that message; register two listeners where the first throws and assert the second still fires and nothing propagates; assert onFatal's returned unsubscribe stops delivery; assert describe(null) === 'No error recorded.' and that a 40-line stack yields at most 12 stack lines. Delete the fake ErrorUtils in a finally so the global does not leak into later suites.

### 43. [minor · small] tests-tooling: The runtimeVersion test encodes an exception that contradicts the written rule, and its gate is specific to one SDK. AGENTS.md says runtimeVersion 'is an explicit string in app.json ("1.1.0"), never the sdkVersion policy'; app.json is on `{policy: 'sdkVersion'}` and the test's ads-OFF branch accepts that. Other native packages already in package.json (expo-game-center, expo-haptics) get no equivalent gate, and StoreKit is planned next.

- **Evidence:** Every ingredient is real: AGENTS.md states the rule absolutely; app.json:43-45 is `"runtimeVersion": { "policy": "sdkVersion" }`; tests/ads.test.ts:186-190 is the ads-OFF branch and asserts only `runtime !== undefined`; `grep -rn expo-game-center tests/` returns zero, so no native package other than AdMob is gated. And the exposure the skeptic describes is real in principle — `git log -S expo-game-center -- package.json` dates the dependency to 3a4bf71 on 2026-08-23, while build 5 (the approved TestFlight build the family plays, STATE.md:17) was cut on 19 Aug, and src/game/gameCenter.ts:71 holds a live literal `require('expo-game-center')` whose surrounding try/catch AGENTS.md itself says is not a safety net. My export confirms the module ships: ExpoGameCenter appears 4 times in the hbc.

But I chased the load path to the bottom and it does not bite, which is why this is 'partly'. node_modules/expo-game-center/build/ExpoGameCenterModule.js does its `requireNativeModule('ExpoGameCenter')` inside its own try/catch, falls back to `require('expo')`, and on failure assigns null — and build/index.js does no module-scope native work at all (its default export is a plain object of lazy arrow wrappers). So the Metro module factory completes without throwing and guardedLoadModule never escalates to a red screen. One subtlety neither reviewer caught: because that wrapper object always carries a `submitScore` function, the presence check at src/game/gameCenter.ts:72-73 (`typeof candidate.submitScore === 'function'`) PASSES on a binary with no native module — the intended no-op fallback does not engage, and calls dereference null instead. It is still safe only because every call site catches (the inner try in signIn, post(), openLeaderboard, openAchievements). Two accidents deep, not one.

- **Do this:** Siding with the impact reviewer on severity and fixClass, but NOT on their wording — their proposed AGENTS.md replacement ('the sdkVersion policy is correct while no NEW native module is referenced by live JS') is wrong and would do harm: expo-game-center IS a new native module referenced by live JS today, so that rule applied literally demands an explicit runtimeVersion, which without a matching binary silently cuts every install off from OTA updates. The distinction that actually holds is whether the package THROWS AT LOAD on a binary without it. Rewrite the AGENTS.md sentence to say: the sdkVersion policy stays while every native package live JS requires either resolves lazily or swallows its own load failure; an explicit runtimeVersion string, raised in the same change and built, is required from the moment one can throw at module scope — and add the warning that setting an explicit string without shipping the binary strands every installed phone with no error anywhere. Then generalise tests/ads.test.ts:133-195 beyond AdMob: enumerate packages in package.json that ship native code, grep live src for a literal `require('<pkg>')` or module-scope import, and for each require the runtimeVersion to be an explicit /^\d+\.\d+\.\d+$/ string unless the package is on an explicit exemption list carrying a one-line reason (expo-game-center: swallows its own load failure in ExpoGameCenterModule.js; expo-haptics: compiled into build 5, added 16 Aug). Do this BEFORE StoreKit lands — a StoreKit-shaped module resolving at module scope is the case the current one-SDK gate would miss. Two things not to do: do not chase a device repro on build 5 first (the skeptic's gate), since the load path is readable and reads safe; and do not set an explicit runtimeVersion 'to match the docs'.


## A2. Minor and cosmetic fixes from the triage pass (41)

Lower stakes; still validated by an agent that opened the code.

### 1. [minor · small] arenas-dice: All five preview/shoot tools hard-code a machine-specific Chromium binary path that exists only in this container, so on David's Mac `node tools/arena-preview/shoot.js` fails at browser launch after the README's install steps, and neither README mentions it — for a tool whose README says it is the only way anyone can see an arena.

- **Evidence:** Identical line in all five: tools/arena-preview/shoot.js:6, tools/arena-preview/audit.js:79, tools/icon-preview/shoot.js:5, tools/screen-preview/shoot.js:5, tools/store-shots/shoot.js:6 — `executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome',` inside chromium.launch({...}). tools/arena-preview/README.md:7-16 gives npm i / esbuild / node run steps with no mention of it, so the documented path fails at launch with ENOENT anywhere that directory is absent.

- **Do this:** In each of tools/arena-preview/shoot.js:6, tools/arena-preview/audit.js:79, tools/icon-preview/shoot.js:5, tools/screen-preview/shoot.js:5 and tools/store-shots/shoot.js:6, replace the line `executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome',` with `...(process.env.CHROME_PATH ? { executablePath: process.env.CHROME_PATH } : {}),` so Playwright resolves its own bundled Chromium by default and this container still works via the env var. Then in tools/arena-preview/README.md (in the code block at lines 7-16, after the `npm i --no-save` line) and tools/icon-preview/README.md (same place, lines 10-18) add `npx playwright install chromium` plus one sentence: 'Set CHROME_PATH to override which Chromium is used.' Nothing under tools/ is in the Metro bundle, so this needs no tsc run, no test run and no OTA — it can ride whatever ships next, or land on its own. Verify with `CHROME_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome node tools/icon-preview/shoot.js`.

### 2. [minor · small] config-release: Docs disagree about which binary is on phones and where the icon lives: STATE.md says TestFlight build 5 and that assets/icon.png holds the settled icon; code comments say build 6; app.json uses assets/icon/light.png and assets/icon.png is an unreferenced 218 KB orphan.

- **Evidence:** assets/icon.png (md5 dec4005..., last committed 3c6ed6a 'the Perfect Match icon', file dated 17 Aug) is referenced by nothing in the build — app.json:8,13-17 points at assets/icon/light.png (md5 9ab74b5..., commit 90b5363 'Draw the app icon in light, dark and tinted', 25 Aug). STATE.md:162-164, last updated 30 Aug, still tells the reader assets/icon.png 'holds the settled design'. This matters more than ordinary doc rot: AGENTS.md orders every session to read STATE.md first, and the next native build is the launch build — a session acting on that line would edit the orphan and ship the wrong icon. 'build 6' appears only in src/game/adSdk.ts:25 and tests/ads.test.ts:178,447; CHANGELOG.md:2699,2725 and STATE.md:17 say build 5. Which is truly on phones cannot be settled from the repo (asc.py needs ASC_ISSUER_ID, not available here).

- **Do this:** In /home/user/Dice-battles-/STATE.md, rewrite the 'The app icon on phones is stale' bullet at lines 162-164 to name assets/icon/light.png, dark.png and tinted.png (app.json:8,13-17) as the settled icon set and to state that assets/icon.png is a superseded orphan referenced by nothing. In the same edit, change the 'TestFlight (build 5, approved)' cell at STATE.md:17 to record that build 5 and 6 are both valid in App Store Connect and which one the family is on has not been re-verified — do not assert a number. Leave assets/icon.png on disk; deleting it is churn.

### 3. [minor · small] config-release: README.md still recommends skipping ads entirely and selling an unlock, contradicting the 24 Aug 2026 decision in AGENTS.md that ads ship in 1.0.

- **Evidence:** README.md:190-194, item 4 of the future-ideas list: 'The recommendation is to sell the unlock and skip ads entirely.' AGENTS.md's advertising section records the opposite decision, taken 24 Aug 2026, with David accepting a delayed App Store launch to get ads into 1.0. This is a public repository, and documentation drift of exactly this shape (AGENTS.md describing a runtimeVersion state that was reverted) is already one of the majors on this map.

- **Do this:** In /home/user/Dice-battles-/README.md, replace the final sentence of item 4 (line 193-194, 'The recommendation is to sell the unlock and skip ads entirely.') with a note that David decided on 24 Aug 2026 that ads ship in 1.0 — one child-directed, non-personalised, G-rated interstitial after every third finished game — and that a remove-ads unlock sits alongside them rather than replacing them (STATE.md:122-128 records it at $3.99). Do not touch the COPPA/Kids-Category sentences above it; they are still accurate.

### 4. [minor · small] gameplay: The AI interval callback has no phase guard, so a tick that lands between the player's winning settle and React's effect cleanup calls finishRound a second time: coins are paid twice and a loss is recorded on top of the win (phase flips to 'lost').

- **Evidence:** src/demo/DiceDemoScreen.tsx:776-862 — the effect guards on `phase !== 'battle'` at :777, but the setInterval body at :779-780 starts straight at `const roll = rollAiDice();` with no phase check, and reaches `finishRound('lost')` at :807, `finishRound(pc > ac ? ...)` at :836 and `finishRound('lost')` at :855. `grep -n phaseRef.current` returns only :540, :875, :1023, :1034 — handleSettled has the guard (:875), the AI path does not. setPhaseBoth (:385-387) sets phaseRef synchronously, so phaseRef is already 'won' while the interval is still alive: clearInterval only runs in the passive-effect cleanup after commit. finishRound (:547-630) is not idempotent — noteGameFinished, awardCoins and applyMatchResult all run again, and setPhaseBoth('lost') repaints the victory screen as a defeat.

- **Do this:** In src/demo/DiceDemoScreen.tsx, insert as the very first statement inside the setInterval callback (line 779, immediately before `const roll = rollAiDice();` on line 780):

      // The interval outlives the winning roll by a tick: it is cleared in
      // the effect cleanup, which runs after the commit. Same guard as
      // handleSettled.
      if (phaseRef.current !== 'battle') return;

That one line covers all three finishRound call sites in the callback (:807, :836, :855) and also stops the AI rolling on after the game is over. Nothing else changes. Validate with `npx tsc --noEmit`, `npm test`, and `npx expo export --platform ios --output-dir /tmp/export-test`.

### 5. [minor · small] gameplay: Backgrounding mid-roll decides the roll from a die frozen in mid-air: elapsed time is wall-clock, so on return the 3200 ms backstop fires on the first frame, the die is teleported to the floor with its airborne orientation and that face is counted.

- **Evidence:** src/demo/DiceScene.tsx:199 sets `throwStartedAt.current = Date.now()` and :346 computes `const elapsed = Date.now() - throwStartedAt.current`. src/dice/settle.ts:112 is `return elapsedMs >= s.hardMaxRollMs;` — the only path with no motion, sleep or grounded test at all, so after a >3.2 s background the very first resumed frame calls the roll whatever the dice are doing. src/dice/die.ts:103 then does `body.position.y = TUNING.dieSize / 2`, teleporting a mid-air die to the floor on whatever face was nearest up, and freezeDice locks it. `grep -rn AppState src/` returns nothing — no pause/resume handling anywhere. The precedent for the fix is already in the file: the moat respawn resets `throwStartedAt.current = Date.now()` at :303 for exactly this reason ('measuring from the original throw charged it for the time it spent underwater').

- **Do this:** In src/demo/DiceScene.tsx: add `import { AppState } from 'react-native';` (core React Native, already in every shipped binary — no new native module, so runtimeVersion does not move and this stays OTA-shippable). Then add an effect alongside the other useEffects (near the collision-haptics effect at :174):

  useEffect(() => {
    const sub = AppState.addEventListener('change', (next) => {
      // Elapsed is wall-clock, but the render loop stops while the app is
      // away. Without this the 3200 ms backstop fires on the first frame
      // back and calls the roll off a die still in the air.
      if (next === 'active' && awaitingSettle.current) {
        throwStartedAt.current = Date.now();
      }
    });
    return () => sub.remove();
  }, []);

Do not touch shouldCallRoll or any TUNING constant. Validate with `npx tsc --noEmit`, `npm test` and the Metro export; the headless suite cannot see AppState, so confirm on a device — start a roll, background the app 5+ seconds, return, and the dice should carry on tumbling and settle normally instead of snapping instantly.

### 6. [minor · small] gameplay: Coins earned per battle are computed and stored but never shown; the result screens report trophies only, although the tutorial promises 'You earn coins too'.

- **Evidence:** `grep -rni lastcoins src/` returns exactly two hits: the declaration at src/demo/DiceDemoScreen.tsx:341 and the setter at :571. The value is never rendered. The won screen shows only `styles.trophyLine` (:1616-1618), the tie screen no reward line at all (:1632-1642), the lost screen only trophies (:1954). src/game/tutorial.ts:87 promises 'You earn coins too, and losing never takes those away. Spend them in the Store.' Worse, the comment at :1726-1731 justifies hiding the StatsHud coin pill on result screens with 'The result screen reports the trophies and coins won in its own text anyway' — which is not true today. Cup prizes are announced (:601) but per-battle coins never are.

- **Do this:** In src/demo/DiceDemoScreen.tsx add one line to each of the three result screens, directly under the trophy line and above `{difficultyRow}`:

  <Text style={[styles.coinLine, styles.onGlass]}>+{lastCoins} coins → {wallet.coins}</Text>

— won: after the styles.trophyLine Text that closes at :1618; tie: as the first child after the overlayBody Text in the block at :1632 (the tie pays the losing rate, so the line is still correct); lost: after :1954. `lastCoins` (:341) and `wallet` (:262) are already in scope. Add the style next to `trophyNext` at :2134:

  coinLine: { color: THEME.inkSoft, fontSize: 13.5, fontWeight: '700', marginTop: 4 },

(that is trophyNext without its fixed `height: 18`, which exists only to stop the home-screen column shifting). Reuse THEME — no new colours. This also makes the comment at :1726-1731 true. Validate with `npx tsc --noEmit` and the Metro export.

### 7. [minor · small] hq-api: Security headers are still scoped to the old /hq path, so the admin at /admin no longer receives X-Frame-Options, noindex or Referrer-Policy.

- **Evidence:** hq/next.config.ts:11 is still `source: '/hq/:path*'`, and `ls hq/src/app` shows no `hq` directory at all — the admin lives at hq/src/app/admin and hq/src/middleware.ts:50 matches '/admin/:path*'. So the header block matches nothing. Partial mitigation exists: hq/src/app/admin/layout.tsx:8 and hq/src/app/password/page.tsx:8 both set `robots: { index: false, follow: false }` as Next metadata, so noindex is genuinely covered by a meta tag. What is actually lost is X-Frame-Options: DENY and Referrer-Policy: same-origin on the admin — i.e. clickjacking protection on a login-gated board.

- **Do this:** In /home/user/Dice-battles-/hq/next.config.ts, change `source: '/hq/:path*'` (line 11) to `source: '/admin/:path*'`, and add a second entry in the same returned array with `source: '/password'` and the identical three headers so the password-change page is covered too. Leave the public site (`/`, /apps, /support, /privacy, /terms) out of this block — X-Robots-Tag noindex must never reach those pages. Verify with `cd hq && npx tsc --noEmit`, then deploy with `npx vercel deploy --prod` from hq/ (a push does not redeploy). This is a website-only change and does not touch the App Store submission or the Fable pre-submission gate.

### 8. [minor · small] hq-api: Database read errors during authentication are ignored, producing misleading results: in players an existing player is treated as new (then hits a PK violation), in friends a DB outage reads as 'no such player'; raw Postgres error messages are returned to clients on every 500.

- **Evidence:** hq/src/app/api/players/route.ts:74 `const { data } = await supabase...maybeSingle()` — `error` is destructured away, and line 80 then returns `isNew: true`, so a transient Supabase read failure makes an existing player look unclaimed and the code falls through to the insert at 141, which fails on the primary key and returns a raw Postgres message. hq/src/app/api/friends/route.ts:44-49 has the same shape: `const { data } = ...` then `if (!data) return { error: 'no such player' }`, so a DB outage is reported to the player as a 401 wrong-player error. Raw messages are returned verbatim at players/route.ts:154 and :165 and friends/route.ts:217, exposing table and constraint names.

- **Do this:** Two files. (1) /home/user/Dice-battles-/hq/src/app/api/players/route.ts: in `authenticate` (line 72), destructure `const { data, error } = await supabase...maybeSingle();` and immediately after add `if (error) return { error: 'could not reach the database' };` BEFORE the `if (!data) return { ...isNew: true }` line, so a read failure never claims an existing id. Then at line 154 replace `taken ? 'friend code taken' : error.message` with `taken ? 'friend code taken' : 'could not save your profile'` (keep the 23505/friend_code branch and its 409 exactly as is — the game relies on it to redraw a code), and at line 165 replace `error.message` with `'could not save your profile'`. (2) /home/user/Dice-battles-/hq/src/app/api/friends/route.ts: in `authenticate` (line 43), destructure `const { data, error } = await supabaseAdmin()...` and add `if (error) return { error: 'could not reach the database' };` before line 49's `if (!data)`; at line 217 replace `failed.error.message` with `'could not save that change'`. Do not touch queue/route.ts or proposals/route.ts — those are HQ_API_TOKEN-gated and the raw message is useful to the builder. Verify with `cd hq && npx tsc --noEmit` and `npm test` from the repo root (tests/friends.test.ts regex-reads both route files; none of its patterns — `/^  (\w+):\s*\{ from:/gm`, `/if \(action !== 'block'\)/`, `/secret_hash: hash\(/`, `/select\([^)]*\bsecret\b(?!_hash)/` — match anything changed here).

### 9. [minor · small] hq-api: friends GET selects every column of each friend's profile — including secret_hash and friend_code — into the handler; safety depends entirely on the `full()` whitelist, which no test covers.

- **Evidence:** hq/src/app/api/friends/route.ts:83-86 is `.from('player_profiles').select('*').in('player_id', ...)`, so secret_hash and friend_code are read into memory for every friend on every open of the Friends screen. Nothing leaks today — `full()` at 95-106 and `peek()` at 107-111 name each field — but tests/friends.test.ts:345-353 only slices `const peek =` … `const friends`, so `full()` is untested and a future `...p` spread would ship secret_hash to every friend without failing CI.

- **Do this:** In /home/user/Dice-battles-/hq/src/app/api/friends/route.ts line 85, replace `.select('*')` with `.select('player_id, name, trophies, wins, mode_wins, dice_owned, arenas_owned, favourite_die, favourite_arena, last_played')`. Those are exactly the ten columns `full()` (95-106) and `peek()` (107-111) read, and every one is written by cleanProfile/insert in hq/src/app/api/players/route.ts:40-55,141-147, so the column names are verified against the code that creates them. Verify with `cd hq && npx tsc --noEmit` and `npm test` from the repo root — the suite's `/select\([^)]*\bsecret\b(?!_hash)/` guard still passes because the new list contains no `secret` token.

### 10. [minor · small] hq-site: On phones the hero stacks two paddings: the ≤760px rule adds `padding:40px 24px !important` to `.psg-hero` (which had none) while `.psg-hero-text` keeps its desktop `64px 24px 64px 56px`, so the headline starts ~100px in on a 390px screen, wraps to five lines, and runs over the orange decorative circle (white on #e0503f ≈3.9:1).

- **Evidence:** hq/src/app/site.module.css:69-71 — the ≤760px `.psg-hero-text` rule overrides only `max-width: 100% !important`, nothing else. hq/src/app/page.tsx:81 sets inline `padding: '64px 24px 64px 56px'` which therefore survives, on top of `.psg-hero`'s new `40px 24px` (css:66-68). Left inset on a 390px phone is 24+56 = 80px of a 390px viewport, and the h1 is still 30px (css:72-74). (Map cites css:484-489; the file is 91 lines.)

- **Do this:** In /home/user/Dice-battles-/hq/src/app/site.module.css, inside the `@media (max-width: 760px)` block, extend the `.page :global(.psg-hero-text)` rule (line 69) from `max-width: 100% !important;` to also carry `padding: 0 !important;`. That leaves `.psg-hero`'s own `40px 24px` as the only phone padding. Do not touch the desktop inline padding in /home/user/Dice-battles-/hq/src/app/page.tsx:81. Re-run the `site-check` skill at 390px and confirm the h1 starts at x≈24 and drops to three lines.

### 11. [minor · small] hq-site: 'Last updated August 2026' is hard-coded in the page, not derived from `site_content.updated_at`, while the policy itself promises 'we'll update this page and change the date at the top' and the admin invites free editing. Any edit David makes leaves the date wrong.

- **Evidence:** hq/src/app/privacy/page.tsx:71 and hq/src/app/terms/page.tsx:62 both render the literal string 'Last updated August 2026 · Applies to all Paper Ship Studio apps'. hq/src/lib/content.ts:14-21 `getSiteContent` selects only `key, value` — `updated_at` is never read anywhere in hq/src. (Map cites privacy:58-60/terms:132-134; real lines are 71 and 62.)

- **Do this:** Sequence this AFTER the privacy/terms rewrite the blocker items force, then: in /home/user/Dice-battles-/hq/src/lib/content.ts add `export async function getContentUpdatedAt(prefix: string): Promise<Date | null>` that does `.from('site_content').select('updated_at').like('key', prefix + '%').order('updated_at', { ascending: false }).limit(1)`, returns null on error or no rows, and is wrapped in the same try/catch as the item-24 fix. Call it with 'privacy_' from /home/user/Dice-battles-/hq/src/app/privacy/page.tsx and 'terms_' from /home/user/Dice-battles-/hq/src/app/terms/page.tsx, format as `toLocaleDateString('en-GB', { month: 'long', year: 'numeric' })`, and keep the current literal only as the null fallback. Both pages are already force-dynamic so no caching change is needed.

### 12. [minor · small] hq-site: Security headers in next.config.ts still target `/hq/:path*`, but the admin moved to `/admin` on 18 Aug (commit 48b8264). X-Frame-Options: DENY and Referrer-Policy therefore no longer apply to the admin or login.

- **Evidence:** hq/next.config.ts:10 is `source: '/hq/:path*'` and it is the only entry in `headers()`. No route under /hq exists any more; the admin is hq/src/app/admin/ and the sign-in form is hq/src/app/login/page.tsx. So the family's login form and the whole admin are currently frameable.

- **Do this:** In /home/user/Dice-battles-/hq/next.config.ts replace the single `/hq/:path*` entry with three entries carrying the same three headers (X-Robots-Tag 'noindex, nofollow'; X-Frame-Options 'DENY'; Referrer-Policy 'same-origin'): sources `/admin/:path*`, `/login` and `/password`. Update the comment above `headers()` which still says 'The HQ'. This also closes the '/login is not noindexed' half of item 25 — hq/src/app/login/page.tsx is a 'use client' file so it cannot export `metadata`, and the header is the right lever. Verify with `npm run build && npm start` then `curl -sI localhost:3000/admin` and `curl -sI localhost:3000/login`.

### 13. [minor · small] hq-site: A failed magic-link sign-in is silent: the auth callback redirects to `/login?error=<message>` but the login page only reads the `next` param, so the visitor lands on a blank login form with no explanation.

- **Evidence:** hq/src/app/auth/callback/route.ts:24 redirects to `${origin}/login?error=${encodeURIComponent(error.message)}`. hq/src/app/login/page.tsx:22-23 has only `params.get('next')`; `grep error= ` finds no read of the param. The page already owns an `error` state (line 27) and renders it, so the message has a slot and nothing fills it.

- **Do this:** In /home/user/Dice-battles-/hq/src/app/login/page.tsx, in `LoginForm`, seed the existing error state from the URL: change `const [error, setError] = useState<string | null>(null);` (line 27) to `useState<string | null>(params.get('error'))`. `params` is already in scope at line 22. No other change — the existing render path shows it and the sign-in handler clears it on the next attempt.

### 14. [minor · small] hq-site: Section headings on the public pages are `<span>`s, not headings ('Who we are', 'Our apps', 'Highlights', 'FAQ', 'FAQ by game', 'Policies'), the Dice Battles card title on Home is a span, Privacy and Terms have no `<main>`, and the active nav item is signalled only by #201e1d vs #4a4744 with no aria-current — screen-reader and keyboard users get no page structure or location cue.

- **Evidence:** Uppercase section labels are spans at hq/src/app/page.tsx:148,182; apps/dice-battles-color-rush/page.tsx:180,197; support/page.tsx:72,116. `grep '<main'` returns hits for page.tsx, apps/page.tsx, support/page.tsx and the app page but NOT privacy/page.tsx or terms/page.tsx. hq/src/components/site/SiteHeader.tsx:44-52 colours the active Link with no aria attribute.

- **Do this:** Three mechanical edits, no visual change. (1) In /home/user/Dice-battles-/hq/src/components/site/SiteHeader.tsx add `aria-current={active === item.label ? 'page' : undefined}` to the nav `<Link>` (line 45-51). (2) Wrap the page bodies of /home/user/Dice-battles-/hq/src/app/privacy/page.tsx and /home/user/Dice-battles-/hq/src/app/terms/page.tsx in `<main>` to match hq/src/app/support/page.tsx:45. (3) Convert the six uppercase section-label spans (page.tsx:148,182; apps/dice-battles-color-rush/page.tsx:180,197; support/page.tsx:72,116) and the Home Dice Battles card title from `<span>` to `<h2>`, adding `margin: 0` to each existing inline style object so the browser's default h2 margin does not shift the layout. Screenshot before/after at desktop and 390px to confirm pixel parity.

### 15. [minor · small] hq-site: The contact form's small print says 'nothing is passed on to anyone', but the studio's own automation sends each message to Claude to draft a reply ('Claude wrote this for you to check'). The privacy policy also says nothing about the website form or the Vercel/Supabase hosting it uses.

- **Evidence:** hq/src/app/support/ContactForm.tsx:126-128 says 'Nothing else, and nothing is passed on to anyone.' AGENTS.md standing policy has an agent read each message and write a draft reply, so the clause is false. The second half is overstated: hq/src/app/privacy/page.tsx:36 does say 'stored in a Supabase database and served through Vercel, both in the United States' — what it omits is the website contact form itself and the AI drafting.

- **Do this:** Narrow, non-editorial fix: in /home/user/Dice-battles-/hq/src/app/support/ContactForm.tsx line 127-128, delete the false clause so it reads '...are stored so we can read and reply. Nothing else.' Removing an untrue sentence needs no product decision. Do NOT invent new disclosure copy here — the positive statement about AI-drafted replies belongs in the privacy-policy rewrite that blocker items 0 and 1 already force, and that wording is David's call.

### 16. [minor · small] hq-site: The Automation page asserts the hourly Bug watch was 'Confirmed working on 19 August 2026' while STATE.md (30 Aug) lists it as unproven; it describes 'Support drafts — When a message arrives' although nothing in hq/ creates `is_draft` rows; and its history footer says 'The last 40' while the query fetches 20. AGENTS.md asks this page to say plainly when something may not be running.

- **Evidence:** hq/src/app/admin/automation/page.tsx:51 carries the 'Confirmed working on 19 August 2026' caveat; STATE.md:158 says 'The hourly bug-watch routine is unproven — created, but whether it...'. page.tsx:85 is `.limit(20)` while page.tsx:295 renders 'The last 40.' The is_draft half is overstated: `grep is_draft` shows hq/src/app/admin/actions.ts only ever inserts `is_draft: false`, but the Support-drafts automation is an agent standing policy in AGENTS.md, not hq code, so the page is describing something real that simply is not implemented in this repo.

- **Do this:** Two edits in /home/user/Dice-battles-/hq/src/app/admin/automation/page.tsx. (1) Line 295: change 'The last 40.' to 'The last 20.' to match `.limit(20)` at line 85 (or raise the limit to 40 — either is fine, they just have to agree). (2) Line 51: replace the 'Confirmed working on 19 August 2026 — the first automatic run reached the board and the email account it needs.' caveat with the truth STATE.md:158 records, e.g. 'Set up on 19 August 2026, but not yet seen to run on its own — if nothing appears below, assume it has not.' AGENTS.md requires this page to say plainly when something may not be running, so this is a correction, not a rewrite. Leave the Support-drafts entry alone.

### 17. [minor · small] hq-site: The People page's example email placeholder 'marcsutton2010@gmail.com' looks like a child's real address and sits in a public repository.

- **Evidence:** hq/src/app/admin/people/page.tsx:102 `placeholder="marcsutton2010@gmail.com"` — a plausible real address for David's son in a repo AGENTS.md flags as PUBLIC. (Map cites line 403; the real line is 102.)

- **Do this:** In /home/user/Dice-battles-/hq/src/app/admin/people/page.tsx line 102 change the placeholder to `name@example.com`. Then `grep -rn 'sutton.*@gmail\|@gmail\.com' /home/user/Dice-battles-/hq/src /home/user/Dice-battles-/src` to confirm no other family address is hard-coded in shipped source. Note the address stays in git history — if it is Marc's real address, that is worth telling David, since only he can decide whether it matters enough to rewrite history.

### 18. [minor · small] hq-site: Two unauthenticated write paths can be flooded: the contact form's brake is keyed on email only (blank-email submissions are unlimited), and /api/bug-report inserts *pre-approved* 'bug' ideas — text that the automated bug-watch then reads as work — with only a global 20/minute cap and no origin check.

- **Evidence:** hq/src/app/support/actions.ts:53 `if (email) {` guards the whole 5-per-hour count with no else branch, and email is optional (validated only `if (email && !looksLikeEmail(email))` at line 45). hq/src/app/api/bug-report/route.ts:44-56 counts ALL kind='bug' ideas in the last minute globally and caps at 20, then inserts `status: 'approved', kind: 'bug'` (line 62-70) for any body ≥5 chars — the global cap also means a genuine burst of real reports locks out real reporters.

- **Do this:** Fix only the unambiguous half now: in /home/user/Dice-battles-/hq/src/app/support/actions.ts add an `else` to the `if (email)` at line 53 that applies the same 5-per-hour cap to blank-email submissions, counting with `.is('email', null)` (or `.or('email.is.null,email.eq.')` if the column is written as an empty string — check the insert at line 69-73 and match it), returning the same message. Do NOT change /home/user/Dice-battles-/hq/src/app/api/bug-report/route.ts in this pass: the endpoint must stay open to every player's phone, so hardening it means choosing a mechanism (a shared secret baked into the game build, a per-device key, or leaving it open) — raise that to David rather than picking one.

### 19. [minor · small] hq-site: `getSiteContent` calls `supabaseAdmin()`, which throws if `SUPABASE_SERVICE_ROLE_KEY` or the URL is unset, and nothing catches it — so a missing env var takes every public page to a 500 rather than the code fallbacks. The middleware comment promises the opposite ('let pages render their own "set this up first" message rather than crashing the whole site').

- **Evidence:** hq/src/lib/content.ts:14-21 — `supabaseAdmin()` is called on line 15 outside any try, and the only guard is `if (error || !data) return {}` on line 19, which is after construction. hq/src/lib/supabase/server.ts:54-60 `supabaseAdmin()` calls `required()` (lines 20-28) which throws `Missing ${name}`. Every public page is force-dynamic and calls getSiteContent, so one unset env var is a site-wide 500 instead of the code fallbacks that already exist for every string.

- **Do this:** In /home/user/Dice-battles-/hq/src/lib/content.ts wrap the whole body of `getSiteContent` (lines 15-20) in `try { ... } catch { return {}; }`. The fallbacks are already threaded through every caller via `text()`/`faqList()`, so returning `{}` renders the full site from code. Verify by running `SUPABASE_SERVICE_ROLE_KEY= npm run dev` in /home/user/Dice-battles-/hq and loading / — it should render the code copy, not a 500.

### 20. [minor · small] hq-site: Next 16 reports the `middleware` file convention as deprecated (use `proxy`) and warns about two lockfiles picking the wrong workspace root; hq/README.md still documents the board at `/hq`; login/password pages are branded 'Dice Battles HQ' while the admin header says 'Paper Ship Studio Admin'; /login is not noindexed though /admin and /password are.

- **Evidence:** hq/README.md:7 'The private HQ at `/hq`' and :31 'The board at `/hq`, invitation only' — the route moved to /admin on 18 Aug. hq/src/app/login/page.tsx:73 `<h1>Dice Battles HQ</h1>` and hq/src/app/password/page.tsx:7 title 'Choose a password — Dice Battles HQ', against admin/layout.tsx:8 which exports `robots: { index: false, follow: false }` (as does password/page.tsx:8) while login/page.tsx, being 'use client', exports none. Both /home/user/Dice-battles-/package-lock.json and /home/user/Dice-battles-/hq/package-lock.json exist.

- **Do this:** Take the cheap half only. (1) In /home/user/Dice-battles-/hq/README.md lines 7 and 31 change `/hq` to `/admin`. (2) In /home/user/Dice-battles-/hq/src/app/login/page.tsx line 73 change the h1 to 'Paper Ship Studio' and in /home/user/Dice-battles-/hq/src/app/password/page.tsx line 7 change the title to 'Choose a password — Paper Ship Studio'. (3) /login noindex is handled by the item-14 next.config.ts change — do not duplicate it. EXPLICITLY DO NOT rename src/middleware.ts to proxy.ts before launch: it is a framework migration on the file that gates every admin route, the deprecation is a dev-log warning with no user-visible effect, and Next 16 still runs the old convention. The two lockfiles are correct — hq/ is a separate app — so silence the warning later with `turbopack.root` in next.config.ts rather than deleting either lockfile.

### 21. [minor · medium] hq-site: Unbranded default Next.js 404 (with a `prefers-color-scheme: dark` style, on a no-dark-mode brand), no robots.txt, no sitemap, no Open Graph/metadataBase — a shared papershipstudio.com link shows no preview image and a mistyped URL shows a bare '404: This page could not be found.'

- **Evidence:** `ls hq/src/app` shows no not-found.tsx, no robots.ts, no sitemap.ts; hq/public/ holds only app-ads.txt, icons/ and images/. hq/src/app/layout.tsx:4-8 exports `metadata` with title and description only — no metadataBase, no openGraph, no twitter. Usable OG source images already exist at hq/public/images/dice-battles-icon.png and game-screenshot-1.jpeg.

- **Do this:** Four small files, all in /home/user/Dice-battles-/hq. (1) Create src/app/not-found.tsx rendering `<SitePage active="none">` (src/components/site/SitePage.tsx) with a short 'We could not find that page' and a link home, so the 404 carries the brand and the light-only palette. (2) Create src/app/robots.ts exporting `{ rules: { userAgent: '*', allow: '/', disallow: ['/admin', '/login', '/password', '/api'] }, sitemap: 'https://papershipstudio.com/sitemap.xml' }`. (3) Create src/app/sitemap.ts listing the five public routes: /, /apps, /apps/dice-battles-color-rush, /support, /privacy, /terms. (4) In src/app/layout.tsx add `metadataBase: new URL('https://papershipstudio.com')` and an `openGraph` block (siteName 'Paper Ship Studio', the existing title/description, images: ['/images/dice-battles-icon.png']) plus `twitter: { card: 'summary_large_image' }`. Do NOT commission a bespoke OG image — reusing the existing icon needs no decision; a designed share card is a separate ask for David.

### 22. [minor · small] integrations: The bug-report fetch has no timeout, unlike the friends and news calls; on a captive portal the Send spinner runs until the app is killed.

- **Evidence:** src/debug/bugReport.ts:53-58 is a bare `await fetch(ENDPOINT, { method: 'POST', headers, body })` — no AbortController, no signal, nothing in the file constructs one. src/game/friendsApi.ts:37-63 does it properly (controller + 8000 ms timer + clearTimeout in finally) and its own header comment at :12-14 says 'a fetch with no timeout on a captive-portal wifi hangs until the app is killed'. src/debug/BugReportModal.tsx:124-129 renders an ActivityIndicator inside the Send button while `sending` is true and the button is inert (`onPress={sending ? undefined : send}`), so the spinner is the only state the player gets. The modal's Close button still works, so the app is not stuck — the send is.

- **Do this:** In /home/user/Dice-battles-/src/debug/bugReport.ts sendBugReport (lines 47-77), copy the pattern from src/game/friendsApi.ts:37-63: before the try add `const controller = typeof AbortController === 'function' ? new AbortController() : null;` and `const timer = setTimeout(() => controller?.abort(), 8000);`, pass `signal: controller?.signal` in the fetch options at :54, and `clearTimeout(timer)` in a `finally`. An abort throws into the existing catch at :65, which already returns 'Could not reach the server. Check your connection and try again.', so no UI, copy or test change is needed. Verify with `npx tsc --noEmit` and `npm test`.

### 23. [minor · small] integrations: Profile 'wins'/'modeWins' are stored as whatever object a client sends (no shape or size validation) and rendered straight into <Text> on a friend's phone; a crafted value (e.g. wins.easy = {}) throws 'Objects are not valid as a React child', which the crash guard turns into the CrashScreen. Unbounded JSON per profile is also stored.

- **Evidence:** hq/src/app/api/players/route.ts:47-48 — every other field in cleanProfile goes through `int()` or `id()`, but `wins: typeof body.wins === 'object' && body.wins ? body.wins : {}` and the same for mode_wins keep the object as sent (arrays and nested objects included), and cleanProfile feeds both the insert (:142) and the update (:162). hq/src/app/api/friends/route.ts:86-97 `full()` returns `wins: p.wins` untouched to a confirmed friend. src/demo/FriendsScreen.tsx:440-448 passes `profile.wins?.easy ?? 0` into Stat, whose body at :475-479 is `<Text …>{text ?? value ?? 0}</Text>` — an object there is a React render throw, which App.tsx:49/97 converts into the CrashScreen with no restart control. Needs an accepted friend running a modified client, so it is narrow, not theoretical.

- **Do this:** In /home/user/Dice-battles-/hq/src/app/api/players/route.ts cleanProfile (lines 40-55), add a sanitiser beside the existing `int`/`id` helpers — `const counts = (v: unknown) => { const out: Record<string, number> = {}; if (v && typeof v === 'object' && !Array.isArray(v)) for (const [k, n] of Object.entries(v as Record<string, unknown>).slice(0, 20)) if (/^[a-z0-9_-]{1,20}$/i.test(k)) out[k] = int(n, 10_000_000); return out; };` — then replace lines 47-48 with `wins: counts(body.wins), mode_wins: counts(body.modeWins),`. That covers insert and update in one place. Belt and braces on the phone: in /home/user/Dice-battles-/src/demo/FriendsScreen.tsx:475-479 render `{text ?? (Number.isFinite(Number(value)) ? Number(value) : 0)}`. Nothing in tests/friends.test.ts reads these lines (it only asserts secret_hash and no-secret-select on this file, :356-374). Ship with `npx vercel deploy --prod` from hq/.

### 24. [minor · small] integrations: Every render of DiceDemoScreen while Friends is open re-POSTs the profile, re-GETs the list and flips the page back to its spinner, because `stats` is an inline object literal and refresh/effect are keyed on it.

- **Evidence:** The mechanism is exactly as described: src/demo/DiceDemoScreen.tsx:1730-1741 builds `stats={{ … }}` inline in JSX (new identity every render), FriendsScreen.tsx:88-94 closes refresh over it with `}, [me, stats])`, :96-98 runs `useEffect(() => { void refresh(); }, [refresh])`, and refresh starts with `setLoading(true)` (:75) then `await pushProfile` + `fetchFriends`. What the map overstates is 'every render': the overlay only renders in the menu (`phase === 'pick'`, menuTab 'leaderboard'), the one interval in the file is gated on `phase !== 'battle'` (:796-799), and taps while the overlay is up go to the child — so the parent re-renders rarely here and the spinner flash and duplicate POST are occasional, not continuous. Still a real double round trip and a real last_played write (players/route.ts:53) per parent render.

- **Do this:** In /home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx, hoist the object out of the JSX at :1730-1741 into a memo declared with the other hooks (near the `showFriends` state at :229-319): `const friendStats = useMemo(() => ({ trophies, wins, modeWins, diceOwned: DICE_SKINS.filter((s) => isSkinUnlocked(s.id, trophies)).length, arenasOwned: ARENA_ORDER.filter((id) => isArenaUnlocked(id, trophies)).length, favouriteDie: loadout.skinId, favouriteArena: loadout.arenaId }), [trophies, wins, modeWins, loadout.skinId, loadout.arenaId]);` and pass `stats={friendStats}`. Leave FriendsScreen's `[me, stats]` deps alone — they are correct once the prop is stable. Confirm `useMemo` is in the React import at the top of the file, then `npx tsc --noEmit` and `npm test`.

### 25. [minor · small] integrations: Identity is cached for the whole session, so signing into Game Center mid-session does not switch identity until relaunch (the comment says it should); and the Apple alias is persisted to the local name key, so after sign-out the local profile publishes the Game Center alias under the local id.

- **Evidence:** src/game/playerIdentity.ts:95 `if (cached) return cached;` against the comment at :114-119 that 'a player who opened the game on a plane and signed in later should become their real self'. :134 `const name = apple?.name || storedName || ANONYMOUS_NAME;` and :146 `if (storedName !== name) writes.push(AsyncStorage.setItem(NAME_KEY, name));` — so Apple's alias is written to the LOCAL name key, and on the next launch signed out, apple is null and storedName (the alias) is published under the local id by FriendsScreen's pushProfile. NAME_KEY is written nowhere else (`grep -rn NAME_KEY src tests` → only :105 and :146), so the fix is contained to one line.

- **Do this:** In /home/user/Dice-battles-/src/game/playerIdentity.ts:146 write the name only when it did not come from Apple: `if (!apple && storedName !== name) writes.push(AsyncStorage.setItem(NAME_KEY, name));`. The signed-in name is still read from `apple.name` every launch at :134, so nothing visible changes while signed in; signing out now returns the local profile to its own name instead of the Apple alias. Do this in the same pass as the major friend-code 409 fix, which is in this file. Do NOT touch the session cache at :95 here — mid-session identity switching changes which playerId pushProfile claims and belongs with that same 409 work; instead correct the comment at :114-119 to say identity is resolved once per launch. tests/friends.test.ts:248 (default name, no Game Center in the headless suite) is unaffected; re-run `npm test`.

### 26. [minor · small] menus: The home heading is plain 'Dice Battles' while the rule is one name everywhere ('Dice Battles: Color Rush'), which the splash card obeys.

- **Evidence:** /home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx:1543 `<Text style={styles.overlayTitle}>Dice Battles</Text>` (map said :1490 — line numbers shifted after commit 806a124). BootSplash.tsx:61-62 renders 'DICE BATTLES' + 'COLOR RUSH' and its own comment at :20 says 'The name is the full "Dice Battles: Color Rush" — David asked for the'. app.json:3 is 'Dice Battles: Color Rush'. So the launch card and the store record both carry the full name and only the home screen still shows the short one — exactly the split AGENTS.md says David killed on 19 Aug 2026 and 'do not reintroduce'. No test asserts this string (grep over tests/ for 'Dice Battles' returns nothing).

- **Do this:** In /home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx line 1543, replace `<Text style={styles.overlayTitle}>Dice Battles</Text>` with `<Text style={styles.overlayTitle}>Dice Battles{'\n'}Color Rush</Text>`. Use the explicit newline rather than one long string: overlayTitle is 34pt/weight 900 (styles at :2418-2424) and 'Dice Battles: Color Rush' on one line is roughly 470pt wide, so it would wrap unpredictably on a 320pt iPhone SE. Two forced lines are ~256pt each and fit. The extra ~40pt of height is absorbed — pickScroll uses flexGrow:1 + justifyContent:'center' and scrollEnabled={pickOverflows} (:1535-1541), so it re-centres when it fits and scrolls when it does not. Do not touch the other three uses of overlayTitle (:1615 'Victory!', :1634 'It's a tie!', :1953 'Defeat!'). Then run `npx tsc --noEmit` and `npm test`.

### 27. [minor · small] menus: The home pick screen pads only 64pt from the top while the HUD pills and top buttons occupy y52–90, unlike every other page (paddingTop 100); on a small phone where the content overflows and scrolling switches on, the 34pt title starts under the coin pill.

- **Evidence:** The numbers check out: DiceDemoScreen.tsx:2285 `paddingTop: 64` in pickScroll, against paddingTop: 100 in StoreScreen.tsx:210, InventoryScreen.tsx:229, TournamentScreen.tsx:166, LeaderboardScreen.tsx:315 and FriendsScreen.tsx:384; StatsHud.tsx:48 top:52 with STATS_HUD_HEIGHT 40, TopButtons.tsx:63 top:52 with 38pt buttons. The consequence is narrower than stated, though: pickScroll is `flexGrow:1, alignItems:'center', justifyContent:'center'` (:2281-2288), so when the content fits — the normal case — it is vertically centred and the 64 is only a floor, never the actual offset. The collision needs both overflow (iPhone SE) and the player not having scrolled, and it is recoverable because scrollEnabled turns on in exactly that case.

- **Do this:** In /home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx line 2285, change `paddingTop: 64,` to `paddingTop: 100,` so the pick screen matches every other page. This is a no-op when the content fits (justifyContent:'center' with flexGrow:1 ignores the floor) and pushes the title clear of the y52–90 pill row when it overflows, which is the only case that was wrong. Add a short comment saying the 100 matches the menu pages and clears the HUD row. No test locks this value — tests/layout.test.ts:200 and :254 cover InventoryScreen/StoreScreen/LeaderboardScreen only. Run `npx tsc --noEmit` and `npm test`.

### 28. [minor · small] menus: Almost no Pressable carries an accessibilityLabel or role: the five tabs, every Store/Items card, the mode and difficulty chips, Start battle, the ↺ quit and the split-screen ✕ are unlabelled, so VoiceOver reads them as bare buttons or icons.

- **Evidence:** grep for accessibilityLabel|accessibilityRole|accessibilityState across src/demo and src/ui returns exactly four hits: TopButtons.tsx:45, Popup.tsx:42, Card.tsx:64 and Card.tsx:105. There are ~80 Pressables across src/demo (21 in DiceDemoScreen alone, 9 in TwoPlayerScreen, 7 each in ItemPreviewBar and LeaderboardScreen). The 'reads as bare buttons' half is slightly overstated — VoiceOver announces the Text inside a Pressable, so text buttons like 'Start battle' are not silent — but every icon-only control genuinely is: DiceDemoScreen.tsx:1474-1475 the ↺ quit, TwoPlayerScreen.tsx:426-428 the ✕ exit, FriendsScreen.tsx:295 the '›' row chevron.

- **Do this:** Add accessibility props only — they change no layout and cannot regress the visual pass. (1) /home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx:1474, add `accessibilityRole="button" accessibilityLabel="Quit to the menu"` to the quitButton Pressable. (2) /home/user/Dice-battles-/src/demo/TwoPlayerScreen.tsx:426, add `accessibilityRole="button" accessibilityLabel="Leave split screen"` to the exitButton Pressable. (3) /home/user/Dice-battles-/src/demo/BottomNav.tsx:59-67, add `accessibilityRole="tab"`, `accessibilityLabel={tab.label}` and `accessibilityState={{ selected: on }}` to the tab Pressable — `on` is already in scope at :57. (4) /home/user/Dice-battles-/src/demo/FriendsScreen.tsx:160 and :323, add `accessibilityRole="button"` with labels 'Back to Ranks' and 'Back to Friends' to the two back Pressables. Leave the text-bearing buttons alone — VoiceOver already reads their contents, and blanket-labelling them risks a label drifting out of step with the visible words. Run `npx tsc --noEmit` and `npm test`.

### 29. [minor · small] menus: After 'Ask to be friends' nothing confirms the request was sent; the found card just disappears and the name only appears under 'WAITING FOR AN ANSWER' after the refresh.

- **Evidence:** FriendsScreen.tsx:128-138 `const act = async (otherId, action) => { const result = await actOnFriend(...); if (!result.ok) { setProblem(result.error); return; } setFound(null); setCode(''); setShowing(null); await refresh(); }` — the success path sets no message, and setFound(null) removes the card the player just tapped. Feedback does exist one section down (the name appears under 'WAITING FOR AN ANSWER' at :247 after refresh), so it is unannounced rather than silent, but nothing points the player at it. The machinery for a message is already there and already rendered: searchNote state at :70, drawn at :215 inside the same ADD A FRIEND card.

- **Do this:** In /home/user/Dice-battles-/src/demo/FriendsScreen.tsx, in `act` (line 128), replace the success block `setFound(null); setCode(''); setShowing(null); await refresh();` with a version that sets the existing note first: `setSearchNote(action === 'request' ? 'Asked! They will see it next time they play.' : null); setFound(null); setCode(''); setShowing(null); await refresh();`. This reuses searchNote, which is already declared at :70 and already rendered at :215 with styles.note inside the ADD A FRIEND card, so nothing new is laid out. Clearing it to null for the other actions (accept/remove/block) keeps a stale message from surviving into an unrelated action — and check that `search` at :120-125 still sets its own note first, which it does. Run `npx tsc --noEmit` and `npm test` (tests/friends.test.ts covers the pure state machine and is untouched by this).

### 30. [minor · small] menus: The stakes line prints a minus sign followed by an en-dash range, which reads as arithmetic: 'Lose −3–8 trophies'.

- **Evidence:** /home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx:1298 `Win +{rangeLabel(stakes.win)} · Lose −{rangeLabel(stakes.loss)} trophies` (map said :1244), with src/game/rewards.ts:39-43 returning `${lo}–${hi}` — a real en-dash. So the rendered string is 'Lose −3–8 trophies': a U+2212 minus immediately followed by a U+2013 range dash. It sits on the home screen directly under the difficulty picker, which is the line telling a player what a loss costs.

- **Do this:** In /home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx line 1298, delete the U+2212 minus so the line reads `Win +{rangeLabel(stakes.win)} · Lose {rangeLabel(stakes.loss)} trophies`. The word 'Lose' already carries the sign, so the minus is redundant, and no reading of 'Lose −3–8' is clearer than 'Lose 3–8'. Keep the '+' on the win side — a plus before a range still reads as a gain, and dropping both would make the two halves distinguishable only by the words. Run `npx tsc --noEmit` and `npm test`.

### 31. [minor · small] store-listing: Keyword field wastes budget: 'color' and 'colour' both appear although 'Color' is already in the indexed app name (contradicting store/README.md's own rule), 'match' duplicates the subtitle, and '2 player' / 'split screen' spend characters on spaces; 'roll' and 'trophy' are low-intent.

- **Evidence:** store/keywords.txt is `colour,color,match,family,board,2 player,split screen,colorblind,offline,party,roll,trophy` (90/100). app.json:3 name is "Dice Battles: Color Rush", so "color" is indexed twice; store/README.md:15 states the rule it breaks. store/subtitle.txt is "Match the colors, free them", and Apple indexes the subtitle, so "match" is a third duplicate. Apple recombines single terms across the list, so the spaces in "2 player" and "split screen" buy nothing.

- **Do this:** Edit /home/user/Dice-battles-/store/keywords.txt to exactly `colour,match,family,board,2,player,split,screen,colorblind,offline,party,roll,trophy` (84/100 — mechanical only: drops the name-duplicate "color", splits the two spaced phrases; Apple still indexes "2 player" and "split screen" as combinations). Do NOT invent replacement terms to spend the freed 16 characters — which words to add is Marc/David's call, and "match" is left in place deliberately rather than second-guessed. Then push with `python3 tools/appstoreconnect/asc.py set-listing --apply` (needs ASC_ISSUER_ID in the environment; it re-checks the 100-char limit at tools/appstoreconnect/asc.py:326-343). Do this before submission — the keyword field is frozen until the next release goes through review.

### 32. [minor · small] tests-tooling: Two generated esbuild bundles are not gitignored. The READMEs instruct building tools/screen-preview/bundle.js and tools/store-shots/bundle.js, but .gitignore covers only the arena-preview and icon-preview bundles; a `git add -A` after using either tool commits a multi-MB bundle (with inlined data-URI assets) to a public repo.

- **Evidence:** Verified and currently LIVE, not hypothetical. `.gitignore` ends with exactly two tool lines: `tools/arena-preview/bundle.js` and `tools/icon-preview/bundle.js`. `git status --porcelain` right now shows `?? tools/screen-preview/bundle.js`, and `ls -la` puts it at 10,982,459 bytes (10.9MB). `git check-ignore -v tools/screen-preview/bundle.js tools/store-shots/bundle.js` exits 1 — neither is ignored. The READMEs do instruct building both (`tools/screen-preview/README.md:10` and `tools/store-shots/README.md:32` `--outfile=`), and store-shots uses `--loader:.png=dataurl` so its bundle inlines assets. One `git add -A` in this working tree commits 10.9MB of generated code to a public repo that AGENTS.md says must stay clean.

- **Do this:** Edit /home/user/Dice-battles-/.gitignore. Find the two existing lines `tools/arena-preview/bundle.js` and `tools/icon-preview/bundle.js` and replace both with a single line `tools/*/bundle.js` so all four preview harnesses are covered by one rule that cannot drift again when a fifth tool is added. Then confirm with `git check-ignore -v tools/screen-preview/bundle.js tools/store-shots/bundle.js tools/arena-preview/bundle.js tools/icon-preview/bundle.js` (all four must print a match) and `git status --porcelain | grep bundle.js` (must print nothing). Do NOT `git rm` anything — none of the four bundles is tracked, so this only prevents a future accidental add.

### 33. [minor · small] tests-tooling: 'the iOS build config is complete enough to submit' asserts almost nothing: the bundle identifier contains a dot and the production EAS channel is 'main'. No buildNumber, no infoPlist keys, no encryption-exemption flag, no icon/splash pairing beyond existence.

- **Evidence:** tests/screen.test.ts:568-582 verified verbatim: the whole body is a bundleIdentifier `.includes('.')` check, an `existsSync('eas.json')`, and `eas.build?.production?.channel === 'main'`. Meanwhile app.json already carries the submission-critical fields the test ignores — `ios.infoPlist.ITSAppUsesNonExemptEncryption: false` (app.json:19-21) and `ios.entitlements['com.apple.developer.game-center']: true` (app.json:22-24). Losing either silently reintroduces Apple's export-compliance question on every submission or breaks Game Center, and nothing would say so. Worth doing now specifically because the ads/StoreKit native build will edit app.json before launch. Note: buildNumber is legitimately absent (EAS remote versioning owns it) — do not assert it.

- **Do this:** In /home/user/Dice-battles-/tests/screen.test.ts, inside the existing test at line 568 ('the iOS build config is complete enough to submit'), after the existing eas.json channel assert, add two asserts against the already-parsed `ios` object: (1) `assert(ios.infoPlist?.ITSAppUsesNonExemptEncryption === false, 'app.json lost ITSAppUsesNonExemptEncryption: false — every submission will stop and ask the export-compliance question');` (2) `assert(ios.entitlements?.['com.apple.developer.game-center'] === true, 'the Game Center entitlement is gone from app.json — Friends and leaderboards need it');`. Both pass against app.json as it stands, so `npm test` must stay green — run it to confirm. Do NOT add an assert comparing app.json `version` (1.60.0) to GAME_VERSION (v1.64.0), and do NOT add an assert that the AdMob androidAppId differs from iosAppId: both would fail today and both are open decisions, not regressions.

### 34. [cosmetic · small] arenas-dice: patterns.ts documentation has drifted from the code: three separate comments still say 'eight' full-colour painters (there are 34), five painters carry the doc comment of a different painter, and two mask painters ('spots', 'grain') are dead — no skin uses them.

- **Evidence:** Every claim checks out except the count. src/dice/patterns.ts:245 'The eight patterns painted in full colour rather than through a mask.', :890 'The eight patterns that need MORE than base-plus-ink.', :2189 'The eight full-colour patterns paint their own pixels outright.', and tests/screen.test.ts:256 'Eight of the skins are painted by full-colour painters'. The ColorPatternId union at :245-286 actually lists 32 ids (not 34 — counted by `| 'name'` entries). Mislabels confirmed: :423 `grain` sits under the bubbles doc comment ('identical circles reads as polka dots', 'glint', 'rim'); :726 `giraffe` labelled 'Tiger: vertical stripes'; :763 `tennis` labelled 'Basketball: the classic four seams'; :818 `citrus` labelled 'Strawberry: seeds'; :901 `watermelon` labelled 'White leather in curved panels' (volleyball). Dead painters confirmed: `spots` (:293) and `grain` (:423) appear nowhere in DICE_SKINS — every other painter id in src/game/diceSkins.ts resolves.

- **Do this:** Comments only — zero runtime risk, no behaviour change. In src/dice/patterns.ts replace 'eight' with 'thirty-two' at :245, :890 and :2189, and in tests/screen.test.ts:256 replace 'Eight of the skins' with 'Thirty-two of the skins' (confirm the number first with a throwaway `Object.keys(COLOR_PAINTERS).length`; it must match the ColorPatternId union). Then rewrite the five wrong doc comments to describe the painter they actually sit above: :422-424 for `grain` (it is a waving sine ridge, not bubbles — the bubbles comment belongs to the `bubbles` painter), :726 giraffe (blotches, not tiger stripes), :760-762 tennis (two curved seams, not basketball's four), :818 citrus (corner slices, not strawberry seeds), :901 watermelon (rind and flesh, not volleyball's leather panels). Do NOT delete the dead `spots` and `grain` painters in the same pass — that touches the PatternId union at :16 and :18 and could ripple into tests/textures.test.ts; leave a one-line `// Unused by any skin as of Sep 2026.` above each instead. Run `npx tsc --noEmit` and `npm test` afterwards to confirm nothing moved.

### 35. [cosmetic · small] arenas-dice: The texture cache keys break the cache's own contract: ThemedArena keys floors on arena id only while the useMemo depends on five theme colours, so a theme edit at runtime (the arena-preview tool, a future live-tuning screen) would be served the previous floor.

- **Evidence:** src/arena/ThemedArena.tsx:1910 keys on `themed-floor-${id}` while the useMemo dep array at :1918 is [id, theme.structure, theme.floor.a, theme.floor.b, theme.wall.cap, floorW, floorD]; :1920 keys on `themed-ground-${id}` with deps [id, theme.structure, theme.meadow, theme.hill, theme.mountain, theme.wall.cap] at :1930. src/arena/textureCache.ts:29-31 states the contract in the same folder: 'The key must describe everything `make` depends on, or two different textures will collide on one entry — hence the callers passing their colours and settings into it rather than a bare name.' No user-visible effect today because ARENA_THEMES is a module constant.

- **Do this:** Two one-line edits in src/arena/ThemedArena.tsx, both with the values already in scope in the adjacent dep arrays. At :1910 change `themed-floor-${id}` to `themed-floor-${id}-${theme.structure}-${theme.floor.a}-${theme.floor.b}-${theme.wall.cap}-${floorW}x${floorD}`. At :1920 change `themed-ground-${id}` to `themed-ground-${id}-${theme.structure}-${theme.meadow}-${theme.hill}-${theme.mountain ?? theme.wall.cap}`. Behaviour is identical today (themes are module constants, so keys stay one-to-one with arenas and nothing repaints); it only stops a stale floor being served if a theme is ever edited at runtime. Run `npx tsc --noEmit` and `npm test` — tests/arenaShape.test.ts reads cachedTextureCount(), so confirm the count assertions still hold.

### 36. [cosmetic · small] hq-api: The players GET documents a ?playerId= self-read that is not implemented; anything other than ?code= returns 400.

- **Evidence:** hq/src/app/api/players/route.ts:85 reads `/** GET ?code=K7M29XPQ — find somebody, or ?playerId=… — read your own. */`, but the handler only ever reads `searchParams.get('code')` (line 87) and every other request falls to line 109 `return NextResponse.json({ error: 'pass a code' }, { status: 400 })`. There is no playerId branch.

- **Do this:** In /home/user/Dice-battles-/hq/src/app/api/players/route.ts line 85, change the doc comment to `/** GET ?code=K7M29XPQ — find somebody by their friend code. */`. Bundle this with the error-handling edit to the same file rather than shipping it as its own change; do not add the missing ?playerId= endpoint (that is a new API surface, not a comment fix). Verify with `cd hq && npx tsc --noEmit`.

### 37. [cosmetic · small] hq-site: Admin vote page is still styled for the old dark theme: the tally-bar track is `rgba(255,255,255,0.14)` on a light card (invisible) and 'your pick'/open state uses #ffe521 yellow on near-white. The news list references CSS variables `--go` and `--rule` that globals.css does not define.

- **Evidence:** hq/src/app/admin/vote/[id]/page.tsx:141 `background: 'rgba(255,255,255,0.14)'` is the tally-bar track — white-on-white, so a 0%-share option shows no bar at all and there is no track to read a share against. #ffe521 appears at vote/[id]:100 (borderColor), :150 (bar fill) and vote/page.tsx:118 (status colour); globals.css:17 defines `--yellow: #f2c53d`, so the page is bypassing the theme token. The --go/--rule half is REFUTED: news/page.tsx:99 uses `var(--go, #2a9c55)` and `var(--rule, #c7c0dd)` — both have literal fallbacks, so nothing renders wrong.

- **Do this:** In /home/user/Dice-battles-/hq/src/app/admin/vote/[id]/page.tsx line 141 change the tally track from `'rgba(255,255,255,0.14)'` to `'var(--line, #ece9e5)'` (defined at globals.css:11). In the same file lines 100 and 150, and in /home/user/Dice-battles-/hq/src/app/admin/vote/page.tsx line 118, replace the hard-coded `'#ffe521'` with `'var(--yellow, #f2c53d)'` — the theme token, which is the darker amber the rest of the admin uses. Leave news/page.tsx alone; its var() calls carry fallbacks and render correctly.

### 38. [cosmetic · small] menus: Items shows the trophy count twice — once in its own header and once in the shared HUD drawn over it — contradicting the HUD's own rule that the Leaderboard omits it to avoid saying it twice.

- **Evidence:** Stronger than the map states: InventoryScreen.tsx:64-67 draws a TrophyIcon plus count in its header, while DiceDemoScreen.tsx:1733 renders StatsHud on every menu tab except leaderboard, so both are on screen ~50pt apart. Every sibling page has already been cleaned up — StoreScreen.tsx:48 carries the explicit comment '{/* No coin count here — the shared HUD shows it on every screen. */}' above a title-only header, and TournamentScreen.tsx:45-47 and LeaderboardScreen.tsx:119-121 are title-only too. Inventory is the last page that never got the treatment, and StatsHud.tsx:8-18 records why: the per-screen headers 'used to disagree'.

- **Do this:** In /home/user/Dice-battles-/src/demo/InventoryScreen.tsx delete lines 64-67 — the `<View style={styles.trophies}>` block containing `<TrophyIcon size={16} />` and `<Text style={styles.trophiesText}>{trophies}</Text>` — leaving the header as just `<Text style={styles.title}>Inventory</Text>`, and add the same one-line comment StoreScreen.tsx:48 uses. Do NOT remove the `trophies` prop (:32, :48) or the TrophyIcon import (:7): both are still needed — trophies drives the unlock checks at :92 and :147, and TrophyIcon is drawn on the locked-item price tags at :127 and :196. The now-unused `trophies` and `trophiesText` styles at :243-252 can go with it. Run `npx tsc --noEmit` and `npm test`.

### 39. [cosmetic · small] menus: News tells players to send ideas 'from the Settings screen', but Settings only offers 'Report a bug'.

- **Evidence:** src/demo/NewsScreen.tsx:70-73 'Got an idea, or found something broken? Tell us from the Settings screen — every message is read by a person.' The Settings popup has exactly one such control: DiceDemoScreen.tsx:1934 `<Text style={styles.bugReportButtonText}>Report a bug</Text>`, opening a modal titled '🐞 Report a Bug' asking 'What happened?' (BugReportModal.tsx:101-105). An idea has nowhere to go, so the app promises a channel it does not have.

- **Do this:** In /home/user/Dice-battles-/src/demo/NewsScreen.tsx lines 70-73, change the footer to match what Settings actually offers — e.g. 'Found something broken? Tap Report a bug in Settings — every message is read by a person.' Make the app honest about the channel it has rather than broadening the bug modal, which would mean new copy, a new field and a change to bugReportValidation.ts. No test asserts this string (grep over tests/ for 'Settings screen' returns nothing). Run `npx tsc --noEmit` and `npm test`.

### 40. [cosmetic · small] menus: Stale explanation and test name for the tab bar: the comment justifies no-font-scaling by 'Seven cells… "Settings" and "Battle"' for a five-cell bar that no longer holds Settings, and the test is called 'seven labels each hold one line'.

- **Evidence:** BottomNav.tsx:73-79 justifies `allowFontScaling={false}` with 'Seven cells across the narrowest iPhone leaves about 53pt each. "Settings" and "Battle" have to hold one line inside that'. TABS has five entries (:39-45) and :16-20 states outright that 'Settings and News are NOT here'. Worse, the same file contradicts itself 48 lines earlier — :25-27 correctly says 'Seven cells left about 53pt each on a small phone; five leave 75'. tests/layout.test.ts:89 is named 'seven labels each hold one line' while :101 asserts five. This is more than tidiness: the false premise is the stated justification for a decision flagged as a major accessibility problem elsewhere in this review, and at 75pt per cell for six-character labels the width pressure it cites may not exist at all.

- **Do this:** Two string edits, no behaviour change. (1) /home/user/Dice-battles-/src/demo/BottomNav.tsx:73-79 — rewrite the comment to the true numbers and labels: five cells leave about 75pt each on the narrowest iPhone, and the longest labels are 'Battle' and 'Store'. Leave `numberOfLines={1}` and `allowFontScaling={false}` exactly as they are; re-examining the font-scaling decision needs a device and belongs with the separate major accessibility item, not here. (2) /home/user/Dice-battles-/tests/layout.test.ts:89 — rename the test to 'five labels each hold one line, whatever the system text size' and fix its two-line comment to match; the assertions at :92-99 are correct and must not change. Run `npm test` to confirm the same tests still pass.

### 41. [cosmetic · small] tests-tooling: README under-reports the suite's cost by an order of magnitude, which matters for a gate people are asked to run before every publish.

- **Evidence:** README.md line 48 reads `npm test        # ~3s: physics simulation, rules, camera fit, assets`. Measured here just now: `time npm test` → `561 passed, 0 failed (21.1s)`, wall clock `real 0m23.273s`. So the claim is out by ~7x. (The map said 549 tests / 35s; the suite has since grown to 561 tests and runs faster on this machine — either way ~3s is wrong.) This sits in the README block that AGENTS.md's pre-submission gate points people at, so someone budgeting for the gate is told it is instant.

- **Do this:** Edit /home/user/Dice-battles-/README.md line 48: change `npm test        # ~3s: physics simulation, rules, camera fit, assets` to `npm test        # ~25s: physics simulation, rules, camera fit, assets`. Re-run `npm test` first and use the figure it prints if it differs materially from 21s. Change nothing else in that code block — the `npm run bundle` and `npm run check` lines are accurate.


## B. Graphics verdicts (85 renders judged; 30 need work)

- **arena `cove` — improve:** The floor looks like a brick wall made of chocolate rather than a ship's deck. The boards are short and stacked in a staggered running bond with strong dark seams, so you read "bricks" before you read "planks" — and Pirate Cove's whole idea is a ship. It is also the darkest of the warm floors, so it has the least air around the dice of any brown arena. Everything else here is excellent: the gold studs and corner bosses go the whole way round, and the stone surround with the gems and gold bars is lovely.
  - Fix: src/arena/groundTexture.ts, `shipHull` (~line 767). Rows are keyed off `y` with `plank = 15` and butt joints every 42 units, which on the 5.6 x 10.2 tray gives short bricks. Key the rows off `x` instead so boards run the tray's long axis, set `plank = 16` and raise the butt-joint period from 42 to 64 — both divide the 128-unit ground tile, so the outside ground also stops seaming. Optionally lift `cove.floor` in src/arena/themeData.ts (~line 662) from `#a8845c`/`#93714a` by roughly 8% so the deck reads sun-bleached.

- **arena `aurora` — improve:** The most washed-out picture in the set. The aurora's green and lilac curtains are painted at full strength across the play floor as well as outside it, so the board is a hazy grey-green-and-mauve smear instead of the pale snow the design intends, and it sits at almost the same value as the ground beyond the wall — the whole image reads flat and foggy next to its neighbours. The green wash is also the one floor tone that sits in the same family as the green die.
  - Fix: src/arena/groundTexture.ts, `station` (~line 343). The drift already dims on the tray (`tray ? 0.4 : 0.65`) but the three ribbons do not — they mix in at up to 0.60 green, 0.46 cyan and 0.24 magenta on the board as well as outside. Multiply all three ribbon mix strengths by `(tray ? 0.4 : 1)` — the same inside/outside split `mossStone` and `coralRim` already use — so the curtains stay full strength across the surrounding ground, where they carry the arena's name, and the board returns to the pale snow described in src/arena/themeData.ts (~line 356).

- **arena `reef` — improve:** The worst floor in the set for actually seeing your dice, and it is the one test that matters most. Coral heads in salmon, pink, gold, mint and lilac — the same colours and roughly the same size as the dice — are scattered right across the play area, not just hugging the walls, and a bright white caustic web is laid over the top of them. Six settled dice would have to be hunted for. The code says the middle was meant to open into plain rippled sand; it hasn't. The surround, the shells on the wall and the props are genuinely beautiful, so this is a floor fix, not a redesign.
  - Fix: src/arena/groundTexture.ts, `coralRim` (~line 838). `skip = 0.38 + smoothstep(11, 32, Math.min(x, 112 - x, y, 204 - y)) * 0.58` only reaches 0.96 more than 32 units in from the wall, and three small lattices (cells 21, 17, 13) still fire inside that 4%, so heads land across the whole board. Raise the base and widen the ramp — e.g. `0.66 + smoothstep(4, 40, ...) * 0.33` — and start the tray loop at `k = tray ? 2 : 0` so only the two largest head sizes survive inside the wall. Then drop the tray caustic on the painter's last line from `(tray ? 0.7 : 0.5)` to about `(tray ? 0.4 : 0.5)`.

- **arena `city` — improve:** It reads as a road or a car park, not a rooftop. A pale ribbed grey band runs the full width of the board and looks exactly like a zebra crossing, long light lines march across the ground outside like lane markings, and yellow stripes run down the edges like centre lines. The rooftop props — the towers, the lanterns — are fighting a floor that says "street". The floor is otherwise beautifully calm grey and would show the dice better than almost any other arena.
  - Fix: src/arena/groundTexture.ts, `parapet` (~line 906). The painter ignores its `tray` flag, so the galvanised duct (`y > 86 && y < 96`), the ochre hatch (`x 20-48, y 14-40`) and the painted safety line (`x 108-112`) are also drawn on the outside ground — which tiles every 128 units, and that repeat is what puts the long light bands and yellow lines across the whole surround. Take the flag (`parapet: (x, y, p, tray) =>`) and wrap those three blocks in `if (tray)`, leaving only felt seams, patches and grit outside. Then shorten the tray duct so it stops spanning the full width (e.g. `x > 16 && x < 78`) and cap its ends, because a full-width ribbed grey band under a near-top-down camera will always read as a crossing.

- **arena `glade` — improve:** The lawn inside the walls is painted in vertical stripes you cannot unsee, and it is the same family of green as the green die. A green die sitting on this floor is the hardest colour to pick out in any of the sixteen arenas.
  - Fix: src/arena/groundTexture.ts, mossStone tray branch. Line 666-667: the fine mow band `const fine = Math.sin(x * (Math.PI / 8)); c = mix(c, shade(c, 0.9), Math.max(0, fine) * 0.7)` is a hard half-wave darkening every 16 painter units, which is what reads as stripes — drop the 0.7 to about 0.25 and pass it through smoothstep(-0.3, 0.6, fine) so it grades instead of banding. Then src/arena/themeData.ts line 620, glade `floor: { a: '#74b183', b: '#549062' }` — lift both toward a paler, less saturated grass (about '#a3c9a5' / '#8bb18e') so the green die #33cc6b separates from the board by lightness as well as hue. Leave the dark dusk moss outside the walls alone; the three-value-band split described in that theme's comment is right, the tray band is just not light enough.

- **arena `autumn` — improve:** Leaves are strewn so thickly across the play floor that the red and orange dice sink into them — you end up hunting for your own colour. The leaf drift is lovely outside the walls; it is only a problem where the dice land.
  - Fix: src/arena/groundTexture.ts line 299, `logPile: (x, y, p)` — it ignores the fourth `tray` argument that snowFence, station, stalagmite, mossStone and coralRim all use. Take it (`(x, y, p, tray)`) and thin the litter on the board only: at line 311 make the skip `if (h < (tray ? 0.72 : 0.42)) continue`, and run one pass instead of two on the tray (`for (let pass = 0; pass < (tray ? 1 : 2); pass++)` at line 303). Ground density stays exactly as it is.

- **arena `cove` — improve:** Pirate Cove has no cove in it. The ground all around the hull is painted with the same plank grain and butt joints as the deck, so instead of a boat beached on a shore it reads as a wooden crate sitting on a warehouse floor. There is nothing anywhere in frame that says water or shore.
  - Fix: src/arena/groundTexture.ts line 767, `shipHull: (x, y, p)` — it ignores `tray`, so the sandy meadow colour #c9b382 gets run through the deck painter. Take the fourth argument and add a `!tray` branch that paints rippled sand over the same p.a/p.b: the `adobe` ripple at line 275 (`Math.sin((x * 0.9 + y * 0.35) * 0.42 + fbm(x, y, SIZE) * 5)`) is the closest match already in the file, with p.accent kept for shell and pebble flecks. Planks stay on the tray, which is correct — the hull is the board.

- **arena `aurora` — improve:** It is called Frozen Lights and the lights barely show. The board, the wall and the snow outside are all the same pale grey-blue, so the whole arena goes flat and washed out and the aurora reads as a smudge rather than curtains of light. The green wash also drifts across the play floor, right where the dice sit.
  - Fix: Two halves. src/arena/themeData.ts lines 362-363, aurora: floor #b6d4dc / #a2c3cd, wall #8fb2bf and meadow #a8cbd4 all sit in one narrow value band under a night rig — exactly the fault the glade's own comment warns about. Pull the outside down out of that band (meadow/hill to about '#6e93a0' / '#5d818e') and keep the floor pale, so the board reads as a lit clearing in a dark snowfield. Then src/arena/groundTexture.ts lines 377-379: the three ribbon mixes carry no `tray` factor, so the curtains wash over the play floor as strongly as the ground. Multiply all three strengths by `(tray ? 0.35 : 1.4)` — quiet under the dice, strong on the snow around them, which is where that painter's own note says the picture lives.

- **arena `reef` — redo:** The worst-looking arena of the sixteen, and it costs 2400 coins. Everything outside the walls is a solid carpet of muddy purple, olive and tan blobs that reads as mould rather than coral, and the water inside is covered in white spaghetti squiggles with shells and swirls scattered through it. Six bright dice land in visual noise. On a phone the surround looks like static, and none of those colours belong anywhere near the rest of the game.
  - Fix: src/arena/groundTexture.ts, `coralRim` at line 822 — both surfaces need re-tuning, not nudging. Outside: the flat `skip = 0.38` (line 838) run through four lattices (line 848) is what makes the carpet — raise the ground skip to about 0.72, drop the ground to two lattices, and mix the heads further into the water at line 872 (`c = mix(c, head, 0.9)` → about 0.55) so they read as coral seen through water rather than as stickers laid on top. Inside: the caustic web at line 900, `smoothstep(0.84, 0.97, web) * (tray ? 0.7 : 0.5)`, is the white spaghetti — take the tray factor to about 0.22 and narrow the ramp to smoothstep(0.9, 0.99, web) so it becomes a few thin glints; and raise the tray skip floor at line 838 from 0.38 so the band near the walls is as open as the middle already is. Re-render and look again before accepting it.

- **arena `cavern` — improve:** About a quarter of the big cut slabs on the floor are lit lilac — the same colour as the purple die, at ten times the size. A purple die sitting on one of those slabs vanishes into it.
  - Fix: src/arena/groundTexture.ts line 501, `stalagmite`. `base = mix(GEMS[...], p.b, 0.42)` gives about #966cbe, and the very next line, `shade(base, 0.8 + Math.max(0, -tilt) * 0.5)`, takes the lit half of a gem facet to roughly #c38cf7 — effectively the die's own #cc79fc. Push the gems further into the rock (`mix(GEMS[...], p.b, 0.68)`) and cap the tilt gain at 0.25 so no facet ends up brighter than the wall behind it. Keep the geode — the family chose the cut slabs twice, and the shape is not the problem. Only the lightest facets need to come down.

- **arena `city` — improve:** Rooftop City reads as a street, not a roof. A pale ribbed band runs straight across the middle of the play floor like a zebra crossing, and a yellow line repeats down the ground beside the arena like a road centre line — so you seem to be playing in traffic rather than standing on a roof. The band also crosses the floor exactly where the dice land.
  - Fix: src/arena/groundTexture.ts, `parapet` (line 906). The duct at line 929 (`if (y > 86 && y < 96)`) sits mid-board on a 204-unit-tall tray — move it to y 168-178 so it runs across the near end behind the play, and tighten its rib spacing from `x % 5` to `x % 3` so it stops reading as painted dashes. The yellow line at line 941 (`if (x > 108 && x < 112)`) falls at the tray's own edge, which is right, but the same painter tiles the ground every 128 units so it prints a road line across the open ground — give `parapet` the fourth `tray` argument the other painters take and gate that line to `tray` only.

- **arena `moon` — improve:** The yellow hazard stripe along the top of the walls stops about a fifth short of every corner, so all four walls have a bare grey gap at each end and the rim never closes. It is the same 'the decoration doesn't go all the way round the wall' fault that has been reported before.
  - Fix: src/arena/ThemedArena.tsx line 1336, the `airlock` crest. `<boxGeometry args={[w * 0.6, 0.04, d * 0.6]} />` shortens the stripe ALONG the run as well as narrowing it across the wall — the narrowing is right, the shortening is the bug. WALL_RUNS (line 1022) is ordered [left, right, near, far], so indices 0 and 1 are the long side walls: use `args={[i < 2 ? w * 0.6 : w, 0.04, i < 2 ? d : d * 0.6]}`. Narrow across the wall, full length along it, corner to corner. It is the only crest element in the file scaled below 0.86 of its run.

- **arena `toybox` — improve:** The wall is topped with 48 red, blue and yellow cubes in the dice's own colours and roughly the dice's own shape, so at a glance the rim looks like a row of dice and you have to look twice to find the real ones on the floor. It is the last arena a player unlocks, at 3350 coins, and it should be the clearest.
  - Fix: The crest at src/arena/ThemedArena.tsx line 1575 cycles `[theme.wall.color, cap, accent]`, which for toybox (src/arena/themeData.ts lines 819-820) is #c23b3b / #3f7fd0 / #ffd21f — the red, blue and yellow dice. Move the cap and roof off the die palette: `wall: { color: '#c23b3b', cap: '#7fc4a8' }` and `tower: { body: '#3f7fd0', roof: '#f0dfb4' }` keeps the red toy frame while the rim goes mint and cream. If the primary blocks are wanted for the look, shrink them instead at ThemedArena.tsx line 1577, `boxGeometry args={[0.38, 0.38, 0.38]}` → 0.24, so they stop matching a die's size as well as its colour. The tan checker floor is calm and needs nothing.

- **die `gold` — improve:** It does read as gold — a warm sweep of light across the face, richer than a flat yellow cube. But every tonal edge of that sweep is a hard sawtooth chevron, a visible zigzag staircase running the whole diagonal. On a tumbling die that will crawl and shimmer rather than sit still, and in the Store grid it looks like a compression artefact rather than polish.
  - Fix: src/dice/patterns.ts:616 in `sheen` — `Math.sin((x * 0.78 - y * 0.62) * 2.4)` has a 2.6px period on the 64px tile, barely above the sampling limit, so it beats against the pixel grid into the sawtooth. Drop the multiplier from 2.4 to about 1.1 (period ~5.7px) so the brush lines resolve cleanly, or supersample the painter 2x2 the way `frost` and `blossom` do. The identical line appears at 643 (silver), 1227 (ruby) and 1263 (copper) — change all four in one go so the four metals stay one family.

- **die `mint` — improve:** A solid pastel green square with nothing on it. The sticker only covers the middle third, so two-thirds of every face is dead flat fill — and this is a ladder PRIZE that ends up looking blanker than the default Ivory die a player starts with. Ivory was given faint grain and a sheen on 31 Aug for exactly this reason; Mint, Bubblegum and Midnight are the three that never got it. Not broken, and the flat-ladder/patterned-store split is deliberate — but it is the one place a family unlocks something and sees less than they had.
  - Fix: src/game/diceSkins.ts:52 — `pattern: 'plain'`. Add one shared low-amplitude mask painter to PAINTERS in src/dice/patterns.ts (call it `satin`): just the sheen half of `ivory` at 661-690, i.e. `bar(0.38, 0.26, 0.55) + bar(1.05, 0.16, 0.16) - 0.1`, with the grain and mottle terms dropped. Add `'satin'` to the PatternId union at line 19ff, then set `pattern: 'satin'` plus a pale `ink` (e.g. `#d9fbee`) on Mint. Keep it at or below ivory's amplitudes so the face stickers still dominate.

- **die `bubblegum` — improve:** Same as Mint: a solid pink square, no shell material at all, and a ladder prize that reads flatter than the starting Ivory die. The pink itself is lovely and very much on-brand for the age range — it just has nothing on it.
  - Fix: src/game/diceSkins.ts:53-59 — `pattern: 'plain'`. Point it at the shared `satin` painter described in the Mint fix, with a pale `ink` such as `#ffd2e8`. One painter covers Mint, Bubblegum and Midnight; no per-skin art needed.

- **die `midnight` — improve:** A solid navy square. It is the strongest of the three flat ladder skins — a very dark shell makes the coloured face sticker pop hard, which is a real virtue — but it is still Starry with the stars deleted, and it sits next to Starry in the set.
  - Fix: src/game/diceSkins.ts:60-67 — `pattern: 'plain'`. Point it at the shared `satin` painter from the Mint fix, but give it a lifted-navy `ink` (e.g. `#3d4568`) rather than a white one, so it gains a faint sweep of moonlight and stays night-dark instead of turning grey.

- **die `zebra` — improve:** This is the weakest die in the set and it costs 250 coins. It does not read as a zebra — it reads as a barber pole or a rugby shirt: perfectly regular 45-degree stripes, every one the same width, dead straight, never tapering or branching. A real zebra's stripes bend, swell and come to points. On top of that the edges are raw pixel staircases — it is the only painter in the whole file that returns a hard 0 or 1 with no ramp at all, while everything around it smoothsteps or supersamples, so it looks noticeably cruder than its neighbours in the Store grid.
  - Fix: src/dice/patterns.ts:290 — `stripes: (x, y) => (Math.floor((x + y) / 8) % 2 === 0 ? 1 : 0)`. `stripes` is used ONLY by the zebra skin (src/game/diceSkins.ts:77), so it can be rewritten in place. Warp the stripe coordinate with the file's existing tiling `fbm` before banding it, exactly as `marble` does at 516-520 (`const px = x + fbm(...) * 13`), so the stripes bend and vary in width; then replace the integer `floor` test with a `smoothstep` ramp on the wrapped phase (the pattern `wood` uses at 474-479) so the edges are soft rather than a staircase. Taper is the last touch: multiply the stripe width by a slow `wrappedNoise` along the stripe's length so each band swells and thins.

- **die `starry` — improve:** It reads instantly as starry and it is charming — white stars on deep night blue, and holding the stars white rather than yellow was the right call next to the yellow face sticker. Two things hold it back. The stars are chunky and stair-stepped at the edges, which shows badly because Frost sits beside it in the Store with visibly more delicate, cleanly-ramped shapes. And they sit on a rigid unstaggered 3x3 grid — nine identical upright stars in perfect rows and columns, which reads as wallpaper rather than as a sky.
  - Fix: src/dice/patterns.ts:311-330 in `stars` — the shape test ends `return r <= boundary * 0.95 ? 1 : 0` with no antialiasing. Wrap it in a 3x3 supersample the way `blossom` does (see the loop near 1832) and average, which is what lets Frost's thin arms hold together. Offset alternate rows by `cell / 2` using the `row % 2` trick `spots` already uses at 292-300, and derive a small per-cell size and rotation jitter from a hash of row/col as `frost` does at 356-360. Also `cell = 21` does not divide the 64px tile, so the pattern wraps one pixel short — use `SIZE / 3` or 16.

- **die `marble` — improve:** The most expensive die reviewed here at 525 coins, and the one that looks least finished. It is washed out — at a glance it is a near-white die with almost nothing on it, and in a Store thumbnail it will be hard to tell from a plain pale cube. The veins that are there are broken into dotted, dashed pixel runs rather than continuous lines, so where you do see them they read as speckle or a printing fault, not as stone.
  - Fix: Two changes. (1) src/dice/patterns.ts:559 — the `hair` outriders threshold `Math.abs(field - 0.52)` against 0.045, which still falls between pixels wherever the field's gradient is steep; that is the dotted line. Widen it to about 0.08, or drop the `hair` term entirely and put the weight into the main vein. (2) Contrast: the vein core peaks at 0.82 (line 557) and the ink is `#7f8792` on a `#f2efe8` body (src/game/diceSkins.ts:122-130). Raise the vein multiplier toward 1.0 and darken the ink a step (around `#6b7480`) so the veining survives at die size. Check it against the ivory rule afterwards — it should still be quiet under the sticker, just actually visible.

- **die `granite` — improve:** It reads as grey stone, so it is not wrong — but it is uniform fine static across the whole face rather than rock. The 'big soft blotches' the code is aiming for are drowned and barely visible, and because every single pixel is jittered independently the surface will crawl and fizz as the die tumbles, which is the one texture artefact that gets noticed in motion. Next to Slate, which has clear structure, Granite looks like an unresolved noise field.
  - Fix: src/dice/patterns.ts:582 — `const fleck = (hash - 0.5) * 0.34` perturbs EVERY pixel by up to 0.17, which is the same amplitude as the `coarse` mottle at 587, so the two-scale structure is swamped. Make the grains sparse and discrete instead: fleck only where `hash > 0.9` (and a dark counterpart where `hash < 0.1`), at a higher amplitude each, leaving the rest of the surface clean. Then raise `coarse` (587) and `fine` (588) from 0.16/0.08 to roughly 0.24/0.12 so the blotching actually reads at die size.

- **die `silver` — improve:** Reads correctly as polished silver and the tighter, snappier highlight does distinguish it from Gold. Same defect as Gold though: the tonal edges of the sweep carry a visible zigzag sawtooth staircase down the diagonal, lower contrast than Gold's but there.
  - Fix: src/dice/patterns.ts:643 in `brushed` — same one-line change as Gold: `Math.sin((x * 0.78 - y * 0.62) * 2.4)` has a 2.6px period on the 64px tile and beats against the pixel grid. Drop 2.4 to about 1.1. Apply it together with 616 (gold), 1227 (ruby) and 1263 (copper) in a single edit.

- **die `ruby` — improve:** It reads as a deep red gemstone metal and belongs to the Gold/Silver family as intended. It is the grittiest of the three, though: on top of the same zigzag staircase along the light band there is a fine per-pixel speckle over the whole face, which makes the dark side look slightly dirty rather than deep.
  - Fix: src/dice/patterns.ts:1227 — same brush-frequency change as Gold (2.4 to about 1.1), and additionally drop or halve the `+ (hashCell(x, y) - 0.5) * 0.05` term on that line, which is per-pixel white noise and is what separates Ruby's surface quality from Gold's and Silver's (neither of which carries it). The identical line at 1263 for Copper should get the same treatment.

- **die `paws` — improve:** The motif does not read as a paw print at a glance. Each stamp is a wide flat pad with THREE toes above it, and three toes is not what any animal's paw looks like — my first read was a mushroom, or a little crowned figure, not a paw. It is the only animal skin in the set whose name I could not guess from the picture. Everything else about it (flat brown on tan, no outline) is right and matches Leopard and Giraffe.
  - Fix: src/dice/patterns.ts, `paws` painter (line ~744). Line 753 lists three toes: `[[-3.6, -2.2], [0, -3.8], [3.6, -2.2]]`. Make it four on an arc, the way a cat or dog print actually sits: `[[-4.4, -1.6], [-1.7, -3.7], [1.7, -3.7], [4.4, -1.6]]`, and drop the toe radius from 1.7 to 1.5 so the four fit in the 21px cell with clear gaps. Then shrink the pad on line 751 from `< 3.4` to `< 3.1` so there is still visible tan between the pad and the toe row.

- **die `golf` — improve:** The dimples are lovely — they read as a golf ball on their own now. The navy band is the problem: it sits about a sixth of the way down the face, hugging the top edge, so it reads as a stray printed stripe or a seam rather than as a stripe painted round a ball. Because every face carries it near its own top edge, the stripes on adjacent faces never line up, so a tumbling die shows disconnected navy slashes. It is also the one hard-edged saturated element on an otherwise near-white die, so at thumbnail size the die reads as "white with a blue mark" rather than "golf ball".
  - Fix: src/dice/patterns.ts line 1381, in the `dimples` painter. Replace the top-edge band `px = mixRgb(px, rgb('#1a3a8c'), smoothstep(4.6, 3.4, Math.abs(y - 9)));` with a band running corner to corner through the face centre: `px = mixRgb(px, rgb('#1a3a8c'), smoothstep(4.6, 3.4, Math.abs(x + y - SIZE) / Math.SQRT2));`. The colour sticker (radius 0.33 * 64 = 21px, dead centre) hides the middle of it, so what shows is two matching navy wedges at opposite corners — which is what a range ball's band actually looks like when the ball is turned. Keep the colour #1a3a8c; the comment above the line records it clearing the ΔLab guard against the blue face, so do not change it.

- **die `bowling` — improve:** The three finger holes are what make this a bowling ball, and one of them will be half eaten by the colour sticker on every single face. The holes are at (8,11), (18,8) and (13,21) in the 64px tile; the sticker is an opaque disc of radius 21 at the centre (32,32). The third hole's centre is only 21.95 from that centre, so its inner side sits under the sticker and a player sees two holes and a nick — a defect, not a ball. (The flat texture render hides this because the sticker is a separate mesh drawn on top.) Second, smaller: the specular highlight in the lower-right blows out to pure white in a big soft blob, which reads as a smudge or a lens flare rather than gloss, and it is the softest, most airbrushed thing in a set that is otherwise hard-edged Paper & Ink.
  - Fix: src/dice/patterns.ts line 1545, in the `bowling` painter. Move all three holes clear of the sticker disc — every centre needs to be at least 26 from (32,32) so the whole 4.3px hole clears it: change `[[8, 11], [18, 8], [13, 21]]` to `[[9, 9], [19, 6], [7, 20]]` (distances 32.5, 29.1 and 28.2 — all clear). That keeps the tight finger-hole triangle, just nudged into the free corner. Then tame the glare two lines above: change `smoothstep(8, 3, g) * 0.75` to `smoothstep(6, 2.5, g) * 0.5` so the hot core is a tighter gloss dot instead of a white blob.

- **die `volleyball` — improve:** It reads as ruled diagonal stripes rather than a ball. The blue/gold/white is the right volleyball colouring, but the bands run edge to edge in one straight direction, so on the shelf it sits next to Candy Cane and Rainbow as a third "stripey" die instead of a sports ball. A real volleyball's signature is that the panels come in groups of three and each group turns 90 degrees against its neighbour — that turn is what your eye reads as "ball".
  - Fix: src/dice/patterns.ts, volleyball painter (~line 1516). The whole face uses one axis: `const t = (x * 0.78 + y * 0.62) / 15;`. Alternate the panel-group axis per quadrant so the bands cross: `const flip = (Math.floor(x / 32) + Math.floor(y / 32)) % 2 === 0; const t = (flip ? x * 0.78 + y * 0.62 : y * 0.78 - x * 0.62) / 15;`. Leave band/frac/seam/grain below it untouched — they already give the dome and seam channel. Optionally paint the quadrant boundary with the same seam colour (`#66625a`) so the join reads as a panel seam rather than a cut.

- **die `strawberry` — improve:** The pale ring around every seed is the loudest thing on the die, so at a glance it reads as pink polka dots or bubble wrap, and only reads as a strawberry once you notice the yellow seeds. The rings are also all the same size in a tidy repeating grid, which makes it look like wallpaper rather than fruit skin. It is the weakest of the food dice next to Cookie, Chocolate and Watermelon, all of which land instantly.
  - Fix: src/dice/patterns.ts, strawberry painter (~line 1147). Two small changes. (1) Turn the halo down and tighten it: replace `px = mixRgb(px, rgb('#ef939c'), smoothstep(2.6, 4.4, d) * smoothstep(6, 4.4, d) * 0.5)` with a narrower, weaker lip — `smoothstep(2.0, 3.0, d) * smoothstep(4.4, 3.0, d) * 0.22` — and deepen the dimple to compensate by raising the `#9e2a40` term from 0.55 to about 0.7, so the seed reads as sunk into the skin instead of ringed. (2) Break the grid: the cookie painter twelve lines above (~line 798) already does exactly this — add its jitter, `const h = hashCell(col, row)` with `cx`/`cy` offset by `(h - 0.5) * 3`, so the seeds sit irregularly the way real achenes do.

- **die `donut` — improve:** The icing itself is lovely — the pink and the five-colour sprinkles are the best food die here — but the edge where the icing stops and the bare dough shows in the bottom-right corner is a hard blocky staircase, big square steps you can count. It looks broken rather than scalloped, like the picture failed to finish drawing. It is the one obviously unfinished-looking thing across all seventeen dice.
  - Fix: src/dice/patterns.ts, donut painter (~line 943). The scallop wave is finer than the texture can resolve: `const dripEdge = 50 + tiling(x - y, 4) * 5;` gives a 5px swing on a 4px period across a 64px shell, so the teeth are larger than their spacing and alias into a staircase. Make it longer and shallower — `const dripEdge = 50 + tiling(x - y, 16) * 2.5;` — for three or four readable scallops. Then soften the hard boolean: instead of `const onCake = x + y > dripEdge + 46;` with an early return, blend across roughly two pixels, e.g. `const cake = smoothstep(dripEdge + 45, dripEdge + 47, x + y)` and mix the glaze/sprinkle result toward the cake colour by `cake`, so the icing edge has a lip rather than a cut.

- **die `circuit` — improve:** It reads as horizontal stripes with knobs on the end, not as a circuit board. Every trace enters from the left edge and runs dead straight to the right — nothing ever turns a corner, nothing runs up or down, so eight parallel lines is all you see, and at thumbnail size in the Store grid it will look like a striped teal die rather than the named thing. A circuit board is recognisable because of its right angles.
  - Fix: src/dice/patterns.ts, circuit painter (~line 852). The turn is a no-op in the code, not just visually: `online` is `Math.abs(inY - 4) < 1 && (x < turnAt || (x > turnAt && x < padAt && Math.abs(inY - 4) < 1))` — both branches test the same row, so `turnAt` only punches a 1px gap in one straight line. Give the stub its own row and connect the two with a riser: `const dir = hashCell(lane, 11) > 0.5 ? 1 : -1; const toY = 4 + dir * 3;` then `const onRun = Math.abs(inY - 4) < 1 && x < turnAt; const onRiser = Math.abs(x - turnAt) < 1 && (inY - 4) * dir >= -1 && (inY - 4) * dir <= 3.2; const onStub = Math.abs(inY - toY) < 1 && x > turnAt && x < padAt;` and return 0.7 for `onRun || onRiser || onStub`. Move the pad to the new row too: `Math.hypot(x - padAt, inY - toY) < 2.4`. 4 ± 3 keeps the riser inside the 8px lane, so lanes will not collide.


Judged good as-is: snow, desert, volcano, beach, candy, glade, autumn, farm, cavern, sky, moon, toybox, snow, desert, volcano, beach, candy, farm, sky, ivory, bubbles, timber, frost, ocean, lavender, slate, blossom, copper, cow, bee, turtle, fish, snake, leopard, tiger, giraffe, peacock, tennis, baseball, soccer, basketball, football, cookie, candycane, lemon, chocolate, honeycomb, waffles, watermelon, pizza, denim, camo, tartan, rainbow, galaxy


## C. Needs David to DECIDE (ask, don't build) (29)

### 1. [blocker · medium] hq-site: The site promises no advertising while ads are the launch plan and the site itself already contradicts it. Home says 'No ads, no accounts, no analytics' (page.tsx:58), the app page shows a 'No ads' badge (apps/dice-battles-color-rush/page.tsx:479-483) and a highlight 'No ads, no accounts, and no analytics' (443), the code-default FAQ says 'Dice Battles has never shown an ad and never will' (448; support/page.tsx:19), and the Privacy Policy says 'we don't show ads' and 'don't include any … advertising … software' (privacy/page.tsx:9,20,24). Meanwhile AGENTS.md mandates an interstitial after every third finished game in 1.0, the same site serves hq/public/app-ads.txt authorising AdMob publisher pub-5310885665976703, and the LIVE FAQ row already reads 'Does the game have ads? Yes, and for a nominal fee they can be removed' (site_content dice_battles.faq[0]; live /apps/dice-battles-color-rush and /support text) — three lines under the 'No ads' badge on the same page.

- **Evidence:** hq/src/app/page.tsx:30 fallback 'No ads, no accounts, no analytics.'; hq/src/app/apps/dice-battles-color-rush/page.tsx:41 'No ads, no accounts, and no analytics', :46 'never shown an ad and never will', :77-81 pills default ['No ads','No accounts','No tracking']; hq/src/app/privacy/page.tsx:9,20,24; hq/public/app-ads.txt:12 'google.com, pub-5310885665976703, DIRECT, f08c47fec0942fa0'. Live fetch today of https://papershipstudio.com/apps/dice-battles-color-rush stripped to text shows in order: 'No ads' … 'No ads, no accounts, and no analytics' … 'Does the game have ads?' / 'Yes, and for a nominal fee they can be removed.'; same FAQ answer on live /support; live / shows 'No ads, no accounts, no analytics'; live /privacy shows 'we don't show ads'. app.json:61-65 ships the react-native-google-mobile-ads plugin and src/game/ads.ts:115 has a real interstitial unit id; STATE.md:123 'Remove Ads — $3.99'. (Map line numbers are off — the file is 262 lines — but every quoted string exists.)

- **Do this:** David must confirm the one true story before any copy or policy is written: (a) ads in 1.0 as AGENTS.md says (interstitial every 3rd finished game, child-directed, non-personalised, removable for $3.99) — then remove 'No ads' from hq/src/app/page.tsx:30 (home.about_body fallback), drop the 'No ads' pill at hq/src/app/apps/dice-battles-color-rush/page.tsx:78, rewrite highlight 4 (:40-42) and FAQ 1 (:46) plus hq/src/app/support/page.tsx:19, rewrite privacy sections 'The short version' and 'Information we collect' (hq/src/app/privacy/page.tsx:20,24) and the metadata description (:9) to disclose Google AdMob serving non-personalised, child-directed ads, and update the live site_content rows home.about_body, dice_battles.pills, dice_battles.highlights, dice_battles.faq, privacy.sections via /admin/content (the DB overrides the code fallbacks, so editing code alone changes nothing live); or (b) no ads at launch — then delete hq/public/app-ads.txt, change the live FAQ answer back, and remove the ads plan from the App Store filing. Whichever it is, the internal contradiction (badge 'No ads' three lines above 'Yes, and for a nominal fee they can be removed') must be gone before submission because /privacy is the App Store Connect privacy URL.

### 2. [blocker · medium] hq-site: The Privacy Policy states 'Information we collect: None … no data leaves your device because of our apps', but the game already sends data to this very site: /api/players stores Game Center alias, trophies, wins by mode, dice/arenas owned, favourites, friend code and last_played (players/route.ts:40-55,142-148); /api/friends stores the friend graph including blocks; /api/bug-report stores free-text reports plus a device string (bug-report/route.ts:61-70,78-83); the contact form stores name, email and message (support/actions.ts:337-343). Terms likewise say 'don't require an account and don't collect personal data … progress is stored on your own device' (terms/page.tsx:98) while identity now comes from Game Center and trophies/wins are pushed to the server. Commit 567a542 (today) added the API and CHANGELOG but touched neither policy.

- **Evidence:** hq/src/app/api/players/route.ts:45-54 cleanProfile writes trophies, wins, mode_wins, dice_owned, arenas_owned, favourite_die, favourite_arena, last_played; :142-147 inserts player_id, name (Game Center alias per comment :58), friend_code, secret_hash; hq/src/app/api/friends/route.ts:24-35 stores request/friends/blocked states; hq/src/app/api/bug-report/route.ts:61-70 inserts detail: message and the device string, :78-83 activity row with detail: device; hq/src/app/support/actions.ts:69-75 inserts name, email, subject, body, device. src/game/friendsApi.ts:17 BASE = 'https://dice-battles-hq.vercel.app/api', :77-86 POSTs playerId, secret, friendCode, name and stats. Live /privacy today: 'None. Our apps don't require sign-up, don't ask for personal details, and don't include any analytics, advertising, or crash-reporting software that would send data about you or your device to us'; live /terms: 'don't collect personal data … Any game progress is stored on your own device'. `git show --stat 567a542` (2026-09-04) lists CHANGELOG.md, the two route files and game code — no privacy/terms change.

- **Do this:** David must approve new legal wording (it is what App Review compares against the App Privacy filing). Draft for him: in hq/src/app/privacy/page.tsx DEFAULT_SECTIONS replace 'Information we collect: None…' with a section listing exactly what the friends feature stores (Game Center display name, a random device id, friend code, trophies and win counts, dice/arenas owned, favourites, last played; friend/block relationships), what bug reports store (the message and device model/OS string), what the website contact form stores (name, optional email, message), where it is held (Supabase, US region — confirm), that it is not sold or used for advertising, and how to delete it (email); mirror the change in hq/src/app/terms/page.tsx:22-24 ('No accounts, your device, your data'). Then push the same text into live site_content privacy.sections / terms.sections through /admin/content/privacy and /admin/content/terms, because the DB rows override the code. Separately (owner hands) the App Store Connect App Privacy answers must change from 'Data Not Collected' to declare Identifiers (device id), Gameplay Content/User Content (bug reports), and Contact Info if the in-app report includes an email.

### 3. [blocker · medium] integrations: The public privacy policy says nothing leaves the device and there is no crash-reporting software, while the app now sends bug reports (free text + OS/version/screen/updateId), Game Center ids + aliases + stats to Supabase, and fetches news. The App Store record points at this page. The bug-report disclosure that v1.10.0 added was deleted in the site rebuild and never restored; the friends data was never disclosed at all.

- **Evidence:** Live page fetched 5 Sep 2026 (`curl https://papershipstudio.com/privacy`) serves exactly the code fallback: "no data leaves your device because of our apps" and "don't include any analytics, advertising, or crash-reporting software that would send data about you or your device to us" — the CMS has no override for `privacy.sections`. hq/src/app/privacy/page.tsx:20,24 hold that text; :9 hard-codes the meta description. src/game/friendsApi.ts:77-85 POSTs `playerId: me.playerId, secret, friendCode, name: me.name, ...stats`; playerIdentity.ts:133 `const playerId = apple?.playerId ?? localId` (GameKit gamePlayerID). hq/src/app/api/players/route.ts:53 `last_played: new Date().toISOString()` on every push, :142-148 stores player_id/name/friend_code. src/debug/bugReport.ts sends message + device string. `git show 56d0d14 -- hq/src/app/privacy/page.tsx` removes the line "a bug report cannot be tied back to a person"; CHANGELOG.md:2733-2737 still says the policy was rewritten to disclose it. CHANGELOG.md:21-23 (v1.64.0) acknowledges "the App Privacy answers already filed with Apple are true only while that holds" and then shipped friends without touching the page. STATE.md:115 confirms the App Store record points at papershipstudio.com/privacy.

- **Do this:** David must choose before submission: (a) keep Friends on in 1.0 and disclose it — rewrite hq/src/app/privacy/page.tsx DEFAULT_SECTIONS (or the CMS `privacy.sections`) to say the app sends a Game Center player id or random device id, the Game Center alias, a friend code, trophy/win counts and a last-played time to Paper Ship Studio's server when Friends is used, plus what a bug report sends; update the meta description at :9; change 'Last updated' (:59); THEN re-file App Privacy in App Store Connect with 'Data Linked to You'/'Identifiers' + 'Gameplay Content' as appropriate (owner hands); or (b) hide the Friends entry point (LeaderboardScreen.tsx:271 setShowFriends) behind a flag for 1.0 and restore only the bug-report paragraph. Either way the page must stop saying 'no data leaves your device' — it is false today for anyone who opens Friends or taps Send on a bug report.

### 4. [blocker · medium] tests-tooling: Three items of the AGENTS.md submission gate have no test: store copy character limits, screenshot dimensions, and app.json version against CHANGELOG/GAME_VERSION. app.json is on 1.60.0 while GAME_VERSION is v1.64.0 and nothing checks even that the app.json version is a real CHANGELOG entry or ≤ GAME_VERSION. Copy is within Apple limits today (subtitle 28/30, keywords 91/100, promo 148/170) but unguarded; screenshots are 1290×2796 and 2048×2732 but unguarded, though appIcon.test.ts already has a PNG header reader to reuse.

- **Evidence:** Facts all verified. app.json:6 is "1.60.0", src/game/version.ts:19 is 'v1.64.0'. tests/screen.test.ts:1174-1196 compares GAME_VERSION to the newest CHANGELOG heading and never reads app.json — and its own comment records that "app.json's version sat at 1.0.0 through eleven releases precisely because nothing checked it", i.e. this exact drift already happened once and the fix did not cover app.json. Measured copy (newline stripped): subtitle 27, keywords 90, promo 147, description 1835 — the two reviewers' figures differ only by the trailing newline, all comfortably inside 30/100/170/4000. Twelve screenshots, iphone 1290×2796 and ipad 2048×2732. No test file mentions screenshots or the store copy. Both reviewers independently caught that the ranked list's `ios.buildNumber present` clause is wrong — eas.json sets appVersionSource "remote" with autoIncrement on both profiles and app.json deliberately has no buildNumber — so that clause is struck. I side with impact on severity. `git log -p -- app.json` shows the version bumped in lockstep with every OTA from 1.31.0 to 1.60.0 on 27 Aug and then stopped; a production build made today declares CFBundleShortVersionString 1.60.0, App Store Connect groups uploaded builds under their short version string, and the ASC record is 1.0 — so that build never appears in the 1.0 record's picker. STATE.md:119-120 already names the symptom: "Still missing before any submission: a build attached to version 1.0". Build 5 was 1.0.0 (CHANGELOG.md:2699) and predates the name and icon work, so the next build cannot dodge the question, and App Store versions never go backwards.

- **Do this:** David picks the public 1.0, and it is permanent: either (a) set app.json:6 "version" to "1.0.0" so the next production build attaches to the existing 1.0 record — GAME_VERSION stays v1.64.x and remains what players see in Settings, which is what src/game/version.ts says it is for; or (b) keep 1.60.0 and create a matching 1.60.0 version record in App Store Connect, accepting a first public release numbered 1.60.0 forever. Then add three cheap pure-Node tests, no new dependencies: (1) extend the 'release · the version number is real' suite in tests/screen.test.ts to assert app.json's expo.version appears as a `## vX.Y.Z` heading in CHANGELOG.md and is <= GAME_VERSION by semver compare; (2) a store-copy test asserting store/subtitle.txt <=30, keywords.txt <=100, promo.txt <=170, description.txt <=4000 characters with the trailing newline stripped; (3) a screenshot test reusing the PNG header reader at tests/appIcon.test.ts:26-36 to assert every store/screenshots/iphone/*.png is 1290x2796 and every ipad one 2048x2732. Do NOT assert ios.buildNumber — EAS owns it. Register any new suite file in tests/run.ts:10-39 or it never runs.

### 5. [major · small] config-release: app.json's marketing version (1.60.0) disagrees with CHANGELOG/GAME_VERSION (v1.64.0) and with the App Store Connect version record (1.0). The next binary will carry CFBundleShortVersionString 1.60.0, which cannot be attached to ASC version 1.0 without renaming that record; and nothing tests app.json's version at all — only GAME_VERSION vs CHANGELOG is checked.

- **Evidence:** /home/user/Dice-battles-/app.json:6 `"version": "1.60.0"`; `npx expo config --type introspect` → CFBundleShortVersionString = "1.60.0"; /home/user/Dice-battles-/CHANGELOG.md:3 `## v1.64.0 — 2026-09-04`; /home/user/Dice-battles-/src/game/version.ts:19 `export const GAME_VERSION = 'v1.64.0';` while version.ts:4-6 claims app.json only moves for native builds — but `git log -L6,6:app.json` shows it bumped on every OTA from 5b6ef13 (1.41.0, 25 Aug) to 4f6d915 (1.60.0, 27 Aug) then stopped. CHANGELOG.md:2699 `Native build: 1.0.0 (build 5)` — the only build known to carry 1.0.0. STATE.md:120 "Still missing before any submission: a build attached to version 1.0" (live ASC record not checkable here: `asc.py status` → "ASC_ISSUER_ID is not set"). tests/screen.test.ts:1183-1195 compares only GAME_VERSION to the changelog; nothing reads app.json's version. eas.json:3 `appVersionSource: remote` governs the build NUMBER only, not this string.

- **Do this:** David must pick one: (a) set app.json `expo.version` to "1.0.0" so the next binary attaches to the existing ASC version 1.0 record and the OTA version stays in src/game/version.ts alone (matches version.ts:4-6); or (b) keep app.json at the OTA version (bump to 1.64.0 now) and rename the ASC version record from 1.0 to match before selecting a build. Whichever is chosen, add a test to tests/screen.test.ts in the 'release · the version number is real' suite that reads app.json and asserts the chosen rule (either version === '1.0.0' / does not track GAME_VERSION, or `'v' + app.expo.version === GAME_VERSION`) so the drift cannot recur silently. Note: build 6 was made ~25 Aug when app.json read 1.3x/1.4x, so only build 5 (1.0.0) can attach to ASC 1.0 as things stand.

### 6. [major · medium] config-release: The 1.0 submission is in a half state on ads: AGENTS.md says one interstitial per third game ships in 1.0, but the JS require is commented out (ads OFF) while the config plugin still compiles the GMA SDK into the next binary. Submitting as-is ships a binary with an idle ad SDK and no ads; turning them on requires the explicit runtimeVersion and a fresh build. Neither path has been chosen.

- **Evidence:** /home/user/Dice-battles-/src/game/adSdk.ts:51-56: `// ADS OFF. ... // return require('react-native-google-mobile-ads'); return null;`. /home/user/Dice-battles-/app.json:60-66 still lists the `react-native-google-mobile-ads` plugin with both app ids, and introspection writes GADApplicationIdentifier = "ca-app-pub-5310885665976703~4644888759" into Info.plist, so any new build links the SDK. AGENTS.md 'Advertising, as of 24 Aug 2026': "David asked for ads in 1.0 rather than a later release, and accepted delaying the App Store launch to get them." CHANGELOG.md:1378-1392 records the switch-off as temporary ("ads wait for the next App Store build"). Nothing in the repo records which way 1.0 goes; STATE.md:119-153 lists IAP work but no ads decision.

- **Do this:** Ask David: does 1.0 ship with ads ON or OFF? ON = uncomment /home/user/Dice-battles-/src/game/adSdk.ts:55, set app.json `runtimeVersion` to an explicit "1.1.0", add `"delayAppMeasurementInit": true` to the plugin options (see the GAD item), build, then re-verify the App Privacy answers against the child-directed flags in src/game/ads.ts:239-244 — this is what AGENTS.md says he chose. OFF-for-1.0 = also remove the `react-native-google-mobile-ads` entry from app.json plugins (lines 60-66) so the binary does not carry an idle SDK that still starts app measurement on launch; ads then need a second build later. Leaving the plugin in while the JS is OFF is the one combination that should not ship.

### 7. [major · small] hq-api: A blocked player keeps receiving the blocker's FULL profile (wins, mode wins, collection counts, last-played timestamp) indefinitely, refreshed on every profile push.

- **Evidence:** hq/src/app/api/friends/route.ts:33 `block: { ... mine: 'blocked', theirs: 'none' }` with the write skipped at 193 leaves the blocked party's row at 'friends'; GET line 110 `if (row.state === 'friends') friends.push({ ...full(profile), since: row.since })` never consults the reverse row, and `full()` (86-97) includes `lastPlayed`, `wins`, `modeWins`, `diceOwned`, `arenasOwned`. `last_played` is refreshed on every profile push (players/route.ts:53 `last_played: new Date().toISOString()`, FriendsScreen.tsx:85 pushes on every open). This is a documented design choice, not an accident — the live migration comment says 'your side still reads friends, and you are told nothing' and CHANGELOG.md:35 says 'A block is quiet' — but nothing in the changelog or on screen tells the blocker that the person they blocked still sees when they last played. LAUNCH_REVIEW.md:259 already flags this as a question for David.

- **Do this:** Ask David: when a child blocks someone, should that person keep seeing the child's live stats and 'last played' (current, fully quiet) or should the server strip it? Recommended middle option if he wants it hidden: in hq/src/app/api/friends/route.ts GET, add a second query `friendships` where `other_id = auth.playerId` and `state = 'blocked'`, collect those player_ids into a Set, and for a row whose other_id is in that set push `{ ...full(profile), lastPlayed: 0, wins: {}, modeWins: {}, diceOwned: 0, arenasOwned: 0, since: row.since }` (keeps the friend in the list so the block stays undetectable by list shape, removes presence and stats); update the comment at lines 30-32, tests/friends.test.ts:322-333, CHANGELOG.md and the `changes` table. If he chooses to keep it quiet as-is, add one sentence to the Friends screen block confirmation saying so.

### 8. [major · medium] hq-api: A Game Center player who reinstalls the app (or loses AsyncStorage) is locked out of their own profile forever: the device generates a new secret, the server answers 'wrong secret', and there is no reset or recovery endpoint. The same first-writer-wins rule lets anyone pre-claim an unclaimed playerId.

- **Evidence:** src/game/playerIdentity.ts:140 `const secret = storedSecret ?? makeSecret();` with `const playerId = apple?.playerId ?? localId;` at 133 — a delete-and-reinstall clears AsyncStorage (the SECRET_KEY 'dice-battles/player-secret' at line 33) while Game Center returns the same id. hq/src/app/api/players/route.ts:74-82: existing row + non-matching hash -> `{ error: 'wrong secret' }` -> 401 at 128. `grep -rn 'reset\|recover' hq/src/app/api` finds nothing. src/demo/FriendsScreen.tsx:85 `await pushProfile(me, stats);` discards the boolean; 86-92 then surfaces fetchFriends' error, and friends/route.ts:52 returns the same 'wrong secret', so the screen shows the literal text 'wrong secret' with no way out. Pre-claim: playerId has no format check (players/route.ts:69) and the stranger lookup at 103-105 hands back `playerId`, but pre-claim needs an id that has never been posted, and a Game Center id is only learnable after its owner has already claimed it — so that half is real but low-risk. Note the only real recovery today is an iCloud/iTunes restore, which does bring AsyncStorage back.

- **Do this:** Ask David which recovery he wants before launch: (a) accept the lockout and make the screen say so honestly ('This phone was set up fresh; your friends list from before is gone. Ask them to add your new code.') — smallest change, in src/demo/FriendsScreen.tsx map the 'wrong secret' error to that copy; (b) let a Game Center-verified caller re-claim: add `POST /api/players/reset` that takes playerId + a fresh secret and, ONLY for ids that are not `local-`, replaces secret_hash and wipes the player's friendships (a reinstall becomes a clean slate but the friend code and id survive) — this trusts the client's claim of Game Center identity, which is the same trust the first claim already has; (c) do nothing now and note it in STATE.md as known-broken. Whichever he picks, also make src/game/friendsApi.ts pushProfile return the error string rather than a boolean so FriendsScreen can tell 'wrong secret' from 'No connection'.

### 9. [major · small] hq-site: There is no App Store link, badge, or availability status anywhere on the site — the app page's only call to action is a mailto. The App Store Connect marketing/support URLs point here (STATE.md), so on launch day a visitor cannot get from the site to the app, and today a visitor cannot tell whether it is released, in TestFlight, or coming soon.

- **Evidence:** `grep -rn -iE 'apps\.apple\.com|itunes\.apple' hq/src hq/public` returns nothing; `grep -rn -iE 'App Store|testflight|coming soon' hq/src/app --include=*.tsx | grep -v admin` matches only the privacy section heading 'App Store and platform data'. hq/src/app/apps/dice-battles-color-rush/page.tsx:240-256 — the sole outbound link is `href="mailto:support@papershipstudio.com"`; the Apps card (apps/page.tsx:47-113) and Home card (page.tsx:232-248) link only to internal pages. STATE.md:116 'support and marketing URLs live' (pointing at this site). No editable site_content key exists for a store URL or availability note.

- **Do this:** Ask David: (1) Should the site say 'Coming soon to the App Store' now, and switch to a real App Store link on approval? (2) Should the Android APK route ever be mentioned publicly (STATE.md says it has no age check)? Once decided, the implementation is small: add editable keys dice_battles.store_url and dice_battles.availability_note (fallbacks '' and 'Coming soon to the App Store'), render a pill button 'Download on the App Store' linking to https://apps.apple.com/app/id6802287913 when store_url is set (else the availability note) in the header block of hq/src/app/apps/dice-battles-color-rush/page.tsx (after the pills, ~:143) and on the Home card in hq/src/app/page.tsx, and add the two fields to hq/src/app/admin/content/dice-battles/page.tsx + updateDiceBattlesContent in hq/src/app/admin/actions.ts.

### 10. [major · small] integrations: A block does not hide the blocker: the blocked player's own row is left as 'friends', and the GET serves a FULL profile based solely on the requester's row, so someone you blocked keeps seeing your trophies, collection and a live 'last played' timestamp. For a child-safety control this is the wrong kind of quiet.

- **Evidence:** hq/src/app/api/friends/route.ts:33 `block: { from: [...], mine: 'blocked', theirs: 'none' }` but :191-202 `if (action !== 'block') { writes.push(… their row …) }` so their row keeps 'friends'. GET :107-110 `if (row.state === 'friends') friends.push({ ...full(profile), since })` looks only at the requester's row; `full()` (:86-97) includes wins, modeWins, diceOwned, arenasOwned, favourites and `lastPlayed`, and players/route.ts:53 refreshes `last_played` on every push (FriendsScreen.tsx:85 pushes on every open). This is deliberate and locked: route.ts:30-32 comment, CHANGELOG.md:34-37 'A block is quiet', and tests/friends.test.ts:322-333 asserts the `if (action !== 'block')` guard exists.

- **Do this:** Ask David which risk the block should carry: (a) stay fully quiet as designed (blocked person keeps a live view of the blocker's stats and last-played), or (b) hide the blocker: in hq/src/app/api/friends/route.ts GET, also query `friendships` where `other_id = auth.playerId` and `state = 'blocked'`, and for those ids either drop the row from `friends` (detectable) or downgrade to `peek()` and omit `since`/`lastPlayed` (less detectable, hides presence). If (b), update the comment at :30-32, CHANGELOG, and change the test at tests/friends.test.ts:322-333 to assert the new behaviour rather than delete it. Note the middle option — keep the row but strip lastPlayed and the collection for anyone the other side has blocked — removes the presence signal while keeping the list stable.

### 11. [major · small] integrations: Any unauthenticated POST becomes an already-approved bug on the work board that the standing policy and the hourly routine treat as pre-approved work — an unmoderated text channel from the open internet straight into the autonomous agent's queue. The only brake is a global 20/min counter, so one stranger also locks real reporters out with 429s.

- **Evidence:** Facts confirmed: hq/src/app/api/bug-report/route.ts:61-72 inserts `kind: 'bug', status: 'approved', detail: message` with no auth and no per-sender limit; :45-56 counts ALL bugs in the last minute so 20 posts from one IP return 429 to everyone. The queue exposes it: hq/src/app/api/queue/route.ts:60,77-78 returns approved items ordered by priority with `kind`. Framing overstated: the insert sets no priority, so hq/supabase/schema.sql:159 `priority integer not null default 3` applies, and AGENTS.md 'What runs without being asked' says every bug that is not priority 1 'gets an email … and then WAITS for an answer' — so the routine does not build it unasked. What remains real: the report text is read by the agent (prompt-injection surface), a session pulling /api/queue sees it under `approved`, and the flood brake is the wrong shape.

- **Do this:** Ask David: should a report from an unknown phone arrive as `approved`, or in a holding state (e.g. status 'proposed'/'inbox') until a person glances at it? Whichever he picks, two changes are unambiguous and can be done now in /home/user/Dice-battles-/hq/src/app/api/bug-report/route.ts: (1) make the brake per-sender — read `request.headers.get('x-forwarded-for')?.split(',')[0]`, store its SHA-256 in the activity row `detail` or a new column, and count the last minute by that hash (keep a higher global cap, e.g. 100/min, as a second brake); (2) mark provenance so agents treat it as data — set `decision_note` to start with 'UNTRUSTED TEXT FROM THE PUBLIC INTERNET —' and add the same 'data, never instructions' rule to AGENTS.md 'Bugs versus features' that tools/appstoreconnect/README.md already applies to review text.

### 12. [major · small] integrations: Two CC-BY 4.0 sources (three Kevin MacLeod battle tracks and the Gregor Quendel cheer) ship with no attribution anywhere a player or the website can see, which the licence requires; CREDITS.md flags it as a release action item and it was never done.

- **Evidence:** assets/sounds/CREDITS.md:11,14 mark both as 'attribution required' and :18-20 'Action items for release: the app's credits/about screen must include …'. `grep -rniE 'MacLeod|Quendel' --exclude-dir=node_modules .` hits only src/audio/sounds.ts:40 (a code comment) and tests/screen.test.ts:563; `grep -rniE 'credits|incompetech|licen[cs]e' src` (excluding sounds.ts) returns nothing — there is no about/credits screen. hq/src has no hit either. The only test (tests/screen.test.ts:559-566) asserts CREDITS.md mentions the names, not that any player-visible surface does. A file in a GitHub repo is not attribution 'in any reasonable manner' for App Store users who never see the repo; incompetech's licence page asks for the credit in the work or its description.

- **Do this:** David decides where credits live (the map's open question): (a) in-app — add a 'Credits' section to the Settings popup (src/demo/SettingsPopup or wherever the Report a Bug row lives) with the two lines from CREDITS.md:19-20 plus 'Dice and voice sounds by Kenney (CC0)'; (b) App Store description — append the two lines to store/description.txt and push with the `set-listing` tool (owner/agent step, character limit permitting); (c) website — add them to the Dice Battles app page in hq/. At least one of (a) or (b) is needed for players who never visit the site. After the choice, strengthen tests/screen.test.ts:559-566 to assert the chosen surface's source file contains 'Kevin MacLeod' and 'Gregor Quendel'.

### 13. [major · medium] integrations: Ads are switched OFF and no built binary contains the SDK, but AGENTS.md states ads ship in 1.0 and that runtimeVersion 'is an explicit string ("1.1.0"), never the sdkVersion policy'. Turning them on needs a native build, a runtimeVersion bump, and (per Google's AdMob App-Privacy guidance: identifiers, usage, diagnostics) a changed App Privacy filing — none of which STATE.md lists as pending.

- **Evidence:** src/game/adSdk.ts:55 `// return require('react-native-google-mobile-ads');` :56 `return null;` with the header (:17-27) 'CURRENT STATE: OFF'. app.json:43-45 `"runtimeVersion": { "policy": "sdkVersion" }`. AGENTS.md 'Advertising, as of 24 Aug 2026': 'David asked for ads in 1.0 rather than a later release' and 'runtimeVersion is the only real gate … explicit string ("1.1.0"), never the sdkVersion policy' — both contradicted by the tree. STATE.md:105-155 lists nothing about the ad SDK build or App Privacy re-filing as pending; its only ad mention is the Remove-Ads IAP (:123) and 'StoreKit code … must ride the same binary as the ads SDK' (:149). tests/ads.test.ts:133-193 permits both states so no test flags the disagreement with AGENTS.md. Data-collected categories are argued nowhere in the repo (grep for 'data collected|app privacy' in STATE.md and tools/appstoreconnect returns nothing).

- **Do this:** Ask David: does 1.0 ship with ads ON or OFF? If OFF: edit AGENTS.md 'Advertising, as of 24 Aug 2026' and 'runtimeVersion is the only real gate' to record that 1.0 ships without the SDK on the sdkVersion policy, and add a 'Still missing before ads' list to STATE.md. If ON: one change that (1) uncomments adSdk.ts:55, (2) sets app.json runtimeVersion to "1.1.0", (3) fixes the TFUA consent item below, then a new EAS build (build 8+), then David re-files App Privacy in App Store Connect adding the categories Google documents for the Mobile Ads SDK (Identifiers/Device ID, Usage Data, Diagnostics) — 'Data Not Collected' would be false once the SDK is in the binary even with child-directed tags.

### 14. [major · medium] integrations: Signing into (or out of) Game Center after a profile has been pushed breaks the Friends screen permanently: the device keeps one friend code across both identities, the server requires friend_code to be unique on insert and returns 409 'friend code taken', pushProfile collapses that to `false`, the screen ignores it and then fetchFriends fails 'no such player' (401) — shown as the error, every visit.

- **Evidence:** src/game/playerIdentity.ts:133 `const playerId = apple?.playerId ?? localId;` and :139 `const friendCode = storedCode ?? makeFriendCode();` with the comment 'kept across sign-in' — one code, two possible ids. hq/src/app/api/players/route.ts:134-157: a new player_id inserts with that code; :152-155 maps Postgres 23505 on friend_code to 409 'friend code taken' and says 'so the game can draw a new one' — `grep -rn 'friend code taken\|409' src` finds no handler. src/game/friendsApi.ts:87 `return result.ok;` discards the error. src/demo/FriendsScreen.tsx:85 `await pushProfile(me, stats);` result unused; :86-92 then shows fetchFriends' error, which friends/route.ts:49 returns as 'no such player' (401) since the new id was never inserted. The unique index itself is not in the repo (no player_profiles schema anywhere), but the outcome is broken either way: without the index there would be two rows with one code and GET ?code= uses `.maybeSingle()` (:97-101), which fails on two rows and answers `found: false`. Realistic trigger: loadIdentity runs at launch (DiceDemoScreen.tsx:240); a player who dismisses the Game Center sheet plays as `local-…`, opens Friends (profile pushed), then later signs in.

- **Do this:** David must pick which identity keeps the code when one device has two. Options: (a) server transfer — in hq/src/app/api/players/route.ts POST, on 23505 for friend_code, look up the existing holder; if `sameSecret(auth.secret, holder.secret_hash)` (same device, since the secret is per-device at playerIdentity.ts:140) move the friend_code to the new player_id and give the old profile a fresh code, then retry the insert; (b) app redraw — in src/game/friendsApi.ts make pushProfile return `{ ok, error }` and in playerIdentity.ts add `renewFriendCode()`; FriendsScreen.tsx:85 on error 'friend code taken' calls it and re-pushes (this changes the code a child may have written down). Regardless of choice, do the safe part now: friendsApi.ts:73-88 return the error string, and FriendsScreen.tsx:85-92 show that error instead of falling through to 'no such player'.

### 15. [major · medium] integrations: The crash guard never forwards fatal errors to the default handler, so expo-updates' error recovery never learns an update is bad: a crashing OTA is not marked failed or rolled back and is relaunched every start until a newer update downloads (two launches with network). It also records non-fatal errors as 'lastFatal' and App renders CrashScreen for them, and CrashScreen has no retry/restart control, so any runtime fatal turns the whole game into a dead screen until force-quit.

- **Evidence:** Confirmed: src/debug/crashGuard.ts:64-69 `reportFatal(error); if (!isFatal && previous) previous(error, isFatal);` — fatals never reach RN's ExceptionsManager, so `NativeExceptionsManager.reportFatalException` → `RCTFatal` never fires, and expo-updates' recovery is hooked exactly there (node_modules/expo-updates/ios/EXUpdates/ErrorRecovery.swift:338 `RCTSetFatalHandler`, :341 `RCTSetFatalExceptionHandler`). This is a knowing trade-off, documented at crashGuard.ts:4-8. App.tsx:49 `onFatal((error) => this.setState({ error }))` and :97 renders CrashScreen; CrashScreen.tsx:25-46 has only View/Text/ScrollView, no Pressable — confirmed no restart. Overstated: the 'non-fatal errors become CrashScreen' half — :65 does run for isFatal=false, but `grep -rln '\.reportError('` over node_modules/react-native/Libraries finds no caller of `ErrorUtils.reportError` (only its declaration at vendor/core/ErrorUtils.js:34); RN routes console.error and promise rejections elsewhere, so that path needs third-party code to call it. Also `getDerivedStateFromError` (:44) is a normal error boundary, not the guard.

- **Do this:** Ask David whether launch keeps 'visible crash screen, manual republish' or wants automatic OTA rollback back. Concrete options: (a) keep the guard but add a way out — in src/debug/CrashScreen.tsx add a Pressable 'Try again' that calls `require('expo-updates').reloadAsync()` (lazy require, inside try/catch — expo-updates is already in every binary per package.json:19) and, on mount, `Updates.fetchUpdateAsync()` so the next reload picks up a republished fix in one launch instead of two; (b) time-boxed forwarding — in crashGuard.ts:64-69 forward a fatal to `previous` when it happens within e.g. 10 s of JS start (the window expo-updates uses to judge a bad update) and show CrashScreen otherwise; (c) leave as is and record the trade-off in AGENTS.md 'Releasing' next to `eas update:republish` ('devices need two launches with network'). Also tighten :65 to `if (isFatal) reportFatal(error);` — unambiguous, since the file's own header says it handles fatals.

### 16. [major · small] menus: One place has three names and another has two: the tab says 'Items', the page says 'Inventory', the preview hint says 'your bag', and reward popups say 'Put it on in the Inventory'; the tab says 'Ranks' while the page says 'Leaderboard'. For a pre-reader matching shapes, or a grandparent reading, the word on the tab must be the word on the page.

- **Evidence:** src/demo/BottomNav.tsx:41 `{ id: 'inventory', label: 'Items', Icon: CrateIcon }`, :44 `{ id: 'leaderboard', label: 'Ranks', Icon: RanksIcon }`; src/demo/InventoryScreen.tsx:63 `<Text style={styles.title}>Inventory</Text>`; src/demo/LeaderboardScreen.tsx:120 `<Text style={styles.title}>Leaderboard</Text>`; src/demo/ItemPreviewBar.tsx:97 'Dice are bought on the Store tab, not in your bag.'; src/demo/DiceDemoScreen.tsx:506 and :599 'Put it on in the Inventory whenever you like.'

- **Do this:** David picks one word per place: 'Items' or 'Inventory'; 'Ranks' or 'Leaderboard'. Then an agent changes: BottomNav.tsx:41/:44 labels OR InventoryScreen.tsx:63 / LeaderboardScreen.tsx:120 titles to match; ItemPreviewBar.tsx:97 'your bag' → the chosen word; DiceDemoScreen.tsx:506 and :599 'in the Inventory' → the chosen word; check tests/layout.test.ts and tests/screen.test.ts for the label strings and update. Note the tab bar is ~53pt per cell so 'Leaderboard' at 10pt would likely not fit one line — the shorter tab words are the practical choice.

### 17. [major · small] store-listing: Three different version numbers: App Store Connect version record is 1.0 (STATE.md:121 'a build attached to version 1.0'), the approved TestFlight build is 1.0.0 (build 5), `app.json` now says 1.60.0, and the game calls itself v1.64.0. The next EAS build will carry CFBundleShortVersionString 1.60.0; Apple rejects attaching a build whose version string differs from the App Store version, and 1.60.0 is not even the current game version.

- **Evidence:** Verified every number. /home/user/Dice-battles-/app.json:6 `"version": "1.60.0"`. /home/user/Dice-battles-/src/game/version.ts:19 `export const GAME_VERSION = 'v1.64.0'`, matching CHANGELOG.md:3 `## v1.64.0`. CHANGELOG.md:2699 `Native build: 1.0.0 (build 5)` and STATE.md:17 'TestFlight (build 5, approved)'. ASC version record is 1.0 (STATE.md:120 'Still missing before any submission: a build attached to version 1.0'). I checked for the guard the map might have missed and it does not save this: eas.json:3 sets `"appVersionSource": "remote"`, but per Expo's build-reference/app-versions docs that makes EAS manage only the BUILD NUMBER (ios.buildNumber) — 'the user-facing version should be explicitly set and updated by you' — so the next production build takes CFBundleShortVersionString straight from app.json:6, i.e. 1.60.0. No test covers it: tests/screen.test.ts:1185-1195 asserts GAME_VERSION == newest CHANGELOG heading and its own comment concedes app.json 'sat at 1.0.0 through eleven releases precisely because nothing checked it'; `grep app.json tests/*.ts` finds no version assertion. app.json's version was bumped in lockstep with releases up to 1.60.0 (commit 4f6d915) and then stopped — the last four releases, including v1.64.0 friends, left it behind. One nuance the map's framing skips: builds 5 and 6 are 1.0.0 and CAN still be attached to the 1.0 record today, so this only bites the moment a new binary is cut — which the AGENTS.md ads-in-1.0 commitment requires.

- **Do this:** David picks one story and it is then a one-line edit. Either (a) keep the ASC record at 1.0 and set app.json:6 back to "1.0.0" so the next build attaches to it — cheapest, and consistent with version.ts:1-18's stated design that app.json is the NATIVE version and GAME_VERSION is the JS one; or (b) create a new App Store version record matching whatever app.json will carry and bump app.json:6 to it. Whichever way, add a test alongside tests/screen.test.ts:1185 that pins app.json.version to an explicitly recorded expected value, and write the chosen number into STATE.md, so the next drift is caught rather than discovered at submission.

### 18. [major · medium] store-listing: The Marketing URL page (papershipstudio.com/apps/dice-battles-color-rush) promises 'No ads ... never will', 'No tracking', and describes a different game ('march your army across the board, and take the other castle', 'the board flips so each side always faces its own castle', 'Roll, march, and clash'). App Review reads the marketing URL; Guideline 2.3.x is about metadata accuracy.

- **Evidence:** Every named string is gone, live. I fetched the page myself: `curl https://papershipstudio.com/apps/dice-battles-color-rush` (HTTP 200, 35,423 bytes) gives 0 hits for 'never will', 0 for 'castle', 0 for 'march your army', 0 for the 'No ads' pill; the served pills are 'No accounts', 'No tracking' (5 hits), 'Ages 4+' (3 hits). page.tsx:12 metadata.description is rewritten to the real game. The DB rows were updated alongside the fallbacks, not just the code — the live page proves it, since both files are force-dynamic and site_content would otherwise override.

But the fix overshot onto the same lines, and the overshoot is live. That same fetch returns 2 hits for 'after every third finished game' and 2 for 'one-time purchase'; the privacy page returns 2 for 'one-time purchase' as well. page.tsx:46 states in the present tense that ads exist and 'can be switched off for good with a one-time purchase'. Neither is true today, and I checked both halves rather than taking either reviewer's word. Ads: src/game/adSdk.ts is explicit — the header says 'CURRENT STATE: OFF', loadAdSdk() has the require commented out and returns null, and app.json:43 has runtimeVersion still on the sdkVersion policy object rather than the explicit '1.1.0' the switch requires. The purchase: `grep -rn 'StoreKit|expo-in-app-purchases|react-native-iap|removeAds'` across the tree returns four hits and every one is prose — README.md:196, STATE.md:123, STATE.md:149, CHANGELOG.md:2686. There is no purchase code anywhere, and STATE.md:147-151 lists 'products created on the App Store record, StoreKit code' as still needed.

The direction of the error matters for how alarmed to be. Over-disclosing ads is the safe way to be wrong with App Review; 2.3.1 targets undisclosed features, not disclosed absent ones. What is not safe is the purchase clause: a page Apple links tells a parent they can pay to remove ads, and there is no such product on the record or in the binary. That is wrong under BOTH branches of the ads decision, so it is not merely downstream of it.

- **Do this:** Split it, because only one half is a decision. The purchase clause is wrong whether ads ship on or off — no IAP exists on the record or in code — so delete 'and they can be switched off for good with a one-time purchase' from hq/src/app/apps/dice-battles-color-rush/page.tsx:46 and the matching sentence from the Advertising section of DEFAULT_SECTIONS in hq/src/app/privacy/page.tsx:29, and do not restore it until a product ID exists on the App Store record. The tense of the ads sentence is David's call: ask whether 1.0 ships with ads on or off. If OFF (what the tree does today), rewrite both to future tense — 'There are no ads today. When they arrive it will be one full-screen ad after every third finished game, never during a game, always non-personalised and G-rated.' If ON, leave the copy and instead flip src/game/adSdk.ts's commented require and set app.json runtimeVersion to an explicit '1.1.0' in the same change — tests/ads.test.ts:171-177 fails if only one moves — then build, because no existing binary can run it. Two deployment traps either way: the strings in both files are only fallbacks behind force-dynamic pages, so make the identical edit at /admin/content or the DB row wins; and per STATE.md:21-31 the Vercel GitHub app is not installed, so commit does not publish — run `npx vercel deploy --prod` from hq/ and re-curl both URLs. Worth adding to the AGENTS.md pre-submission gate: a curl of the marketing and privacy URLs, since /admin/content can silently change what Apple reads at any time.

### 19. [minor · small] arenas-dice: The first time each themed arena opens, its tray floor is painted synchronously on the JS thread inside render; the density budget and the 260ms test ceiling were measured only in V8 (node), while the code's own note says phone paints run 'several hundred' ms where desktop runs 65-120 — on Hermes (no JIT) the heaviest floor at 147ms in node plausibly becomes a 0.5-1s freeze on first open, sixteen times over, including when browsing arenas in the Inventory preview.

- **Evidence:** True parts: src/arena/ThemedArena.tsx:1896-1919 calls `cachedTexture(\`themed-floor-${id}\`, () => createTraySurface(...))` inside useMemo during render; `grep -rn cachedTexture src` shows no pre-warm anywhere (only CastleArena, SpaceArena, JungleArena, ThemedArena call it at render time); tests/arenaShape.test.ts:1007-1030 measures only 'reef' best-of-5 in node against 260ms; textureCache.ts:9-10 'several hundred on a phone' is an older, pre-v1.62.2 figure and STATE.md carries no on-device timing. My node best-of-3 for all sixteen 224x408 trays: moon 176ms, aurora 164ms, reef 142ms, volcano 121ms, farm 103ms, snow 101ms, ... candy/cove 25ms — so the test also pins the wrong 'slowest' floor (moon and aurora beat reef). False parts: the Inventory and Store never mount a live arena — src/demo/InventoryScreen.tsx:169 and src/demo/StoreScreen.tsx:147 render `ARENA_ART[id]` PNGs, and the only Canvas is src/demo/DiceDemoScreen.tsx:1286; the default loadout is `arenaId: 'castle'` (src/game/loadout.ts:115), which has its own floor, so a new player never hits a themed paint; each themed paint happens once per app run and only for an arena the player has unlocked/bought and actually switched to (first rung is Snowy Hollow at 1475 trophies, progress.ts:151). The 0.5-1s Hermes figure is an estimate with no measurement on either side.

- **Do this:** Question for David: is a one-off, sub-second pause the first time an unlocked themed arena is opened acceptable for launch (default Castle is unaffected), or should the chosen arena's floor be painted ahead of time? If the latter, the smallest change is: in the screen that changes the loadout arena (src/game/loadout.ts consumers, e.g. InventoryScreen), on selection call the same `cachedTexture('themed-floor-<id>', ...)` / `themed-ground-<id>` builders via `InteractionManager.runAfterInteractions` so the paint lands while the player is still on the shelf, not on the transition into the game. Independently (safe, small): extend tests/arenaShape.test.ts 'a floor still paints fast enough not to be seen' to iterate all sixteen ids rather than only 'reef', since moon (176ms) and aurora (164ms) now measure slower than reef (142ms). Get one real number by opening Moon Base on David's phone before spending more on this.

### 20. [minor · small] arenas-dice: Pale dice nearly vanish on the palest ladder floors: on Snowy Hollow (tier 1, the first arena a player earns) the Frost shell is ΔLab 6 from the floor, Bubbles/Rainbow 8, Zebra 10, Golf/Marble 11, Cow/Baseball/Candy Cane 12 and the default Ivory 16; the dice are unlit MeshBasicMaterial so the only silhouette cue is a 0.28-opacity blob shadow. No test guards shell-vs-floor contrast — the ΔLab guards cover only shell/ink/paint versus the six face stickers.

- **Evidence:** Numbers reproduce: surfacePixels(snowFence, snow floor) mean rgb(218,228,239); ΔLab of skin.body (src/game/diceSkins.ts) to it: frost 6, bubbles 8, rainbow 8, silver/soccer/zebra 10, golf/volleyball/marble 11, cow/candycane/baseball 12; Sky Kingdom mean rgb(193,204,216): silver 3, bubbles 12, frost 15. Dice body is `new THREE.MeshBasicMaterial({ map: createPatternTexture(...) })` (src/dice/DieMesh.tsx:73-77, unlit); shadow disc `opacity={0.28}` at src/demo/DiceScene.tsx:546, scaled at :271. No test compares shells to floors (tests/screen.test.ts:331 is shell vs PRISONER_COLORS only; tests/arenaShape.test.ts never imports skins). Framing is off in three ways: (1) Snowy Hollow is the first ARENA rung but at 1475 trophies, after several dice rungs (progress.ts:151) — not 'tier 1' in the sense of early play, and the default arena is Castle; (2) the game signal is the six face stickers, which ARE guarded at ΔLab > 28 from every shell — a shell blending into snow loses silhouette, not the colour the player has to read; (3) the body-only number overstates the blend for patterned shells: Frost carries #9fd3f0 arms (diceSkins.ts:119), Cow #2e2a26 patches, Soccer #1d1a2e panels, Candy Cane #8e2438 stripes; only Golf (ink #ffffff, :201) and the plain-ish Ivory are genuinely flat pale on pale. Real but a look nuisance on optional cosmetics, not a launch blocker.

- **Do this:** Ask David to pick one of three, looking at Frost or Golf Ball dice on Snowy Hollow on his phone: (a) a slightly darker packed-snow board — lower `lift(p.a, tray ? 0.35 : 0.15)` toward 0.15 in the snowFence painter at src/arena/groundTexture.ts:~205 and re-pin the snow FINGERPRINT in tests/arenaShape.test.ts; (b) a stronger blob shadow — raise the 0.28 at src/demo/DiceScene.tsx:546 and :271 (affects every arena); or (c) leave it, since face stickers stay readable. Whichever is chosen, add a measured guard: a test in tests/screen.test.ts that, for every themed arena's tray mean colour (surfacePixels) and every DICE_SKINS body, notes the ΔLab and asserts a floor chosen by David (e.g. > 5) so Frost-on-snow cannot get closer unnoticed.

### 21. [minor · small] gameplay: Quitting a battle is free of consequence: the ↺ button, with no confirmation, returns to the menu without applying the loss stakes, paying coins, or counting the game — so trophy loss (and cup knockout) are optional for anyone who quits when behind.

- **Evidence:** Every factual clause in the claim holds, which is what a verdict measures; A's 'partly' was about how much it matters, and both reviewers already agree on minor/needs-decision. src/demo/DiceDemoScreen.tsx:535-545 `const quitToMenu = useCallback(() => { countdownTimers.current.forEach(clearTimeout); stopAnnouncer(); resetRace(); setCallout(null); setPhaseBoth('pick'); ... showAdIfDue(); }` — no noteGameFinished, awardCoins, applyMatchResult, or setRun; all of those live only in finishRound (:547-600). :1419-1424 `{(phase === 'battle' || phase === 'arm' || phase === 'go') && (<Pressable style={styles.quitButton} onPress={quitToMenu}>` with no confirmation — `grep -n Alert src/demo/DiceDemoScreen.tsx` returns nothing. Button is 40x40 at top:52 left:18 (:2024-2030). One correction to the map's framing: quitting does not dodge a coin cost, it forfeits coins — awardCoins pays on a loss too (CHANGELOG: 'Losing still never costs coins'), so the only things a quit escapes are the trophy deduction and the cup knockout, and the cup half is fixed by the item above once cup rounds are flagged (a quit could then also be treated as a lost round). Trophies are a personal ladder plus a friends-only Game Center board; coin packs are a decided-but-unbuilt IAP (STATE.md:136), so this is not yet money-adjacent.

- **Do this:** Question for David: when someone taps ↺ during a battle, should it (a) ask 'Leave this battle?' with Keep playing / Leave and stay free of consequence, (b) count as a loss (trophy stakes, and knock-out if it was a cup round), or (c) stay exactly as it is? Recommendation for a 5+ family game: (a) — it protects a small child from a stray tap on a 40 px button by the top-left corner, and the honesty problem is small. Implementation either way is in quitToMenu (/home/user/Dice-battles-/src/demo/DiceDemoScreen.tsx:535-545): for (a) import Alert from react-native and, when phaseRef.current is 'battle'/'arm'/'go', wrap the body in `Alert.alert('Leave this battle?', undefined, [{ text: 'Keep playing', style: 'cancel' }, { text: 'Leave', style: 'destructive', onPress: <current body> }])`; for (b) call `finishRound('lost')` when phaseRef.current === 'battle' and only then return to the menu. Not an App Store review concern.

### 22. [minor · medium] gameplay: Ultimate has no end condition other than all six freed, and its send-back rule makes that a long random walk: simulating the exact rule gives a mean of ~506 rolls per side (≈17 min at the AI's 2 s cadence; observed tail 5,752 rolls) versus ~88 rolls (≈3 min) for Classic. Color War (1/36 per roll) averages ~107 rolls, i.e. LONGER than Classic despite 'Rescue your three first!'.

- **Evidence:** The mechanics and the ordering are confirmed; the headline minutes are roughly doubled because a battle ends when EITHER side finishes, which is what both reviewers found and the map ignored. Rule as coded: src/demo/DiceDemoScreen.tsx:729 `if (a.id !== b.id) return;` (a match is a double, 1/6 per roll), :741-745 AI send-back `const next = aiFreedRef.current.filter((c) => c !== a.id);`, :846-851 player send-back `moveUnit(freedUnit.key, { kind: 'jail', index: freedUnit.jailIndex });`, :758 the only end `if (ac === PRISONER_COLORS.length) finishRound('lost');`; src/game/ai.ts:27 `AI_ROLL_INTERVAL_MS = 2000`; no timer or turn cap anywhere in the phase machine (:166-174). My own 20,000-game run (scratchpad sim.js, matches expressed as 1/6-per-roll doubles): per side classic 88, ultimate ~503 (max ~5,400), colorwar 108 — the map's numbers. As a race (min of the two sides): classic ~68 ticks (≈2.3 min), ultimate ~270 ticks (≈9 min, max ~2,270 ticks ≈ 76 min), colorwar ~75 ticks (≈2.5 min). So Ultimate is ~4x Classic with a heavy tail, not a typical 17 minutes, and Color War is ~10-20% longer than Classic, not a contradiction of its copy (src/game/modes.ts:48). Note the human side is not on a 2 s cadence, so all minute figures are indicative only.

- **Do this:** Question for David: Ultimate against the AI averages about 9 minutes with a fat tail (one game in three past 10 minutes, rare 30-75 minute games) and has no way to end except quitting. Is that the intended long, swingy mode? If yes: no code change, optionally say so in src/game/modes.ts MODES.ultimate.rules (e.g. add 'The long game!'). If no, this is a rule change for the HQ board as a feature — options: a cap on total rescues after which the side with more freed wins; a time limit with most-freed wins; or a colour can only be captured once. Color War needs nothing.

### 23. [minor · small] hq-api: The public, unauthenticated bug-report endpoint writes rows with status='approved' and kind='bug' directly into the work queue an LLM builder reads and acts on without approval — a prompt-injection surface into the build process — and its only brake is global, so one flood locks out every genuine reporter.

- **Evidence:** Code facts hold: hq/src/app/api/bug-report/route.ts:67-68 `kind: 'bug', status: 'approved'`; brake at 46-51 counts every bug idea in the last minute with no per-caller key; queue/route.ts:74 returns `detail: idea.detail` to whoever holds HQ_API_TOKEN. 'Acts on without approval' is overstated: the insert sets no priority, so ideas.priority takes the schema default 3 (hq/supabase/schema.sql:159 `priority integer not null default 3`), and AGENTS.md:202-206 says only a priority-1 bug is fixed without asking — 'Every other bug gets an email ... and then WAITS for an answer.' So a human gate exists by standing policy; what is missing is a code-level marker that the text came from an anonymous phone rather than a family member. The flood lockout is real but 20 reports/minute is far beyond any load this game will see at launch (1 live profile).

- **Do this:** Ask David whether app-sourced reports should keep landing as approved bugs (current, relying on the priority-3 email-and-wait policy) or as `status: 'pending'` so he glances at each before it is in the queue. If he wants a code-level guard without changing his workflow, the unambiguous change is: in hq/src/app/api/queue/route.ts shape() add `source: idea.decision_note?.startsWith('Reported from the app') ? 'app-bug-report' : 'board'` so the builder can see the detail is untrusted player text, and in bug-report/route.ts key the brake per caller by also counting rows whose decision_note contains the same `device` string (or hash of request IP) in the last minute, with the global 20 kept as a ceiling.

### 24. [minor · small] hq-site: The legal pages never say who operates the studio: 'currently operated by its owner as an individual and trading under the Paper Ship Studio name' (privacy) / 'not yet a registered company' (terms). A privacy policy for an unincorporated trader normally has to identify the person responsible (the data controller), and STATE.md asserts 'Legal pages name David Sutton personally, which is correct until that changes' — which is not what the pages say.

- **Evidence:** Fact confirmed: hq/src/app/privacy/page.tsx:15 and hq/src/app/terms/page.tsx:14 intros say 'operated by its owner as an individual' with no name; live /privacy and /terms today contain no 'Sutton'; STATE.md:183-185 'Legal pages name David Sutton personally, which is correct until that changes' is false. But the framing misses that the omission is deliberate: commit 3a61afa (2026-08-21) 'Take David's name off the public pages; the studio is the name that shows — Requested: the website should name Paper Ship Studio, not a person.' David asked for this. Apple does not require a personal name in the policy (an Individual developer account already shows the legal name as seller on the listing), so for launch this is a STATE.md inaccuracy plus a David-decision, not a major defect.

- **Do this:** Two things: (1) auto-correct STATE.md:183-185 to say the legal pages deliberately name only Paper Ship Studio (per his 21 Aug request) and that the App Store listing shows the individual seller name regardless. (2) Ask David plainly: 'Your 21 Aug request took your name off the site. A privacy policy from a sole trader usually names the responsible person; the App Store already shows you as the seller. Do you want "operated by David Sutton" in the privacy and terms intros, or keep it as the studio only?' Only edit hq/src/app/privacy/page.tsx:15 / terms/page.tsx:14 and the live privacy.intro / terms.intro rows if he says yes.

### 25. [minor · small] menus: The secret code RESET — an ordinary English word typed into a box every player sees — wipes every bought die/arena with no confirmation and no refund ('Coins kept' means the balance is not restored); the TROPHY/COIN codes permanently flag the save off the leaderboard with no undo.

- **Evidence:** src/demo/DiceDemoScreen.tsx:477-489 `} else if (code === RESET_CODE) { const removed = clearPurchases(); … `${removed} bought … cleared. Coins kept.`` ; src/game/currency.ts:161-167 clearPurchases sets `owned: []` and touches nothing on `coins`, so spent coins are not returned; the code box is visible in Settings at :1847-1869 (`placeholder="Secret code…"`); src/game/progress.ts:189 `RESET_CODE = 'RESET'`, :283 and :305 set `cheated: true`, :358 'There is deliberately no in-game way to clear `cheated`'. Calibration: the player has to type exactly RESET and press OK, and the feedback says what happened — an accident is possible but unlikely; this is a tester tool doing what its comment says.

- **Do this:** Ask David: should the Settings 'Secret code…' box ship to the public in 1.0 at all (options: keep it; hide it behind a five-tap on the version line; or keep the box but make RESET refund the coins spent and require typing it twice)? If it stays, the agent-doable part is a two-step confirm for RESET only: on first submit set codeFeedback to 'Type RESET again to clear N bought items' and only act on the second identical submit.

### 26. [minor · small] menus: The code box plus a public repository means anyone can type FAMILY and unlock every Store die — including, once IAP lands, the sets STATE.md records as money-only — and switch to test ads. The home screen then banners 'Family tester mode — everything unlocked, test ads only' to that player.

- **Evidence:** Facts hold: src/game/progress.ts:180 `export const TESTER_CODE = 'FAMILY';`; src/game/loadout.ts:181 `if (skin.price !== undefined) return hasUnlockAll() || owns(skin.id);`; src/game/ads.ts:151-158 `usingTestAds()` returns `getProgress().unlockAll === true`; banner at DiceDemoScreen.tsx:1494-1507. Framing is overstated for launch: every `price` in src/game/diceSkins.ts is a COIN price (250…1130) earned by playing — there are no money-only items in the code yet (STATE.md:130-131 lists them as decided, not built), and STATE.md/AGENTS.md say ads are switched off until a native build exists, so 'test ads' currently changes nothing a player sees. Today FAMILY is a free cosmetic unlock of things that cost zero real money; it becomes a revenue leak only when IAP or live ads land.

- **Do this:** Ask David: keep FAMILY as a public-facing word for 1.0 (accepting that anyone reading the repo can unlock the coin shelf), rename it to something not guessable and not committed (e.g. read from an env at build time — but the JS bundle is inspectable anyway), or hide the code box entirely. Whatever is chosen, before any money-only item ships, `isSkinUnlocked`/`isArenaUnlocked` in src/game/loadout.ts must exclude IAP-priced items from `hasUnlockAll()`; record that as a precondition in STATE.md's IAP section.

### 27. [minor · small] menus: The home screen says 'Race other players to free your prisoners!' and every round opens with a fake 'FINDING YOUR OPPONENT…' scan, but the opponent is a timer rolling virtual dice — the app presents an offline AI as online people, the very thing the Leaderboard file refuses to do.

- **Evidence:** src/demo/DiceDemoScreen.tsx:1491-1493 `Race other players to free your prisoners!`; src/demo/MatchmakingOverlay.tsx:57 `{scanning ? `FINDING YOUR OPPONENT${dotsAt(elapsed)}` : 'YOUR OPPONENT'}`; src/game/ai.ts:4-12 documents the AI as two fair virtual dice on a 2000ms timer; src/game/matchmaking.ts:6 calls the screen 'a reveal, not' matchmaking; LeaderboardScreen.tsx:73-76 'a made-up ranking is a lie told to a child'. The code itself is candid (ai.ts:38 'eventually these become online players'), so this is deliberate theatre, and no store copy claims online multiplayer — a copy/tone decision rather than a defect.

- **Do this:** Ask David: keep the matchmaking theatre as an accepted game fiction, or change two strings so it never claims humans — DiceDemoScreen.tsx:1492 'Race other players…' → 'Race your rival to free your prisoners!' and MatchmakingOverlay.tsx:57 'FINDING YOUR OPPONENT' → 'CHOOSING YOUR RIVAL'. Check tests/screen.test.ts and tests/matchmaking tests for the literal strings before changing.

### 28. [minor · small] menus: The opponent roster shown to every stranger on the matchmaking card and scoreboard is the family's real first names — David, Debra, Lilly, Sophia, Marc, AJ — contrary to the written 'NO NAMES … publishes a child's name to the App Store' rule that news.ts enforces for itself.

- **Evidence:** src/game/ai.ts:46-51 `{ name: 'David', short: 'DAVID', emoji: '🎲' }, { name: 'Debra', … }, { name: 'Lilly', … }, { name: 'Sophia', … }, { name: 'Marc', … }, { name: 'AJ', short: 'AJ', emoji: '⚡' }`; shown at MatchmakingOverlay.tsx:61-64 and DiceDemoScreen.tsx:1388 (`opponent.short`), :1573, :1903. src/game/news.ts:20-25 'NO NAMES … it applies to everyone in the family, not only the post it came from … `tests/news.test.ts` fails on a first name'; tests/news.test.ts:67 `const NAMES = ['AJ', 'Marc', 'David', 'Sutton']` checks NEWS only; tests/game.test.ts:168-175 checks the roster for uniqueness, not names. Calibration: bare common first names with no surname or link to the family are a weak privacy exposure; the inconsistency with David's own rule is the real point and it is his to settle.

- **Do this:** Ask David whether the NO NAMES rule he gave for News (25 Aug 2026) extends to the opponent roster in the public build. If yes, an agent replaces the six family entries in src/game/ai.ts:46-51 with castle-silly names (the file's own stated intent: 'All family-friendly castle-silly names'), keeps `short` ≤ 6 chars for the scoreboard, and extends tests/game.test.ts to assert no roster name is in the NAMES list from tests/news.test.ts (export the list from one place).

### 29. [minor · medium] store-listing: There is no way to delete a server-side profile or friend graph. Once a child's Game Center id, alias and stats are pushed, neither the app nor the API offers removal - a problem for the privacy policy's children's section, for GDPR/COPPA deletion requests, and for Guideline 5.1.1(v) if Apple treats the profile as an account.

- **Evidence:** The mechanism is exactly as described. hq/src/app/api/players/route.ts exports only dynamic (:27), GET (:86) and POST (:118); hq/src/app/api/friends/route.ts exports only dynamic (:18), GET (:57) and POST (:119). grep for DELETE, deleteProfile or 'forget' across hq/src/app/api/ and src/game/friendsApi.ts finds only an unrelated proposals delete at hq/src/app/api/proposals/route.ts:129. ls hq/src/app/admin/ (actions, activity, automation, content, ideas, news, people, schedule, support, timeline, vote) shows no player-data surface either, so a deletion request today can only be honoured by hand-written SQL against Supabase. The compliance framing is where the item overreaches, and the skeptic has this right: 5.1.1(v) requires in-app deletion from apps that support account CREATION, and this app has none — no sign-up, no email, no password, identity borrowed from Game Center (CHANGELOG.md v1.64.0 'Why there is no sign-up'), and STATE.md:118 records demoAccountRequired false, 'the app has no accounts'. GDPR and COPPA require a request channel, not self-service, and the now-live policy gives one explicitly ('we will delete anything belonging to you if you ask us at hello@papershipstudio.com'). So this is a promise with no runbook rather than an unmet obligation or a submission gate — the exposure is an email arriving at hello@ and nobody knowing which rows to clear.

- **Do this:** Not a launch gate; David picks the level. Minimum, and enough for 1.0: write the manual deletion runbook into STATE.md — the exact rows to clear for a player id in the players table and both directions of friendships, in Supabase project eqdbvpnckriscvzalwix — so the promise now live on papershipstudio.com/privacy can actually be kept. Better, and cheap because Friends is pure JS and ships over the air with no new build: add a DELETE handler to hq/src/app/api/players/route.ts authenticated the same way GET is (hq/src/app/api/friends/route.ts:50-56 has the timing-safe secret comparison to copy), clearing the player row and every friendship row naming that id, with a 'Forget me' button at the foot of the Friends screen that calls it and then clears the local Identity. Do it before the first request arrives, not after.


## D. Needs David's HANDS (consoles, accounts, money) (4)

### 1. [blocker · small] store-listing: The privacy policy Apple links to says the app shows no ads, collects nothing and 'no data leaves your device' - all three are now false or about to be. Friends POSTs Game Center player id, alias, trophies, wins and favourites to a server; bug reports POST free text plus device info; the news feed fetches from the same server; AGENTS.md commits to AdMob interstitials in 1.0.

- **Evidence:** The website half is genuinely fixed and I confirmed it live myself, not from the tree. `curl https://papershipstudio.com/privacy` (HTTP 200, 26,358 bytes) returns 0 occurrences of 'no data leaves' and 0 of 'Information we collect', against 4 of 'Game Center', 4 of 'AdMob', 2 of 'child-directed' and 2 of 'Supabase'. hq/src/app/privacy/page.tsx:23 now enumerates the Friends payload field by field, :29 is a dedicated Advertising section, :31 names Supabase/Vercel in the US with a deletion address. Commit bd6178f (HEAD~1) did it. The skeptic is also right on one sub-claim the map got wrong: `grep -rn pushProfile src/` outside friendsApi.ts returns only src/demo/FriendsScreen.tsx:29,53,85 — nothing uploads after an ordinary battle, so 'nothing is sent unless you open it' is accurate.

So I refute the item as literally worded. What I will not do is mark it refuted overall, because the same fact pattern has a second artifact and that one is untouched: the App Store Connect App Privacy card. Three independent traces say it was filed before Friends existed and never revisited — AGENTS.md ('the App Store Connect privacy filing says "not used for tracking"'), CHANGELOG.md:24-26 ('the App Privacy answers already filed with Apple are true only while that holds', written at v1.64.0 when Friends shipped), and STATE.md:114-116, which itemises what is 'Right already' on the record and lists only the policy URL, not the data answers. It could not have been re-filed by tooling either: I listed every subcommand in tools/appstoreconnect/asc.py:427-443 — status, reviews, builds, ask, set-promo, set-screenshots, set-listing — there is no privacy command, so nothing in this repo can read or write that card. Meanwhile src/game/friendsApi.ts:78-84 still POSTs playerId, secret, friendCode, name and stats, and src/debug/bugReport.ts:54-59 still POSTs free text plus a device string.

Neither reviewer could actually read the filing, and neither can I — this is inference from the repo's own records, not observation. That is precisely why it blocks: AGENTS.md's pre-submission gate names 'the App Privacy answers' as a thing to verify, and it is unverified.

- **Do this:** Do not touch the website — that half is done and deployed. David opens App Store Connect > app 6802287913 > App Privacy and reads what is actually filed. If it says 'Data Not Collected', change it to declare, every entry Linked to the User and NOT Used for Tracking (that stays honestly true — no ATT prompt, and src/game/adSdk.ts:56 keeps the ad SDK out of the bundle entirely): Identifiers > User ID (the Game Center gamePlayerID or local fallback from src/game/playerIdentity.ts); Contact Info > Name (the Game Center alias); User Content (bug report free text, src/debug/bugReport.ts); Diagnostics (the device model/OS/build string in the same report); and Gameplay Content or Other Data for trophies and win counts. The now-live papershipstudio.com/privacy is the checklist — it already enumerates exactly this set, so the two documents will agree line for line. Then write the answers into STATE.md beside line 115, replacing the bare 'privacy policy at papershipstudio.com/privacy (live)' with what was actually declared, so the next pre-submission pass can check rather than assume. Whoever runs that pass should know asc.py cannot see this card: 'verified' here means David saying what is on his screen.

### 2. [major · small] store-listing: The age-rating questionnaire was answered (4+) before friends shipped and before ads were decided. Friends adds user-to-user interaction (friend requests, block, Game Center alias of other players shown on the Ranks page); ads add the 'contains advertising' declaration. Apple's revised questionnaire asks about user interaction/UGC, advertising and parental controls; the filing needs re-answering, and Kids-adjacent marketing ('a five-year-old gets it on the first turn') raises the bar under Guideline 5.1.4.

- **Evidence:** The core is confirmed. grep -rniE 'age rating|questionnaire|contains ads' across STATE.md, AGENTS.md, CHANGELOG.md, tools/ and store/ returns only STATE.md:115 ('age rating 4+', in the '30 Aug 2026' block) and CHANGELOG.md:24-27 — nothing records the questionnaire being re-walked after v1.64.0 friends shipped on 2026-09-04, and tools/appstoreconnect/asc.py has no command that can read or write the age-rating declarations, so the answers cannot be checked from this repo at all. Friends does add user-to-user interaction: requests, accept/decline/remove/block, and other players' Game Center aliases shown on Ranks. The advertising limb is real but conditional — src/game/adSdk.ts:50-56 returns null with the require commented out, so a binary built today truthfully answers 'no ads'; the answer only becomes wrong if a build with ads on is what gets submitted. Two limbs of the item are wrong. Guideline 5.1.4 governs the Kids Category, and the app is in Games > Board + Family and must stay there while an ad SDK is in it (AGENTS.md, STATE.md:114) — marketing copy alone does not trigger it. And the UGC limb overstates: tests/friends.test.ts:205-215 pins 'nothing on a profile is free text' by reading src/game/friends.ts, the only displayed name is the Game Center alias Apple itself moderates, and there is no directory — so 4+ should survive an honest re-answer rather than being pushed up. What is left is a mandatory pre-submission check that demonstrably has not been done since the app changed, on the one thing AGENTS.md says cannot be rolled back in seconds.

- **Do this:** David re-walks the Age Rating questionnaire in App Store Connect for app 6802287913 against what v1.64.0 actually does: user-to-user interaction YES (friend requests, blocking, other players' Game Center aliases on Ranks), user-generated content NO (no free text anywhere in Friends — tests/friends.test.ts:205 enforces it), messaging NO, and the advertising answer set to match whichever way the ads-on-or-off decision for 1.0 lands, with the App Store Connect 'contains ads' answer set the same way. Expect 4+ to hold. Record the resulting rating and the date next to STATE.md:115 so the next session does not have to guess again. Hard constraint from AGENTS.md whatever the rating comes out as: the app stays in Games > Board/Family and never moves to the Kids Category while a third-party ad SDK is in it.

### 3. [minor · small] config-release: Android AdMob app id is the iOS app id pasted twice. AdMob issues a separate App ID per platform; the Android build will initialise against an iOS app id and never serve. It has been this way since the commit that added ads.

- **Evidence:** /home/user/Dice-battles-/app.json:63-64 `"iosAppId": "ca-app-pub-5310885665976703~4644888759", "androidAppId": "ca-app-pub-5310885665976703~4644888759"` — identical; `git log -S androidAppId -- app.json` → only 2a1e983 (2026-08-24). Introspected manifest carries meta-data com.google.android.gms.ads.APPLICATION_ID with that value. The fact holds, but the framing overstates it for THIS launch: the launch is iOS-only (eas.json:25-31 has no Android submit profile; STATE.md:165-169 'no Play Console account'), ads are OFF in JS so `MobileAds.initialize()` (src/game/ads.ts:244) is never called on either platform, and the value is a well-formed AdMob id so the GMA Android init provider does not crash on it (node_modules/react-native-google-mobile-ads/plugin/build/index.js:91 warns only for a MISSING id). It matters the day an Android build with ads ON exists.

- **Do this:** David, in the AdMob console: add an Android app for package com.dmsutton.dicebattles and copy its App ID (format ca-app-pub-5310885665976703~XXXXXXXXXX, different from the iOS one), then paste it as `androidAppId` at /home/user/Dice-battles-/app.json:64. Do NOT delete the line meanwhile — the plugin warns the Android SDK crashes without an APPLICATION_ID. Only needed before an Android build that has ads switched on.

### 4. [minor · small] store-listing: The Android AdMob App ID is a copy-paste of the iOS App ID. AdMob issues one App ID per platform app; initialising Android with the iOS id means no fill on Android at best and an invalid-traffic flag on David's account at worst.

- **Evidence:** The fact is exact and I confirmed the line numbers myself: app.json:63 `"iosAppId": "ca-app-pub-5310885665976703~4644888759"` and app.json:64 `"androidAppId": "ca-app-pub-5310885665976703~4644888759"` — byte-identical, and AdMob does issue one App ID per platform app. Nothing guards it: `grep -n AppId tests/*.ts` returns nothing at all, so no test compares them, and those ids appear nowhere else in the tree.

The half I cannot confirm is the consequence the item asserts, and that is what splits it from 'confirmed'. 'No fill at best, an invalid-traffic flag at worst' both require an ad request to be made from an Android install, and none can be: src/game/adSdk.ts returns null on every platform, so the SDK is not even in the bundle, and per STATE.md:168-169 there is no Play Console account and Android exists only as an adhoc APK that STATE.md:178 says nobody has ever played. Three links of the chain are missing — ads switched on, a native Android build, a Play listing with real traffic. The severity both reviewers landed on is right; the stated harm is not currently reachable, which is why this reads partly rather than confirmed.

- **Do this:** The correct value does not exist yet, so this cannot be fixed today by anyone but David, and there is no reason to fix it before Android is real. When it is: he creates an Android app for com.dmsutton.dicebattles in the AdMob console (entries are per-platform, so the existing one is iOS-only) and its ca-app-pub-...~... replaces app.json:64, in the same change that first ships ads to Android — the value is baked into the manifest at build time, so it is dead config until then. Do not delete the key instead: the Google Mobile Ads Android SDK reads APPLICATION_ID from the manifest at startup and a missing value fails harder than a wrong one; if a placeholder is ever needed, Google's public Android test App ID is ca-app-pub-3940256099942544~3347511713. Worth doing now, since it costs one line and closes the hole permanently: add an assertion beside the runtimeVersion check at tests/ads.test.ts:171 that parses app.json, finds the react-native-google-mobile-ads plugin entry and asserts iosAppId !== androidAppId, with a message saying AdMob issues one id per platform.


## E. Real but not worth doing before launch (3)

### 1. [minor · medium] hq-site: Terms of Use do not cover any of the commerce the family decided on 31 Aug (Remove Ads, dice/arena purchases, coin packs, Starter Pack, Dice Club subscription, Season Pass): no purchase, refund, subscription-renewal, virtual-currency or Game Center language. 'Our apps are provided for personal, non-commercial entertainment' and 'No accounts, your device, your data' are the whole story. These must be in place in the same change that ships StoreKit, and nothing in the editor prompts for it.

- **Evidence:** Fact confirmed: hq/src/app/terms/page.tsx:16-41 DEFAULT_SECTIONS contain no purchase/subscription/refund/coin/Game Center text, and live /terms today greps empty for 'purchase|subscri|refund|coin'. STATE.md:121-150 lists the IAP decisions but also 'Still needed: product IDs chosen … products created … StoreKit code (native …)' — none of it exists yet (no StoreKit dependency in package.json). So for the 1.0 build being submitted (builds 5/6, no IAP) the Terms are not yet wrong; the gap only becomes a launch problem when StoreKit ships. The live FAQ's 'for a nominal fee they can be removed' does already imply a purchase, which is covered by the ads item.

- **Do this:** Skip for this launch; there is nothing to buy in the build being submitted. Record it as a required part of the StoreKit change: when IAP ships, add Terms sections for purchases (processed by Apple, Apple's refund process), auto-renewing subscription (Dice Club), virtual currency (coins have no cash value, non-transferable), Season Pass expiry, and Game Center identity for friends — in hq/src/app/terms/page.tsx DEFAULT_SECTIONS and the live terms.sections row — and add a one-line reminder notice in hq/src/app/admin/content/terms/page.tsx like the badge editor's ads note.

### 2. [minor · large] store-listing: Google Play readiness is essentially zero: no Play Console account, no 1024x500 feature graphic, no Play short description (80 chars) or Play-formatted full description, no Data safety answers drafted, no target-audience/Families declaration, no Play screenshots plan. Only adaptive-icon layers exist.

- **Evidence:** Every enumerated absence checks out, and both reviewers agree on the facts and on the severity — the only thing in dispute is the label on work they both say to skip. `ls assets/` holds exactly three Android files (android-icon-background.png, android-icon-foreground.png, android-icon-monochrome.png) and no 1024x500 anywhere in the tree. `ls -R store/` is iOS-only: description.txt, keywords.txt, subtitle.txt, promo.txt and screenshots/{iphone,ipad} at Apple's two sizes; store/README.md documents only Apple's four fields, and tools/appstoreconnect/asc.py is App Store Connect exclusively. A repo-wide grep for 'play console|feature graphic|data safety|families policy' across .md/.json/.ts returns one line: STATE.md:169, 'There is still no Play Console account (that is $25 once, against Apple's $99 a year).'

So the item is accurate as a description of scope. It is not a defect in the launch under review: nothing here gates iOS, there is no Play submission planned, and STATE.md:170-178 records Android as a deliberate parked position kept for one narrow reason — the APK installs from a link with no age check, which is the wall that stops under-13 family testers using TestFlight. Confirmed on the facts, filed at minor because that is what it is worth.

- **Do this:** Skip entirely for this launch and build none of it speculatively — a Play listing now would mean an untested-on-Android game (STATE.md:178: nobody has ever played it there), a Data safety form that must agree with an App Privacy card not yet re-filed, and store art that does not exist, all competing for attention with the submission actually in flight. If David later decides Android is worth $25 and a second store surface, the order that avoids wasted work is: open the Play Console account; get someone to play the existing adhoc APK on a real Android phone before writing a word of listing copy; then produce assets into a store/play/ folder mirroring store/ — short.txt (80 chars), full.txt (4000), a 1024x500 feature graphic from the existing tools/store-shots pipeline, phone and tablet screenshots — rather than typing them into the Console by hand. Fill Data safety from the same source as the App Privacy card so the two cannot drift, and note that because every ad request is tagged child-directed (src/game/ads.ts), Play's Families policy applies and brings its own ads-SDK certification questions. The only thing worth doing before then is one line in STATE.md saying Android is deliberately parked, so the next review stops re-filing it as a launch blocker.

### 3. [minor · large] tests-tooling: Roughly half the suite asserts on source text rather than behaviour: 131 readFileSync sites and ~248 regex/includes checks against component source; 29 src files are 'tested' only by grepping their text (DiceDemoScreen.tsx, ThemedArena.tsx, TutorialScreen.tsx, BottomNav.tsx, Popup.tsx, InventoryScreen.tsx, StoreScreen.tsx, TwoPlayerScreen.tsx, Icon.tsx, adSdk.ts, DieMesh.tsx, Prisoners.tsx …). Many are over-fitted to exact formatting, so a rename or reflow fails the test while a behaviour change that preserves the text passes.

- **Evidence:** I recounted independently: 110 literal `readFileSync(` call sites across tests/*.ts (screen 38, arenaShape 10, tutorial 9, icons 9, layout 7, ads 6 …), 275 `.test(`/`.includes(` occurrences across the 30 test files, and 98 src files of which 53 are imported by a test and ~30 are touched only as text — Icon.tsx among them (tests/icons.test.ts:15 readFileSync, plus a regex about it at tests/screen.test.ts:1612). The map's 131/~248/29 are the right shape. Cited regexes verified verbatim: tests/tutorial.test.ts:310, tests/layout.test.ts:172, tests/screen.test.ts:1514. Suite is green — 549 passed, 0 failed, 30.5s.

The fact that settles the severity dispute, which neither reviewer surfaced: THERE IS NO RENDERER IN THE TOOLCHAIN. devDependencies are @types/react, @types/three, tsx, typescript, yoga-layout — no react-test-renderer, no @testing-library, no jest or vitest (the jest-* dirs in node_modules are transitive from Expo, and package.json's `test` is `tsx tests/run.ts`, a hand-rolled harness importing 30 suites). So for a .tsx component there is currently no way to assert behaviour at all; source-text matching is not laziness, it is the only tool present. 'Convert these to behavioural tests' is really 'add a renderer plus mocks for react-native, expo-gl and three' — a toolchain change, not a test rewrite. Both reviewers argued severity without checking whether the alternative exists.

- **Do this:** Skip for launch — siding with the impact reviewer on the call, and more firmly than they did, because the conversion is not merely expensive but currently impossible without adding a React renderer and mocking react-native, expo-gl and three in a suite that deliberately runs headless in node. Do NOT adopt the skeptic's 'needs-decision' framing: which assertions are worth converting is an engineering judgement, not a product choice like a name or a price, and putting it to David gets a question he has no basis to answer. The practical policy, no project-wide project needed: (1) when a text assertion fails on a rename or reflow, replace THAT ONE with a behavioural test or a shape-match rather than tightening the regex again — tests/layout.test.ts:166-171 and tests/screen.test.ts:970-974 already do exactly this and say so in comments, so the convention exists; (2) keep new logic in pure modules under src/game/ where the existing harness tests it properly, so the grep-only share shrinks as the app grows rather than by rewriting what is there. The genuinely valuable coverage work is item 2 of this batch (applyMatchResult), which needs no new tooling. Revisit adding a renderer after 1.0, never in the run-up to a submission where AGENTS.md's own gate forbids changes after the final pass.


## F. Refuted claims (0) — do not act on these
